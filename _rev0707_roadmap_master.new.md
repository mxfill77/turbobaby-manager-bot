KB_ROADMAP_MASTER — ПОЛНЫЙ СВОД ПЛАНОВ TurboBaby (локальные + глобальные). Создано 19.06.2026 штабом.
НАЗНАЧЕНИЕ: единая точка для нового чата / Claude Code — ВСЕ открытые планы в одном месте, по приоритету, с указанием ИСПОЛНИТЕЛЯ. Читать вместе с project_state (вечное состояние) + cc_log (журнал Claude Code). При чистке мозга (задача П1) — этот roadmap свести в project_state или оставить как живой план-лист.

────────────────────────────────────────────────────────
АКТУАЛИЗАЦИЯ 07.07.2026 ВЕЧЕР (РЕВИЗИЯ по вехе: ЛЕСТНИЦА О4 СТ1–СТ4 ЗАВЕРШЕНА на VPS-полосе) — ЧИТАТЬ ПЕРВЫМ
────────────────────────────────────────────────────────
АВТОРИТЕТ ПО ГЛАВНОЙ ОСИ = KB_MASTER Раздел 4 (актуализирован тем же вечером). Здесь — сверка backlog.
- МЕТА-ДИРИЖЁР СОБРАН ЦЕЛИКОМ 06–07.07, СТ4 ЗАВЕРШЁН на VPS-полосе: самопочинка шага декомпозера
  (784e453, STEP_SELFHEAL) + фикс класса «самомодификация → ложный failed» (48d9c64) + адаптация плана
  keep/adjust/finish (8ffbf7f, PLAN_ADAPT, самотесты А–Е) + самопочинка одиночных «тз:»/«задача:»
  (9b0a496). Гейт 59 зелёных. ЛЕСТНИЦА СТ1–СТ4 ПРОЙДЕНА.
- Живые цепи «декомпозируй:» 106 и 113 — по 3/3 done. ПЕРВОЕ ЖИВОЕ РАСХОЖДЕНИЕ реестра знания
  (цепь 113 шаг 2: чужой хеш f926733 из ПК-репо userbot) закрыто двойным фиксом 9a695fb
  (FOREIGN_REPO_MARK + переатрибуция f926733→userbot suggest в KB_MASTER).
- ХВОСТ финзаписей окна обвала API 06.07 03:18–06:14 ЗАКРЫТ владельцем руками 06.07 (баланс поправлен
  вручную) — снят из KB_MASTER Раздела 7.
- ОТКРЫТЫЙ ХВОСТ: функциональная обкатка думателя — ждёт естественных провалов в проде (самопочинка
  шага/одиночной, adjust/finish, 🔁 планового рестарта); страховки на месте (KB_MASTER Раздел 7).
- НОВЫЙ ГОРИЗОНТ ОСИ (кандидаты, ВЫБИРАЕТ ВЛАДЕЛЕЦ): (а) думатель на полосу pc (pc_orchestrator,
  отдельный код на ПК, канал Dispatch) — ПРЯМОЕ ПРОДОЛЖЕНИЕ ОСИ; (б) кирпичи реестра знания
  (клиентский FAQ↔Лист1 модели, карта↔таблицы, интерактивная регистрация); (в) операции O3
  (бронь/выдача/приём). Детали — KB_MASTER Раздел 4 🧭.

────────────────────────────────────────────────────────
АКТУАЛИЗАЦИЯ 07.07.2026 УТРО (РЕВИЗИЯ; веха O4-ст2 ЗАКРЫТА, клиентский контур ушёл вперёд)
────────────────────────────────────────────────────────
АВТОРИТЕТ ПО ГЛАВНОЙ ОСИ = KB_MASTER Раздел 4. Здесь — сверка backlog.
- O4-СТУПЕНЬ 2 ЗАКРЫТА (обкатана 05.07). СТУПЕНЬ 3 полуавтономный НАЧАТ 06.07 — первые кирпичи в проде:
  • единый инбокс подтверждений (08c6086, INBOX_TOPIC_ID=1160, /inbox, Вариант Б);
  • разрыв петель конвертов op=other (fa90193, 3 слоя в orchestrator_daemon).
  АКТИВНЫЙ следующий кирпич ст3 = АВТО-ЭСКАЛАЦИЯ результата к думательному слою (мета-дирижёр).
- §12 SPLINTER LLM переведён с платного API на подписку Max (Фаза1 95121f3+d8acd35 / Фаза2 громкий провал
  d946f92 / изоляция тест-шума 46ba002 / spend_ledger 1ae7559). ХВОСТ: финзаписи, потерянные в окне обвала
  API 06.07 03:18–06:14 UTC — ручное внесение владельцем (🔴 «да»).
- КЛИЕНТСКИЙ КОНТУР (userbot ПК) УШЁЛ ВПЕРЁД 06.07: генерирует черновики (Max), трёхуровневая модерация,
  вычищен и научился учиться — модели→Лист1 (8bff82a), playbook.md (a774435), обучение/«Запомнить» (dbe727b),
  A3+SALES_PRESSURE (8cd68a6), язык/приветствие (5a7650e), утечка контекста (f0b9316). TEST_MODE=on (в модерацию,
  клиенту не уходит). ДЕПЛОЙ последних коммитов (рестарт 205) в cowork_log НЕ подтверждён. NEXT = Фаза 3 (отложено владельцем).
- П6 (userbot FAQ RU+EN) перерос в клиентский бот-менеджер с playbook — статус см. KB_MASTER Раздел 3.

────────────────────────────────────────────────────────
АКТУАЛИЗАЦИЯ 03.07.2026 (ступень 2 O4 собрана); ниже актуализация 29.06 и исходный план от 19.06
────────────────────────────────────────────────────────
- ДЕВ-БОТ/ОРКЕСТРАТОР = СТУПЕНЬ 2 в проде (02–03.07): A+B произвольные «тз:» из темы 328 (HQ-форум «TurboControl»,
  chat -1003853365891) через headless Claude Code (b846c5f); Q2 restart splinter headless-CC делает САМ оранжевым
  циклом гейт→restart→проверка→отчёт (7d840e2); C-декомпозер «декомпозируй:» → планировщик → шаги [шаг i/N
  родитель id] по одному → сводка (2733f17); фикс заморозки event loop от синхронных Bridge-вызовов (3978a91).
  Красное — ТОЛЬКО кнопкой Филиппа.
- Хвост «O4 декомпозер» из списка 29.06 ЗАКРЫТ; открытый хвост — ОБКАТКА ступени 2 → ступень 3 (KB_MASTER Раздел 7).
- ДОКТРИНА ПОДТВЕРЖДЕНИЙ (02.07, ea92795): ask только там, где владелец добавляет информацию (бизнес-решения);
  техника проверяется тестами+гейтом+бэкапом+откатом, не человеческим «да».
- П7 (наблюдение дев-бота → полный оркестратор) идёт по главной оси O4 — актуальный статус см. KB_MASTER Раздел 4.
────────────────────────────────────────────────────────
АКТУАЛИЗАЦИЯ 29.06.2026 (ревизия, веха 4255 закрыта); ниже исходный план от 19.06
────────────────────────────────────────────────────────
АВТОРИТЕТ ПО ГЛАВНОЙ ОСИ = KB_MASTER Раздел 4 (O1-O4). Здесь — расширенный backlog (P1-P11), сверять с осью.
ЧТО ЗАКРЫТО С 19.06:
- O1 Heartbeat/вехи ✅, O2 Касса A/B/C ✅ (в проде). O3 наблюдатель выдач — СЛОЙ 1 в проде, слой 2 отложен (хвост).
- ЦЕПОЧКА ТО доведена (веха 4255, 29.06): кнопочные подтверждения 73597e9 / дедуп A 2807718 / тайский C 6d3addc /
  задержка D 5572c7b — всё в проде.
- П1 чистка мозга — выполнена (27.06 перестройка: KB_MASTER + своды KB_RULES/KB_INFRA/KB_STATE_MODEL, дубли в _archive).
- Постоянная кнопка тайцам (обслуживание) — recon готов, ждёт выбора типа штабом (Раздел 4 / Раздел 7 KB_MASTER).
ОТКРЫТЫЕ ХВОСТЫ — единый список в KB_MASTER РАЗДЕЛ 7 (kind bearing, O3 слой 2, Этап 2 трекинга, мост контуров,
клиентский путь C1-C4, O4 декомпозер). P-номера ниже актуальны для userbot/железа (P3-P11).
────────────────────────────────────────────────────────

ВАЖНО ПРО ИСПОЛНИТЕЛЕЙ (карта в KB_executors_map):
- ШТАБ (Claude-на-сайте) = стратегия, ревью, чтение Brain. Пишет в Brain Drive-коннектором (ТОЛЬКО create → плодит дубли; для записи в журналы НЕ годится, использовать редко).
- CLAUDE CODE (VPS) = код на VPS, git, Bridge (write_doc plain-text БЕЗ дублей, clasp деплой), Splinter. Достижим через Termux SSH.
- COWORK (ПК, Linux-песочница) = ФАЙЛЫ userbot на ПК, git (push блокирован прокси → пушит Филипп с Windows). НЕ может Windows-команды (стоп/старт служб, tasklist, админ). mount существующих файлов видит с задержкой → компиляция/коммит правок с Windows.
- PC_AGENT (бот на Windows, тема 205) = управление ПРОЦЕССАМИ userbot с телефона (старт/стоп/статус/сводка/обновись). Команды только от Филиппа 504608015.
- ФИЛИПП = Windows PowerShell (всё на ПК), но стремится рулить с ТЕЛЕФОНА.

════════════════════════════════════════════════════
ГЛОБАЛЬНАЯ ЦЕЛЬ
════════════════════════════════════════════════════
AI-инфраструктура TurboBaby (премиум аренда мотобайков Пхукет, ~38 байков, владелец Филипп не программист, работает разговором с телефона). ДВА КОНТУРА, общий мозг (Brain на Drive), НЕ смешивать:
- ВНУТРЕННИЙ (VPS): учёт/ТО/касса/аудит/автономия. Язык RU+TH (тайцы).
- КЛИЕНТСКИЙ (ПК→Pi): общение с клиентами. Язык RU+EN (без тайского, тайцы с клиентами не общаются).
СВЕРХ-ЦЕЛЬ ФИЛИППА: рулить ВСЕМ с телефона, не садиться за комп. Финал клиентского контура — userbot на Raspberry Pi 24/7 (чёрная коробка), ПК выключен.

════════════════════════════════════════════════════
СОСТОЯНИЕ (что РАБОТАЕТ в проде на 19.06)
════════════════════════════════════════════════════
ВНУТРЕННИЙ КОНТУР (VPS, репо turbobaby-manager-bot):
- Splinter (учёт/ТО/касса/аудитор) в проде, систем-сервис splinter.service.
- Фундамент автономии 1→4.3 ЗАВЕРШЁН: уведомления (notify.py) / здоровье (health.py + таймер 4ч) / обкатка / 4.1 чёрный ящик боевых записей / 4.2 токен-замок / 4.3 тесты-гейт.
- Дев-бот (п.5, полу-оркестратор) в проде, тема 328, обкатан вживую (ручные команды + утренняя авто-сводка ~08:00 Бангкок).
- Мозг: все 4 base-дока plain-text (cc_log/review/project_state/knowledge_base), износ Docs закрыт. project_state разделён: вечное (17.5KB) + KB_sessions_log (самочистится). 3 журнала под автопрореживанием (триггеры cc_log/review/sessions_log ~13:00-13:20 UTC).
- Темы HQ -1003853365891: 161 Аудит / 205 pc_agent / 328 dev-bot. Группа Входящие -1003997419806 = будущий intake A.

КЛИЕНТСКИЙ КОНТУР (ПК, репо turbobaby-userbot, домашний Пхукет-IP):
- userbot_listen.py — Telethon от @turbophuket, слушает входящие ЛИЧНЫЕ, ТОЛЬКО логирует, НОЛЬ исходящих, singleton-lock. Работает.
- pc_agent.py — управление с телефона (тема 205). ФИКСЫ 1+2+3 в проде (commit 6404715 + 1ed503d):
  * ФИКС 1 error-handler — сетевые обрывы (httpx.ReadError) не роняют агента.
  * ФИКС 2 авто-реадопшн — агент при старте сам поднимает userbot (вылечена 2-дневная смерть userbot).
  * ФИКС 3 само-рестарт «обновись» из 205 — агент сам git pull + перезапуск, БЕЗ PowerShell. Работает (оговорка: воскрешение ~5 мин при сетевом обрыве вместо 20с).
  Страховка: задача Планировщика restart-on-failure (RestartCount=3, PT1M).
- collect_booking.py (этап 1 ЗАКРЫТ) — сборщик карточки брони из диалога, read-only, боевой промпт в git (commit ace11dc). Проверен на 3 диалогах.
- Журнал Cowork: ЛОКАЛЬНЫЙ D:\turbobaby-bot\cowork_log.md + git (штаб через Drive НЕ видит — см. задачу П2).
СПЕКИ в Brain (по id): KB_booking_flow (анализ 710 диалогов), KB_collect_booking_spec (промпт-парсер).

════════════════════════════════════════════════════
ОТКРЫТЫЕ ПЛАНЫ — ПО ПРИОРИТЕТУ
════════════════════════════════════════════════════

--- ПРОЦЕСС / ГИГИЕНА (делать ПЕРВЫМ, иначе штаб плодит дубли) ---
П1. ЧИСТКА МОЗГА (Claude Code через Bridge). Свести 7 дублей userbot-журнала в ОДИН plain-text KB_userbot_log (create_brain_plain + склейка + регистрация в манифесте ключ userbot_log). Дубли (id в KB_TODO_brain_cleanup_and_cowork_bridge): KB_claude_userbot_log, KB_userbot_log_v2, _update_0618, _update_0619, _cowork_0619, _fixes_confirmed_0619, _fix3_0619. Старые → удалить через DriveApp ПОСЛЕ сверки контента (осторожно, необратимо, подтвердить с Филиппом). Также свести: этот KB_ROADMAP_MASTER, KB_executors_map, KB_TODO_* — решить что в project_state, что отдельным план-листом. НЕ трогать booking-спеки (не дубли).

П2. ОБЩИЙ МОЗГ ДЛЯ COWORK через Bridge (Claude Code). Чтобы штаб видел работу Cowork по «проверь». Сейчас Cowork пишет в локальный cowork_log.md (штаб не видит). Решение: plain-text док cowork_log в Brain (create_brain_plain, манифест ключ cowork_log) + дать Cowork URL+BRIDGE_TOKEN + научить слать write_doc (Bridge доступен с любого IP). Тогда петля Cowork↔штаб как с Claude Code. Связано с П1 (обе про Bridge).

--- КЛИЕНТСКИЙ КОНТУР (userbot) ---
П3. УЛУЧШИТЬ ВОСКРЕШЕНИЕ pc_agent (Cowork правит + ревью штаба). Сейчас ДВЕ системы воскрешения (detached-помощник ФИКС 3 + restart-on-failure Планировщика) — могут конфликтовать, неясно кто сработал. Оставить ОДНУ надёжную. Нюанс: os._exit(0) — чистый выход, не «падение» → restart Планировщика может не триггерить надёжно; помощник контролируемее. Ускорить/застраховать при сетевых обрывах (агент висит на подключении к Telegram). СВЕЖИЙ план.

П4. ЭТАП 2 СБОРЩИКА (Cowork + штаб межконтурно). collect_booking карточку → отправка в группу Входящие -1003997419806 → Splinter принимает (intake A). ПЕРЕД ЭТИМ: живой тест что Splinter в Входящих реально принимает карточку (intake A строился, но карточкой живьём НЕ тестировался — проверяет штаб через Termux со стороны Splinter). Решить триггер: через pc_agent («собери бронь @X») или вручную. Сборщик остаётся read-only к клиенту (карточка показывается/уходит во внутренний контур, клиенту НИЧЕГО не пишет).

П5. CRLF-ЧИСТКА репо turbobaby-userbot (Cowork). Шум CRLF↔LF (.gitignore, bot.py, list_ops_groups.py, scout.py, topics_map.json) → git pull --ff-only в «обновись» может спотыкаться если origin тронет эти файлы. Нормализовать (.gitattributes + renormalize). Мелочь, но разблокирует надёжность ФИКС 3.

П6. userbot FAQ-ОТВЕТЫ RU+EN (Cowork + штаб). На базе turbobaby_faq_v1.md (топ-темы: цена/выбор/оплата/доставка/сроки/депозит). Сначала черновики-менеджеру → полуавтомат → узкие авто-сценарии. БЕЗ тайского. Никаких рассылок (только ответы на входящие). Это шаг к клиент-боту.

--- ВНУТРЕННИЙ КОНТУР (Splinter) ---
П7. НАБЛЮДЕНИЕ ДЕВ-БОТА → ПОЛНЫЙ ОРКЕСТРАТОР (Claude Code). Дев-бот обкатывается; следующий уровень — оркестратор, СТРОГО с правилом: красную зону сам не проходит. Сначала период наблюдения (утренние сводки, ручные команды), потом расширение. Не инициативно — по задачам Филиппа.
П8. (опц.) LLM-проверка перевода в аудиторе (естественность тайского, совпадение смысла блоков). Отложено.

--- ЖЕЛЕЗО / ИНФРАСТРУКТУРА ---
П9. RASPBERRY PI (НЕ куплен — блокер). Когда купят: перенос userbot (session/.env/код) на Pi + systemd-автозапуск (на Linux просто, в отличие от Windows) + Tailscale на Pi (доступ с телефона по SSH для правок кода) + pc_agent на Pi. Тогда ПК ВЫКЛЮЧЕН, чёрная коробка на Pi. ЭТО ФИНАЛ сверх-цели «не садиться за комп». Автозапуск userbot на Windows-ПК НЕ делаем (двойная работа — на Pi проще). Диагностика автозапуска ПК (за 19.06): задача pc_agent = LogonTrigger + InteractiveToken + AutoAdminLogon=0 → после холодного ребута ПК висит на пароле, userbot не встаёт без входа Филиппа. На ПК НЕ чиним (отложено до Pi).
П10. WHATSAPP Cloud API (после клиент-бота). Виртуальный номер (НЕ личный бизнес-номер), легально по политике Meta. Клиент-бот: сначала Telegram, потом WhatsApp. Отложено до стабильного клиент-бота.

--- БЕЗОПАСНОСТЬ ---
П11. EdenAI key (OCR паспортов клиентов) + GitHub PAT — ОТОЗВАТЬ. ОТЛОЖЕНО по решению Филиппа до 17.08.2026. НЕ поднимать раньше. После 17.08.2026 — напомнить один раз.

════════════════════════════════════════════════════
ГРАНИЦЫ БЕЗОПАСНОСТИ (НЕ ослаблять никогда)
════════════════════════════════════════════════════
- userbot ТОЛЬКО на домашнем Пхукет-IP. НЕ строить мостик VPS→личный аккаунт (бан @turbophuket с 1798 диалогами). Tailscale = доступ К машине, НЕ перенос на VPS.
- userbot клиенту НИЧЕГО не пишет на текущих этапах (zero-out гард). Только читает/логирует/собирает карточки.
- Splinter сам деньги не двигает; изменения в Sheets через approve-флоу.
- Боевые записи (confirmed=true, Лист1/CRM, деньги, delete_event, sqlite3, clasp) — красная зона, строго «да» Филиппа.
- Важные цифры с фото (пробег, суммы) — всегда подтверждать у человека (vision ошибается на LCD).
- Никаких рассылок клиентам.
- Бизнес-правила: депозит деньги ИЛИ паспорт (не оба); CLICK 125 не сдаём; доставка только Пхукет; интервал ТО масла скутеры 4000 / мото+XADV 5000; ТО Oil из колонки I.
- Источник правды кода = git. Brain (KB_*) = зеркало для документов.

════════════════════════════════════════════════════
УРОКИ СЕССИИ 18-19.06 (чтобы не повторять)
════════════════════════════════════════════════════
- Правки СУЩЕСТВУЮЩИХ файлов через Cowork-Edit: mount-кэш песочницы показывает устаревший/битый вид → компиляцию (py_compile через venv) и git-коммит делать с WINDOWS, не из песочницы. Cowork правит файл → Windows компилирует+коммитит.
- Cowork НЕ может Windows-команды (Linux-песочница). Процессами Windows рулит pc_agent (с телефона) или Филипп. «Cowork сам остановит/запустит службу» = невозможно.
- Штаб Drive-коннектором плодит дубли (только create). Запись в Brain журналов — через Bridge (Claude Code), plain-text, перезапись. Штаб — для чтения и редких документов.
- Одна активная среда за раз (Cowork ИЛИ Termux ИЛИ штаб дают команды) — не параллельно, иначе гонка.
- Проверять результат ФАКТОМ (tasklist/локи/py_compile с Windows), не доверять отчёту вслепую (особенно после mount-глюка).

ПОРЯДОК СТАРТА следующего захода: рекомендуется П1+П2 (чистка мозга + общий мозг Cowork) через Claude Code свежим заходом — пока не сделано, штаб продолжит плодить дубли, и не видит Cowork по «проверь». Затем П3/П4 по userbot.