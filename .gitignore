# === СЕКРЕТЫ — НИКОГДА не коммитить ===
.env
.env.*
*.env
.env\ ex.txt
*.session
*.session-journal
.wwebjs_auth/
.wwebjs_cache/

# === Память / данные бота (не нужны в репо, переезжают отдельно) ===
memory.db
memory.db-journal
*.sqlite
*.sqlite3

# === Python ===
venv/
.venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# === Логи ===
*.log
logs/
# журнал взятий задач в работу (рантайм-состояние демона, не код)
orchestrator_claims.jsonl
# реестр успешных записей в cc_log (следы работы для правды статуса; рантайм-состояние)
cc_log_ledger.jsonl
# ключи дедупа постановки задачи (рантайм-состояние клиента, живёт окном ~10 мин)
enqueue_dedup*.json

# === Выгрузки / временное ===
topics_map.json
data_export/
exporttg*/
chats/
*.zip

# === IDE / ОС ===
.vscode/
.idea/
.DS_Store
Thumbs.db

# --- vps-git-cleanup 2026-07-24: runtime state + scratch ---
# runtime state (git rm --cached; disk copy is source of truth)
spend_ledger.json
verified_facts.json
wallet_cache.json
_pcport185
# scratch / throwaway
_*.py
*.bak*
*.diff
*.lock
# боевая база вебхука wa-webhook: живёт на диске, в git не хранится (25.07.2026)
wa_queue.db
# 25.07.2026: остатки, не попавшие под правила (отслеживание было старше правил)
tests/test_cclog.py.bak-format3-20260716
tests/test_curator.py.bak-curhuman-20260712
tests/test_curator_devbot.py.bak-status285-20260713
tests/test_inprogress_report.py.bak-stallcap-20260713
tests/test_orchestrator_model.py.bak-execmodel-20260713
tests/test_proc_gate.py.bak
_allow_new_settings.json
_claspsplit_build.py
