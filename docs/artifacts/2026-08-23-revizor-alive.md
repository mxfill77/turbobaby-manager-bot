# Жив ли ревизор — и кто оборвал задачу 110 (23.08.2026, read-only)

Заход read-only: ничего не чинилось, не перезапускалось, не удалялось; в рабочие таблицы, в
зеркало `bridge_prod/` и в код правок нет. Источники: живой `cowork_log` (Bridge `read_doc`,
снят 23.08 14:45 UTC, 615 721 знак, 1789 непустых строк), `orchestrator_daemon.log`,
`journalctl -u orchestrator-daemon`/`-u splinter`/`-k`, состояние яруса 2
`/tmp/cc_expect_seen/state.json`, снимок ПК-порта `/root/_clones_backup_20260726/_pcport185.tar.gz`
(26.07, читался в память, не распаковывался). Черновики захода — `_scratch_revizor_alive/`.

Все времена — UTC. «Сейчас» захода — 23.08.2026 14:46 UTC.

---

## 5. ВЕРДИКТ: НЕИЗВЕСТНО

Не ЖИВ и не МЁРТВ, и это честный третий исход, а не уклонение. Что прочитано:

* последний тик — **22.08 16:29**, ему **22.3 ч** (порог доски `REVIZOR_TICK_HOURS`, дефолт 9 ч,
  давно перейдён — доска права, когда пишет «⚠️ тик старый»);
* **22 часа для него НЕ впервые**: за 30 суток промежутков ≥21 ч — **восемь**
  (23.95 · 24.10 · 24.32 · 30.15 · 36.13 · 72.15 · 83.38 · **96.37**), и после каждого он
  возвращался сам. То есть по одному возрасту тика «мёртв» не следует;
* **машина-носитель жива и пишет**: после последнего тика в журнале полосы ПК **146 строк**,
  наибольшая пауза 162 мин, последняя строка 23.08 **14:44** — за минуту до чтения. Значит
  молчит именно ревизор, а не весь ПК;
* **но и «сломан» из прочитанного не следует**: носитель за эти 22 часа НИ РАЗУ не прожил
  подряд свои ~6 часов хода (таблица в §1), а холостой прогон ревизор не отмечает ничем (§3).

**Чего не хватило, чтобы сказать точно.** Двух вещей, и обе называются прямо:
(1) следа холостого прогона — его не существует по устройству (§3), поэтому «нечего
ревизовать» и «не работает» с этой машины неразличимы; (2) кода и состояния ПК — свежего
исходника `pc_orchestrator` на VPS нет, единственный снимок от 26.07, и в нём ревизор
(`reviewer.py`) в оркестратор ещё НЕ подключён ни одной строкой — значит нынешнюю проводку
и её расписание отсюда прочитать нечем.

**Проверка, которая закроет вопрос и ничего не стоит.** Носитель поднят вотчдогом 23.08
**11:28**; если он доживёт до **≈17:28** (шесть часов хода) — обязан появиться тик:
* тик появился → **ЖИВ**;
* носитель прожил 6+ часов, тика нет → **МЁРТВ**, и это будет доказано, а не предположено;
* носитель снова умрёт раньше → диагноз прежний, а лечить надо не ревизора, а падения ПК-демона.

## 6. Ревизора НЕ поднимали и не трогали

Вердикт не «мёртв», поэтому поднимать нечего; но и будь он мёртв, лечение живёт НЕ на этой
машине. Что нужно сделать (не сделано намеренно — чужая полоса и запрет задания):

1. **дождаться шестого часа текущего сегмента** (см. выше) — это единственная бесплатная проба;
2. **на стороне ПК**: заставить холостой прогон писать строку («0 окон, нечего ревизовать»).
   Пока её нет, у молчания ревизора нет разницы между «работы не было» и «он не работает» —
   это дыра наблюдаемости, а не свойство ревизора;
3. **на стороне ПК**: разобраться с падениями носителя — **6 подъёмов вотчдогом за сутки**
   против ~1 в сутки прежде (§1). Пока сегменты короче 6 часов, шестичасовой ход не наступит
   НИКОГДА, даже если ревизор совершенно исправен.

## 3. Холостой ход ревизора НЕ виден ничем (главное)

Разбор ЖИВЫМ шаблоном читателя доски (`devbot._REV_NOTE_RE`, второй копии не заводилось —
шаблон взят из исходника `devbot.py`): в журнале **56** строк ревизора за 30 суток. Виды:

| строк | вид |
|---|---|
| 29 | прогон: «N окон с активностью с прошлого прогона — пакеты собраны» |
| 16 | исход: owner-карточка выписана |
| 6 | исход: очередь недоступна — находки в спул |
| 3 | исход: owner-карточка принята |
| **1** | прогон: «1 окон, **чисто**» — единственная за месяц, 13.08 13:26 |
| 1 | исход: задачи дирижёру |

**Строк «0 окон» — НОЛЬ.** Все 32 «прогонных» строки называют N ≥ 1. Отсюда прямой ответ:
ревизор объявляет себя, только когда НАШЁЛ хотя бы одно окно с активностью; когда окон нет,
он не пишет ничего. Косвенное подтверждение из того же журнала: при штатном ходе ~6 ч за
598 часов окна ожидалось бы ≈100 прогонов, а видно **32 (32 %)**; и промежуток 22.08
10:28 ← 21.08 22:26 равен **12 ч 02 мин**, то есть РОВНО двум периодам — один прогон
состоялся и промолчал.

**Следствие, которое надо сказать прямо:** по следу ревизора состояния «нечего ревизовать» и
«ревизор не работает» НЕРАЗЛИЧИМЫ. Единственная строка «чисто» за месяц — это не холостой ход,
а «окно было, находок нет». Отметки «я проснулся и мне нечего делать» у него нет.

## 7. Задачу 110 оборвал OOM-killer ядра по лимиту памяти самой службы

Кто послал сигнал — найдено поимённо, гадать не пришлось.

```
14:07:35  NEW id=110 from=Filipp-328-dev text=ultrathink …
14:07:37  ИСПОЛНЕНИЕ id=110 через claude -p (timeout=2700s)
14:15:04  kernel: claude.exe invoked oom-killer … oom_memcg=/system.slice/orchestrator-daemon.service
14:15:04  kernel: memory: usage 4194304kB, limit 4194304kB, failcnt 4959
14:15:04  kernel: Memory cgroup out of memory: Killed process 1588511 (claude.exe)
                  total-vm:6565716kB, anon-rss:4091740kB
14:15:04  systemd: orchestrator-daemon.service: The kernel OOM killer killed some processes in this unit.
14:15:04,623  демон: «получен сигнал 15 — останавливаюсь после текущей итерации»
14:15:06  демон: id=110 claude -p exit=143
14:15:06  METRICS task=110 dur_s=448.38 outcome=failed tokens_in=5115528 tokens_out=24549
14:15:42  FAIL причина=exec_error id=110
14:15:44  task-selfheal: id=110 → думатель halt («claude убит SIGTERM извне»)
14:15:45  systemd: Failed with result 'oom-kill' (4G memory peak, 1.8G swap peak)
14:16:00  systemd: Scheduled restart job … Started orchestrator-daemon.service
14:16:26  devbot: карточка задачи 110 (failed) отправлена в 328, message_id=8321
```

**Цепь целиком:** исполнитель задачи 110 (`claude.exe`, PID 1588511) вырос до **4.09 ГБ**
anon-RSS при контексте **5 115 528** входных токенов и упёрся в **MemoryMax=4G** — лимит
цгруппы самой службы, заведённый владельцем 13.08
(`/etc/systemd/system/orchestrator-daemon.service.d/20-memory-limit.conf`). Ядро убило его
внутри цгруппы демона; systemd со штатной `OOMPolicy=stop` остановил юнит, то есть послал
**SIGTERM (сигнал 15)** — его демон принял в 14:15:04,623 и записал у задачи `exit=143`.
Это НЕ правило самомодификации, НЕ рестарт службы руками, НЕ вотчдог ПК и НЕ таймаут
(448 с из 2700 с).

**Про число 14:24:54 из задания.** В журналах его нет ни у одной записи. Оно нашлось в
состоянии яруса 2: `/tmp/cc_expect_seen/state.json` → `qstate.gone[110].at = 1787495094.4` =
**23.08 14:24:54 UTC** — это момент, когда десятиминутный НАБЛЮДАТЕЛЬ УВИДЕЛ, что строка ушла
из открытых, а не момент падения. Падение — 14:15:04–14:15:06.

**Владелец писал в тему байка — НЕ связано, и это проверяется числами.** В 14:10:59–14:13:09
splinter вёл разговор по байку NMAX 155 GREY 5960 (пробег 41346, три работы, запись не легла:
`oil_decreasing`/`km_decreasing`). Связи нет по устройству: (1) убийство было **цгрупповым** —
ядро прямо пишет `constraint=CONSTRAINT_MEMCG` и `oom_memcg=/system.slice/orchestrator-daemon.service`,
то есть выбран лимит ЭТОЙ службы; (2) `splinter.service` — другая служба и другая цгруппа, её
память в чужой лимит не считается; (3) машина в целом голодна не была — 7.7 ГБ ОЗУ, сейчас
свободно 3.9 ГБ. Совпадение по времени — совпадение.

**Побочно, из того же журнала:** перезапуск 14:16:00 живой счёт серии приписал как «ремонт
руками» («не приписан ни одной задаче»), хотя рук тут не было — это машинный OOM.

## 1. Чем ревизор запускается — что доказано, а что нет

**Доказано отрицательно, на этой машине его нет ни в каком виде:**

* среди systemd-юнитов и таймеров VPS ревизора НЕТ (21 таймер, ни одного его; служб
  проекта четыре: `splinter`, `orchestrator-daemon`, `wa-webhook`, `expectations`);
* `crontab -l` для root — «no crontab for root»;
* живого процесса с таким именем нет (`ps aux`);
* в `bot.py`, `splinter.py`, `orchestrator_daemon.py` слова «ревизор»/`revizor` нет ВООБЩЕ
  (0 совпадений) — изнутри splinter он не зовётся;
* всё, что на VPS есть, — это ЧИТАТЕЛИ его следа: `devbot._REV_NOTE_RE` (доска «статус»),
  `revizor_age.py` (возраст тика), `revizor_route.py` (маршрут находок замороженного контура).

**Доказано положительно, но по следу, а не по коду:** ревизор — часть ПРОГОНА ПК-оркестратора.
Основания: автор каждой его строки в журнале — `Orchestrator` полосы ПК; сам текст говорит
«с прошлого **прогона**»; в снимке ПК-порта 26.07 лежит `reviewer.py` — «периодический ревизор
качества черновиков (класс 23.07.2026)», сканирующий записи `moderation_ipc`
(статусы sent/ready/test_held) по трём чекам, с точкой входа `run_reviewer() → (n_windows,
findings)». Это ровно те «N окон», что печатает тик.

**Расписание — ~6 часов, ход внутрипроцессный** (вывод из формы данных, и он назван выводом):
в спокойные сутки тики идут цепочкой 03.08 01:15 → 07:15 → 13:18 → 19:21 → 04.08 01:25 →
07:27 → 13:28 → 19:31, то есть 6 ч + 1–4 мин дрейфа каждый раз. Накапливающийся дрейф — подпись
таймера «спать 6 ч ПОСЛЕ прогона» внутри живущего процесса, а не расписания по стенным часам.

**Чего прочитать нечем:** в снимке 26.07 `pc_orchestrator.py` (2589 строк) не упоминает
`reviewer` НИ РАЗУ — проводка сделана позже 26.07, её кода на VPS нет. Поэтому «чем именно
заводится сейчас» — по следу, а не по исходнику, и так и записано.

**Носитель и его падения (это и есть главный контекст молчания).** ПК-демон поднимается
вотчдогом ПК через `schtasks`. За сутки после последнего тика — **шесть подъёмов** (против
~1 в сутки прежде: 18.08 — 2, 06.08 — 1, 22.08 — 1):

| подъём | лежал | смерть носителя (расчёт) | сегмент жизни |
|---|---|---|---|
| 22.08 16:38 №1 | 8 м 44 с | ≈22.08 16:29 | — (тик 16:29 — последнее, что он успел) |
| 22.08 23:08 №2 | 1 ч 16 м | ≈22.08 21:52 | 5 ч 14 м |
| 23.08 00:18 №3 | 28 м 46 с | ≈22.08 23:49 | 41 м |
| 23.08 05:53 №4 | 2 ч 43 м | ≈23.08 03:10 | 2 ч 52 м |
| 23.08 06:08 №5 | 2 ч 58 м | ≈23.08 03:10 | ≈15 м (подъём №4, судя по числам, не взялся) |
| 23.08 11:28 №6 | 5 м 29 с | ≈23.08 11:22 | 5 ч 14 м |
| — | — | жив сейчас | 3 ч 20 м (на 14:46) |

**Ни один сегмент не дотянул до шести часов.** При внутрипроцессном ходе этого достаточно,
чтобы тика не было вовсе — без единой поломки в самом ревизоре. Числа «лежал» взяты из строк
вотчдога дословно; строка №5 сама себе противоречит и помечена как таковая.

## 2. Тики за 30 суток — числом

Окно тиков: 28.07 18:23 → 22.08 16:29 (598.1 ч). Строк ревизора 56, все со штампом.
Промежутки между ними (часы, сортировано):

```
0.02 ×4 · 0.03 ×10 · 0.05 ×2 · 0.07 ×4 · 0.08 ×2 · 0.35 · 0.73 · 0.77      ← внутри одного прогона
5.38 · 5.95 · 5.97 ×3 · 5.98 ×4 · 6.02 ×2 · 6.03 · 6.05 ×2 · 6.73          ← штатный ход ≈6 ч
11.83 · 12.00 · 17.37 · 18.02 · 18.22 · 18.32 · 18.95                      ← пропущено 1–3 хода
23.95 · 24.10 · 24.32 · 30.15 · 36.13 · 72.15 · 83.38 · 96.37              ← молчания сутки и дольше
```

Полный список отметок — `_scratch_revizor_alive/p2_ticks.json`. **Двадцать один-двадцать два
часа для него норма в том смысле, что случались и такие, и куда более долгие молчания: ≥21 ч
восемь раз за месяц, рекорд 96.4 ч (08.08←04.08).** Впервые это НЕ происходит.

## 4. Перезапуски splinter тик не съедали

Перезапуски `splinter.service` (journalctl): 22.08 15:05:19, 22.08 20:56:47, 23.08 09:59:51,
23.08 11:25:05, 23.08 13:41:31 — пять за сутки с лишним.

Тик 22.08 **16:29** стоит МЕЖДУ перезапусками 15:05 и 20:56, то есть родился уже ПОСЛЕ
ближайшего предыдущего, — съесть его тот не мог. Съесть его не мог и любой другой: тик пишет
ПК в свой журнал `cowork_log` (Drive), а splinter на VPS этот журнал только ЧИТАЕТ (`read_doc`,
без состояния между запусками). Доказательство сильнее рассуждения: я прочитал эту строку
23.08 в 14:45 — она пережила все пять перезапусков и лежит на месте.

## Побочная находка (соседний класс, не трогалась)

У яруса 2 ОТКРЫТ эпизод `o7_pulse_lost` (в `/tmp/cc_expect_seen/state.json`): периодическая
строка ПК о детях контура («ПУЛЬС · ПК … период 360 мин») последний раз вышла **19.08 05:42**,
то есть её нет **4.4 суток** при пороге 12 ч; эпизод насчитал 179 тиков наблюдателя. Это
ВТОРОЙ шестичасовой продукт ПК, который замолчал, и молчит он дольше ревизора. Ревизор при
этом после 19.08 тикал семь раз — значит таймеры у них разные, но обоих стоит смотреть вместе.

## FACT

* `FACT: read-only` — ни один файл вне `docs/artifacts/` и `_scratch_revizor_alive/` не
  изменён, ни один сервис не тронут, к ПК и ревизору обращений нет ни одного.
* `FACT` живого чтения: `read_doc name=cowork_log` → `ok=True`, id `1s9Fy3xm4FB99ah9xovLjYIMOjnDKz3Jc`,
  615 721 знак, последняя строка «NOTE 2026-08-23 14:44 UTC: Dispatch 23.08 22:15: шаг 9…».
* `FACT` ядра: `Memory cgroup out of memory: Killed process 1588511 (claude.exe)
  … anon-rss:4091740kB`, `usage 4194304kB, limit 4194304kB`,
  `oom_memcg=/system.slice/orchestrator-daemon.service`.
