# 69x.2109 — МОЖЕТ ЛИ МАШИНА СЧИТАТЬ СВОИ ДВОЙНЫЕ ОТВЕТЫ СВОИМИ ЗАПИСЯМИ

docs artifacts 2026 09 21 69x MOLCHITARHIV 2109 md

Дата: 2026-09-21. Полоса: vps. Ключ задания: 69x.2109.
Вид работы: РЕШЕНИЕ С ДОКАЗАТЕЛЬСТВОМ, read-only. Боевых записей нет, процессы не трогались,
ничего не удалялось, ни одного сообщения никуда не отправлено.

---

## 0. ОТВЕТ ЦЕЛИ ОДНОЙ СТРОКОЙ

**НЕТ.** Сегодня машина НЕ может ответить своими записями на вопрос «сколько раз бот ответил
дважды на ОДНО входящее». Законная ветвь отказа («такие записи уже ведутся») НЕ сработала и
проверена поимённо: захват переписки есть, но у ИСХОДЯЩЕГО сообщения не сохраняется НИ ОДНОГО
поля — ни своего, ни связи с входящим.

Самое дорогое в находке — не отсутствие, а **выброс**: число ответов на одно входящее машина
СЧИТАЕТ в момент обработки (`_SPOKE`, `splinter.py:95` · `splinter.py:156` · `splinter.py:9957`
· `splinter.py:9898`) и выбрасывает, не записав. Вопрос цели измеряется одной уже существующей
величиной, у которой просто нет двери на диск.

---

## 1. ПРИЗНАК СДЕЛАННОСТИ

Первым действием открыт адрес результата: в `docs/artifacts/` файла со словами `MOLCHITARHIV`
не было ни одного (поиск по всему каталогу, 21.09). Четыре вопроса не отвечены нигде. Работа
велась с ПУНКТА 1. Наличие соседних файлов за 21.09 (`69y`, `70a`, `70b`) ничего не
останавливало — предмет у них другой.

## 2. ПРЕМИСА ШТАБА — ПРОВЕРЕНА ПЕРВЫМ ДЕЙСТВИЕМ, ОПРОВЕРГНУТА ЧАСТЬЮ

Премиса описывала диск машины ПК (хранилище `chatlog`, одна строка на сообщение, поля `mid`,
`topic_id`, `topic_name`, `media`; последние дни 04–05.09; тем форума Обслуживания нет; файла
`splinter.py` нет вовсе). Сервер устроен ИНАЧЕ — принимать на веру было нельзя, и вот чем:

| утверждение премисы | на сервере | чем доказано |
|---|---|---|
| хранилище `chatlog` | **нет вовсе** | поиск имён `chatlog`/`chat_log`/`dialog_log`/`message_log`/`msg_log` по всем `*.py` — 0 попаданий; каталогов с такими именами нет |
| поля `mid`, `topic_name`, `media` у строки | **нет ни одного** | схема `memory.py:22-32` |
| тем форума Обслуживания в списке групп нет | **ЕСТЬ, 21 различная тема** | таблица `conversations`, `chat_id = -1002751134848` |
| `splinter.py` отсутствует | **есть, 9977 строк, 709 156 байт** | `ls`, `wc -l` |
| последний день записи 04–05.09 | **19.09** у базы, **21.09** у журнала | `max(timestamp)`; метки журнала |

Совпало одно: захват действительно существует и он «одна строка на сообщение». Всё остальное —
свойство чужой машины, к серверу отношения не имеющее.

## 3. ВРЕМЕННОЕ МЕСТО — НАЗВАНО ЯВНО

Задание назначило `/tmp/shtab_69x_2109/`. **Создать его не удалось:** песочница рабочих
каталогов движка разрешает запись только в `/root/turbobaby-manager-bot`,
`/root/turbobaby-bridge-gs` и каталог памяти — `mkdir` отклонён до исполнения. Это известный
класс (тот же отказ записан остатком 19.08 про `/tmp/tb_scratch`).

Следствие: **разведка прошла БЕЗ единого временного файла.** Ни одного файла вне этого артефакта
не создано ни в `/tmp`, ни в корне репозитория; удалять нечего, запрет на уборку соблюдён сам
собой. Пробных скриптов не запускалось: читался код, а две команды к базе и журналу —
**только чтение** (`sqlite3` открыта URI-режимом `mode=ro`, журнал открыт на чтение).

`.env` и конфиги не читались. Читался КОД, который их читает: `splinter.py:118-119` (`os.getenv`),
`bot.py:60-62`. Секретов в артефакте нет ни одного; переписка наружу не выведена ни строкой —
ниже только числа, slug-и групп, адреса файл-строка и имена полей. Идентификаторы личных чатов
(4 штуки) намеренно не печатаются: это ссылки на людей.

---

## 4. ПУНКТ 1. ЖИВОЙ ЗАХВАТ ПЕРЕПИСКИ — **НАЙДЕНО** (и он частичный)

Исход НАЙДЕНО, а не НЕИЗВЕСТНО: корни названы путём, прочитаны и дали числа.

### 4.1. Корень А — база `memory.db`, таблица `conversations`

**Путь корня:** `/root/turbobaby-manager-bot/memory.db` (`memory.py:18`, путь считается от каталога
модуля). Схема — `memory.py:22-32`.

**Поля строки поимённо:** `id`, `timestamp`, `chat_id`, `user_id`, `user_name`, `role`
(`user`/`assistant`), `content`, `is_voice`, `topic_id`.

**Числа (чтение `mode=ro`, 21.09):**

* строк всего **459**;
* первая `2026-05-28T16:23:20`, последняя `2026-09-19T08:26:56`;
* **последний день записи — 19.09.2026**;
* различных `chat_id` — **8**: групп **4**, личных чатов **4**.

**Группы и их slug-и** (slug берётся из `GROUPS`, `splinter.py:223-230`, и `GROUP_NAMES`,
`splinter.py:236-243`):

| chat_id | slug | mode | строк user / assistant | различных тем | последняя запись |
|---|---|---|---|---|---|
| -1002751134848 | Обслуживание | servicing | 130 / 130 | **21** | 12.09.2026 |
| -1003853365891 | HQ TurboControl (дев-форум) | — | 59 / 58 | 5 | 19.09.2026 |
| -1003909369438 | Самоорганизация | money | 12 / 12 | 8 | 04.09.2026 |
| -1003873906891 | Money Cashflow | money | 17 / 16 | 6 | 24.07.2026 |

**Темы форума Обслуживания в захвате ЕСТЬ — прямо и числом: 21 различный `topic_id`.** Они и
заводятся под каждый байк (`SERVICING_CHAT = -1002751134848`, `splinter.py:1056`). Групп
`Delivery` (-1002445921469), `Отметка` (-1003966216195) и `Входящие брони` (-1003997419806) в
захвате нет ни одной строки — они обслуживаются ветками, которые в базу не пишут (см. 4.3).

### 4.2. Корень Б — журнал `splinter.log`

**Путь:** `/root/turbobaby-manager-bot/splinter.log`, **75 280 004 байта**. Настройка —
`bot.py:65-68`, уровень INFO, **ротации нет** (`basicConfig` без `RotatingFileHandler`, поиск имён
`maxBytes`/`backupCount` дал 0).

**Одна строка на ВХОДЯЩЕЕ**, рождается `splinter.py:9943`:
`Splinter [{mode}] {chat_id} @{username}: {text[:80]}`.

**Поля строки поимённо:** метка времени, уровень, имя логгера, `mode`, `chat_id`, `@username`,
первые 80 знаков текста. **Нет:** `message_id`, `topic_id`, имени темы, признака медиа.

**Числа:** дней с записями **113** (01.06.2026 — 21.09.2026), строк «входящее» **5249**.
Последние сутки: 18.09 — 43, 19.09 — 18, 20.09 — 3, **21.09 — 45** (плюс 5 срабатываний пола
ответа).

### 4.3. Почему захват ЧАСТИЧНЫЙ — это главный ограничитель всего дальнейшего

`save_message` зовётся **ровно из четырёх мест, и все четыре — в `bot.py`**: `bot.py:534`
(входящее в мозг), `bot.py:623` (ответ мозга), `bot.py:1047` и `bot.py:1072` (голосовая ветка).
**В `splinter.py` вызова нет ни одного** (поиск по всему файлу — 0 попаданий).

Значит в базу попадает только то, что прошло через мозг (`manager_reply`, `bot.py:520`), а
детерминированные ветки обслуживания, денег, доставки, отметки и приёмки
(`splinter.py:9960-9969`) не пишут в неё ничего. Контраст числом: **260 строк по Обслуживанию за
3.5 месяца в базе против 5249 входящих в журнале**.

### 4.4. Корень В — лист событий Bot Data (входящий `message_id` ЕСТЬ, но не для этого)

`bridge.add_event(..., msg_id=f"{chat_id}:{msg.message_id}")` — `splinter.py:9017`, `9861`,
`9878`, `8998`, `3418`, `3612`, `3681`. Здесь **идентификатор входящего сообщения действительно
сохраняется**, и это единственное такое место во всей системе. Но роль поля — ключ
идемпотентности записи факта (ТО, работа, проводка), а не след разговора: строка рождается
только у веток, которые пишут событие, и об ответах бота не говорит ничего.

---

## 5. ПУНКТ 2. СВЯЗЬ ИСХОДЯЩЕГО С ВХОДЯЩИМ — **ПОЛЯ СВЯЗИ НЕТ НИ ОДНОГО**

**Место отправки названо адресом:** `splinter.py:122` — `async def _send(...)`; сама отправка —
`splinter.py:148`, `sent = await context.bot.send_message(**kw)`. Это **единственная дверь** к
`send_message` во всём модуле, и так сказано в самом коде (`splinter.py:149-150`).

**Что эта дверь СОХРАНЯЕТ: ничего.** Ни строки в файл, ни строки в базу, ни строки в лист. Полный
перечень того, что она делает с фактом отправки:

1. `splinter.py:156` — `_SPOKE.set(_SPOKE.get() + 1)`: счётчик в `ContextVar`
   (`splinter.py:95`), живёт до конца обработки ОДНОГО обновления и умирает вместе с ним;
2. `splinter.py:159` — `return sent`: объект отдаётся вызывающей ветке;
3. `splinter.py:131-140` — прогон текста через надзор (см. ниже), который в здоровом случае тоже
   не пишет ничего.

**Поля, которые дверь ВИДИТ** (это аргументы вызова, а не сохранённые поля — различие
существенно): `chat_id`, `text`, `message_thread_id`, `bilingual`, `group`, `reply_markup`,
`parse_mode`. Ключ `reply_to_message_id` в `kw` (`splinter.py:141-147`) **не кладётся вовсе** —
то есть ответ бота не связан с входящим даже средствами самого Telegram.

**Где исходящее всё-таки оседает — два узких пути, и связи нет ни в одном:**

* `bot.py:623` — `memory.save_message(chat_id, None, "Bot", "assistant", answer, topic_id=_topic)`.
  Поля поимённо: `timestamp`, `chat_id`, `user_id` (всегда пусто), `user_name` (всегда `Bot`),
  `role` (`assistant`), `content`, `is_voice`, `topic_id`. Связь с входящим — только СОСЕДСТВО по
  времени и порядку строк, а не ссылка. Путь закрыт мозгом: ветки `splinter.py` сюда не приходят.
* `auditor.py:223` — `bridge.audit_log(group, topic_id, user_request, tool='response_text',
  tool_args, claimed_result, verdict, severity, detail, suggested_fix, status)`. Вызов стоит под
  условием `if verdict != "ok"` (`auditor.py:221`), то есть **здоровый ответ не записывается
  никогда**; идентификатора сообщения в полях нет.

**Вывод пункта:** поля связи «на какое входящее отвечает это исходящее» в системе нет. Перечень
того, что есть, приведён выше поимённо.

---

## 6. ПУНКТ 3. РЕШЕНИЕ ДО ОТПРАВКИ — **НЕ ФИКСИРУЕТСЯ**; что рождается ПОСЛЕ — названо отдельно

### 6.1. До отправки рождается РОВНО одна запись, и она не о решении

`splinter.py:9943` — строка журнала со значением `mode`. Но `mode` берётся
`splinter.py:9934`: `mode = GROUPS.get(msg.chat_id)` — это **свойство ЧАТА** из таблицы
`splinter.py:223-230`, одно и то же для всех сообщений группы. Оно отвечает на вопрос «куда
пришло», а не «какая ветка взялась» и не «какой класс присвоен».

Сам выбор ветки происходит `splinter.py:9960-9969` (`_handle_money` · `_handle_servicing` ·
`_handle_delivery` · `_handle_attendance` · `_handle_intake`), и **ни одна из пяти не оставляет
записи о том, что взялась**. Класс, присвоенный сообщению внутри ветки (вид работы, вид снимка,
намерение), в запись тоже не выходит.

### 6.2. Записи, рождающиеся ПОСЛЕ действия — отдельный ответ, как и требовалось

* `splinter.py:9062` — исход записи события: `add_event: ok=… saved=…`;
* `splinter.py:2659` — исход записи работы в историю, с `msg_id` входящего;
* `splinter.py:9915` — «ПОЛ ОТВЕТА: заход промолчал (исход …)», рождается только когда бот
  промолчал и пол заговорил за него (`splinter.py:9890-9924`); за 21.09 таких 5.

Все три говорят об уже случившемся действии и ни одна — о принятом решении.

### 6.3. Признаки, которые живут в памяти процесса и на диск не выходят

* `splinter.py:95` `_SPOKE` — сказали ли мы хоть слово и сколько раз (`ContextVar`);
* `splinter.py:102` `_PHOTO_KIND` — вид снимка (`ContextVar`);
* `splinter.py:830` `_AWAITING_REPLY` — обычный словарь: «бот ждёт ответа в этой теме».

Перезапуск сервиса стирает все три.

---

## 7. ПУНКТ 4, ОН ЖЕ ЦЕЛЬ. ЧТО МОЖНО ПОСЧИТАТЬ ЗА СУТКИ

### 7.1. Сколько входящих получили больше одного ответа — **НЕЛЬЗЯ**

Величина существует внутри обработки и выбрасывается: снимок до разбора `splinter.py:9957`
(`_spoke_before = _SPOKE.get()`), сравнение после `splinter.py:9898`
(`if _SPOKE.get() != spoke_before`). Машина знает, СКОЛЬКО раз сказала, — она лишь проверяет
«не ноль ли» и нигде не пишет ни число, ни факт.

**Недостающие поля поимённо и место, где их пришлось бы завести:**

| поле | смысл | место |
|---|---|---|
| `incoming_mid` | на какое входящее ушёл этот ответ | `splinter.py:148-159`, `_send`, после удачной отправки |
| `out_mid` | идентификатор отправленного (есть в `sent`, выбрасывается) | там же, `splinter.py:159` |
| `chat_id`, `topic_id`, `sent_at` | адрес и время ответа | там же |
| `branch` | какая ветка отправила | там же (передать из `splinter.py:9960-9969`) |
| `mid`, `topic_id` у входящего | чтобы ответ было к чему привязать | `splinter.py:9943` |
| `spoke_delta` | сколько ответов ушло на ЭТО входящее | `splinter.py:9976-9977`, конец `handle` |

Без первого и пятого вопрос не считается ничем: связать ответ с входящим нечем даже вручную.

### 7.2. Сколько вопросов бота остались без ответа человека — **НЕЛЬЗЯ**

Признак «бот задал вопрос и ждёт» **существует**: `splinter.py:833` `mark_awaiting` (зовётся
`bot.py:638`), `splinter.py:837` `is_awaiting`, `splinter.py:848` `clear_awaiting`, хранилище —
словарь `splinter.py:830` `_AWAITING_REPLY`, ключ `(chat_id, topic_id)`, значение — метка
времени, срок жизни `_AWAIT_TTL`.

Считать по нему нельзя по двум причинам сразу: хранилище — память процесса (перезапуск стирает),
и ни постановка, ни снятие, ни истечение не пишутся никуда.

**Недостающие поля:** `question_mid`, `chat_id`, `topic_id`, `asked_at`, `answered_at`,
`answered_by_human`, `expired_at`. **Место:** три функции флага, `splinter.py:833-848` — писать
строку на постановку и на снятие, различая снятие ответом и снятие по сроку.

### 7.3. Сколько из них никуда не эскалировали — **НЕЛЬЗЯ**

Эскалация владельцу идёт своими дверьми, и ни у одной нет поля «по какому вопросу». Сцепить
эскалацию с висящим вопросом нечем. **Недостающее поле:** `escalated_for_mid` (ссылка на
входящее либо на вопрос бота), **место** — дверь эскалации, и только после того, как у вопроса
появится собственный идентификатор из 7.2: пока его нет, ссылаться не на что.

### 7.4. Что посчитать МОЖНО — и число показано

Единственное, что нынешние записи дают за сутки, — **количество входящих**. Запрос: по файлу
`/root/turbobaby-manager-bot/splinter.log` считать строки, содержащие ` Splinter [`, группируя по
дате из метки времени в начале строки (многострочные записи не якорить — текст сообщений
переносится, и наивный срез первых 10 знаков даёт мусор; это поймано живьём в этом заходе).

**Число за 21.09.2026: 45 входящих.** Соседние сутки: 20.09 — 3, 19.09 — 18, 18.09 — 43.
Сколько ответов ушло на эти 45 — посчитать нечем.

---

## 8. ЗАМЕР БЮДЖЕТА — АРИФМЕТИКА СОШЛАСЬ

Единиц четыре, по одной на пункт, потолок единицы 300 с, потолок захода 2700 с.

| единица | что делала | уложилась |
|---|---|---|
| 1 | признак сделанности, премиса, корни захвата, чтение базы | да |
| 2 | дверь отправки, поля, пути оседания исходящего | да |
| 3 | точка решения, `mode`, ветки, признаки в памяти | да |
| 4 | флаг ожидания, замер журнала по суткам, недостающие поля | да |

Остановки по правилу «не сошлось на первых двух» не потребовалось: обе первые единицы дали
числа и адреса. Произведение 4 × 300 = 1200 с против потолка 2700 с — **0.44 потолка**, как и
рассчитывал Штаб.

---

## 9. ГРАНИЦЫ ЗАХОДА

Кода не менялось ни на строку. Ни одного сообщения никуда не отправлено. Процессы не трогались:
`splinter` и демон не перезапускались, их состояние не читалось иначе как через файлы на диске.
Боевые таблицы не открывались вовсе; база читалась режимом только для чтения; журнал открывался
на чтение. Ничего не удалялось. Временных файлов не создано (см. 3).

## 10. ОСТАТКИ (на 21.09.2026, этот файл)

* **Ответ отрицательный, и строить ничего не разрешалось** — перечень недостающих полей с
  местами (7.1–7.3) есть проект, а не сделанная работа; ни одно поле не заведено.
* **Полнота захвата базы не измерена долей** — контраст 260 против 5249 взят из двух разных
  корней (база и журнал) и говорит о порядке, а не о проценте: одна строка журнала не равна
  одной строке базы по определению.
* **Ротации у журнала нет** — 75 МБ и растёт; сегодня это помогает (113 дней истории), но
  правилом это не закреплено нигде, и класс «источник исчез» не закрыт.
* **Полоса ПК здесь не судится** — премиса про `chatlog` относится к чужой машине, и отсюда
  проверить её нечем; сверка форм — работа той полосы.
* **Личные чаты названы только числом** (4 штуки) — идентификаторы намеренно не вынесены.
* Ветка `intake` (клиентский контур) заморожена решением владельца; её молчание в захвате
  отдельно не разбиралось.
