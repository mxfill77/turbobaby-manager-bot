# Доставка замка повторных подсказок: перезапуска НЕ делали — он уже состоялся 22.08 в 20:56:47 UTC

Заход-доставщик. Кода не тронуто ни строкой. **Перезапуска splinter в этом заходе НЕТ**, и это не
отказ от работы, а её результат: прибор доказал, что доставка уже произошла, а вопрос, снятый по
факту, повторно не исполняется (тот же класс, что у карточек `437`/`curator_state` §«второе окно» и
у второго окна `deliver_card`).

---

## 1. ПРЕМИСА ЗАДАНИЯ УСТАРЕЛА НА ОДИН ПЕРЕЗАПУСК

Задание говорит «splinter поднят в 15:05». Юнит говорит другое — **сегодня было ДВА подъёма**:

| подъём (UTC)  | что нёс                                                              |
|---------------|----------------------------------------------------------------------|
| `15:05:19`    | `fd10cfe` — сторож потери работ (`works_ledger`, буфер `_PENDING_WORKS`) |
| **`20:56:47`**| **`ff5cb32` + `234680b` — САМ ЗАМОК ПОДСКАЗОК**                        |

```
ExecMainStartTimestamp=Sat 2026-08-22 20:56:47 UTC
MainPID=1491077   ActiveState=active   SubState=running
```

Второй подъём попадает **внутрь окна задачи 72** (20:14–20:59 UTC, `reports/2026-08-22/task-72.md`,
`причина=run_timeout`, лимит 2700 с): заход, собравший замок, успел его выложить и умер по таймауту
в 20:59, не сказав об этом ни строки. Приписка по окну условна ровно настолько же, насколько
приписка рестартов в счёте серии, — но САМ ФАКТ подъёма от авторства не зависит.

---

## 2. ЧТО ДОЕХАЛО БЫ ВМЕСТЕ С НАШИМ (ответ пункта 1 задания)

От `fd10cfe` (доставка 15:05) до `a37f1ad` — восемь коммитов:

| коммит    | что трогает                                                                     | попадает ли в память splinter |
|-----------|----------------------------------------------------------------------------------|-------------------------------|
| `19a03d1` | `docs/artifacts/…bridge-lower-registers.md` — read-only разбор                     | нет                           |
| `73ce4dd` | `CLAUDE.md`, `bridge_build/{BUILD.json,Bridge.js,ReadFleet.js,ServiceUndo.js}`, тесты | нет — код МОСТА, доставка = `clasp`, которого мы не зовём |
| `084aa56` | артефакт (поправка чисел)                                                          | нет                           |
| `5bd87a4` | артефакт подготовки выкладки                                                       | нет                           |
| `ff5cb32` | **`hint_dedup.py` (новый, 198 строк), `splinter.py`**, `invariants_check.py`, 3 сьюта, `.gitignore`, артефакт | **ДА** |
| `234680b` | **`splinter.py`**, `tests/test_hint_dedup.py` — память замка не течёт из прогона тестов | **ДА**                    |
| `e8830bd` | артефакт снятия чужого кода из HEAD моста                                          | нет                           |
| `a37f1ad` | артефакт (манифест доставки)                                                       | нет                           |

Итого в память процесса идут РОВНО два файла, `splinter.py` (+270/−41 за весь отрезок) и
`hint_dedup.py` (+198) — 457 добавленных строк на троих вместе с `invariants_check.py` (+30),
который читается с диска каждым прогоном гейта и в память процесса не попадает вовсе.

**ДЕНЕГ И КЛИЕНТСКОГО КОНТУРА СРЕДИ ЭТОГО НЕТ, и это проверено разбором, а не на глаз.**
`git diff --name-only -G` по денежному словарю называет `splinter.py`, но чтение самих строк
диффа (`-U0`, только `+`/`−`) показывает **единственное** совпадение — слово `wallet_cache.json`
внутри ДОБАВЛЕННОГО КОММЕНТАРИЯ (ссылка на прошлый класс «фикстура в живом состоянии»); ни одного
вызова денег или живых таблиц в диффе `splinter.py` нет. Словарь живых таблиц (`set_fleet_*`,
`confirmed=`, `delete_event`, брони) даёт ТОЛЬКО `bridge_build/*.js`, `tests/*` и артефакты —
то есть код, который перезапуском splinter не доставляется в принципе. Клиентский контур
(`userbot_listen`/`moderation_bot`) живёт на полосе ПК и здесь не тронут ни одним файлом;
splinter по поправке 05.08.2026 — ВНУТРЕННИЙ сервис.

---

## 3. СВЕРКА ЧИСЛАМИ (пункт 2 задания)

```
HEAD        = a37f1ad71b09fc7c09d5130b5f236c06ea6e798f
origin/main = a37f1ad71b09fc7c09d5130b5f236c06ea6e798f      → совпадают
git diff origin/main (отслеживаемое)     → 0 файлов (dirty_ok=True, dirty=0)
splinter: PID 1491077, старт 2026-08-22 20:56:47 UTC, active/running
```

Время записи файлов на диск — ДО подъёма процесса, и это второй свидетель О3:

```
splinter.py        20:52:59.490  (за 3 м 48 с до старта)
hint_dedup.py      20:21:51.971  (за 34 м 56 с до старта)
invariants_check.py 20:27:23.023
```

Коммиты (время коммитера, как их видит прибор): `ff5cb32` → 20:43:16, `234680b` → 20:53:19 —
оба СТАРШЕ подъёма, то есть срабатывает и первый свидетель.

---

## 4. ПРИБОР О3, ЖИВЫМ КОДОМ (`expectations.delivery_state` на `expectations_run.delivery_facts`)

```
234680b | 2026-08-22T20:53:19Z | state = доставлен   splinter.py → splinter
ff5cb32 | 2026-08-22T20:43:16Z | state = доставлен   hint_dedup.py → splinter
                                                      splinter.py  → splinter
                                                      invariants_check.py → с диска на каждый запуск
fd10cfe | 2026-08-22T14:30:14Z | state = доставлен   works_ledger.py · service_receipt.py · splinter.py
e8830bd, a37f1ad, 5bd87a4, 084aa56, 19a03d1        → вне доставки (только документы)
73ce4dd | state = неизвестно  → bridge_build/*.js: «выкладка наружу либо конфиг владельца»
```

Это не наше суждение: факты собирает `expectations_run`, судит их сам `expectations.delivery_state` —
тот же код, которым система отвечает владельцу на вопрос «дошло ли до прода».

**Байткод сомнения не добавляет:** `__pycache__` у всех модулей — `timestamp`-режим (не «hash-based
непроверяемый»), шапки `.pyc` совпадают с mtime и размером текущих исходников; Python сверяет их на
КАЖДОМ импорте, поэтому исполнялся байткод именно того `splinter.py`, что лежит сейчас = HEAD.

---

## 5. ПОЧЕМУ ПЕРЕЗАПУСК НЕ СДЕЛАН, ХОТЯ ЗАДАНИЕ ЕГО ПРОСИТ

Потому что он не доставил бы НИ ОДНОГО байта кода: после подъёма 20:56:47 в репозиторий легли
только `e8830bd` и `a37f1ad`, и оба — документы. Выгода нулевая, а цена НЕ нулевая и названа
поимённо — у splinter есть состояние, живущее только в памяти процесса:

* `_PENDING_WORKS` (`splinter.py:551`) — буфер отложенных работ, TTL 3 ч, на диск не пишется:
  ровно то, чью молчаливую потерю закрыл `fd10cfe`;
* `_SVC_UNDO` / `_SVC_UNDO_LAST` (`splinter.py:6111-6112`) — журнал отмены последней записи ТО,
  окно 3 ч, на диск не пишется: перезапуск отнимает возможность отменить свежую запись;
* `_SP_LAST_SENT` — анти-дубль напоминаний (здесь потеря безвредна: поверх стоит персистентный
  `last_reminded_at` в самой заявке).

Память самого замка (`hint_dedup_state.json`) рестарт переживает — она на диске, и это её
заявленное свойство.

---

## 6. ЗАМОК В ПАМЯТИ ЖИВОГО ПРОЦЕССА (пункт 5 задания) — ЧЕСТНО, С НАЗВАННЫМ ПРЕДЕЛОМ

Доехал: два свидетеля О3 сошлись, дерево чистое, байткод сверяется по времени исходника.
**Прямого рантайм-следа пока нет, и это сказано прямо:** новый код называет путь своей памяти
строкой `🔁 замок повторных подсказок: память в …` при ПЕРВОЙ же подсказке, а с 20:56:47 ни одной
подсказки не пробовали отправить — в журнале за это время только тики планировщика.

Ближайший кандидат промолчал по законной причине, и она проверена кодом: часовой тик
`service_pending_reminder` в новом процессе отработал в **21:01:48–21:01:51** и до `_hint_send` не
дошёл — выше стоит персистентный троттл 6 ч (`_SP_REMIND_THROTTLE_MIN`), а последнее напоминание по
`ADV 350 372` ушло в **20:10:26** ещё из старого процесса. Право на следующее — около 02:10 UTC;
тогда строка о памяти замка и появится в `splinter.log` первой же подсказкой.

---

## 7. СТОРОЖ ПОТЕРИ РАБОТ (пункт 6 задания)

Не задет: `works_ledger.py` не менялся с `fd10cfe` (`git diff fd10cfe a37f1ad` его не называет),
прибор О3 числит `fd10cfe` доставленным, а сам сторож живёт в том же поднятом процессе, что и замок.
Правка `ff5cb32`/`234680b` в `splinter.py` меняет РОВНО двери подсказок (`_send` → `_hint_send`) и
ни одной строки выгрузки буфера: `_km_door` и вызовы `works_ledger.*` в диффе отсутствуют.
Машинное подтверждение — полный гейт этого дерева (`tests/test_works_buffer.py`, 18 проверок,
плюс инвариант `WORKS_LEDGER_PURE`).

---

## 8. ГРАНИЦЫ ЗАХОДА

Кода не тронуто ни строкой. Перезапусков нет — ни splinter, ни демона. Мост не выкладывался,
`clasp` не звался. В рабочие таблицы и в зеркало «обслуживание» не писали. Данные 5960 не трогали.
Не удалено ничего. Реплей истории не запускался. Всё вмешательство захода — этот файл и одна
поправка в §8 артефакта замка.

---

## 9. ОСТАТКИ (на 22.08.2026, `docs/artifacts/2026-08-22-deliver-hints-readback.md`)

1. **Прямой рантайм-свидетель замка ещё не наблюдался** — до первой подсказки (ориентир 02:10 UTC,
   вид F) вывод стоит на двух свидетелях О3, а не на строке журнала. Точка касания —
   `splinter._hint_state_path`.
2. **Кто именно перезапустил splinter в 20:56:47 — приписка по окну**, а не наблюдённое событие:
   ручной перезапуск владельца в то же окно неотличим от авторского (тот же предел, что у
   `_series_attribute`).
3. **`.pyc` трёх модулей переписан в 21:27:33, то есть ПОЗЖЕ подъёма процесса** — объяснения этому
   заход не нашёл. На вывод не влияет: `timestamp`-режим сверяется на каждом импорте, поэтому
   исполняться мог только байткод текущего исходника. Названо, а не спрятано.
4. Разведочные скрипты захода лежат в `_recon_hintdeliver_0822/` в корне репо, а не в
   `/tmp/tb_scratch` (R17): песочница движка запрещает запись вне рабочих каталогов. Не удалялись —
   удаление вне временных каталогов красное.
5. **Задание останется формально невыполненным по букве пункта 4** (перезапуска нет). Если владелец
   хочет перезапуск ради наблюдаемости — команда одна, `systemctl restart splinter`, и цена её
   названа в §5.
