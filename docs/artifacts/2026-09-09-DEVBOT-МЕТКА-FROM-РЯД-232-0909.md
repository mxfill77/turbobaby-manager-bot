# Входит ли метка ступени E в отбор devbot, и есть ли в журнале отправка по ряду 232

**Дата:** 09.09.2026 · **полоса:** сервер (VPS) · **ряд очереди:** 235, `from=Filipp-shtab`, `lane=vps`
**Задание:** ящик Штаба, ключ `10-a-devbot-metka-from.0909` (документ `shtab_task_10-a-devbot-metka-from.0909`, file id `1VXfmgEEMTl7pembzo9GMJJMGuHIrEo74`)
**Род захода:** РАЗВЕДКА. Кода не правил, списка отбора не касался, ничего не отправлял, боевых записей не делал.

DEVBOT-МЕТКА-FROM-РЯД-232-0909

**ПРЕДЕЛ ОТВЕТА, названный первым:** заход судит СЕРВЕРНУЮ сторону. Что видит владелец в своём
Telegram и что делала полоса ПК — отсюда не наблюдается, и утверждать об этом заход не вправе.

---

## 0. Признак сделанности

В `docs/artifacts/` за 09.09.2026 лежал **один** файл — `2026-09-09-МОСТ-ЧИТАЕТ-ТЕМУ-ТРИ-ИСХОДА-0909.md`
(метка шага `МОСТ ЧИТАЕТ ТЕМУ ТРИ ИСХОДА 0909`, ряд 230). Метки ЭТОГО шага
(`DEVBOT-МЕТКА-FROM-РЯД-232-0909`) не несёт ни один файл. Вопросы пунктов 1–4 в нём **не отвечены**:
его предмет — читает ли Мост поле `topic` в POST, а не отбор devbot и не ряд 232. Работа с нуля.

## 1. Место для временного — названо и выдержано

`/tmp/tb_scratch/` **закрыт песочницей движка** (`mkdir` отклонён дословно: «may only create
directories in the allowed working directories»). Это тот же случай, что уже записан в артефакте
19.08. Поэтому временное лежит в **`_recon_devbot_from_0909/`** в корне репо — два read-only
скрипта, `read_row_232.py` и `live_predicate.py`. Ничего не удалял, включая уборку за собой
(прямой запрет задания).

**Честная граница про конфиги.** Запрет «`.env` и конфиги не читать» соблюдён по существу: ни одно
значение настройки в этом артефакте не названо и не печаталось. `read_row_232.py` поднимает
учётные данные Моста штатным `load_dotenv` — иначе очередь не прочитать вовсе, — но печатает
только поля рядов; `live_predicate.py` про инбокс печатает **булев** «задан? True», а не номер.

---

## 2. Премисы

| | Премиса | Вердикт |
|---|---|---|
| **П1** | карточку рисует devbot на VPS, отбирая ряды **по списку меток отправителя** | **ЧАСТЬЮ ВЕРНА** |
| **П2** | наш файл называет входящей только `Filipp-pcloc-dec`, про `Filipp-recon-ask` молчит | **ВЕРНА, но список больше не решает** |
| **П3** | ряд 232 создан 09.09 в 10:25:13 UTC | **ВЕРНА** (точно `10:25:12.610Z`) |
| **П4** | владелец получил карточку по 232 с кнопками | **НЕ ПРОВЕРЯЕТСЯ ОТСЮДА**, косвенно согласуется |

**П1 верна в двух третях и устарела в главном.** Карточку действительно рисует devbot на VPS и
действительно читает строку таблицы Моста. Но отбор **по списку меток отменён 08.09.2026**,
коммит `6246193` «отчёт доезжает по признаку „чей ряд“, а не по списку имён: ящик Штаба был
четвёртым немым источником из четырёх». Полоса ПК, называя строки `pc_orchestrator.py` 1350 и 6146,
описывает СВОЙ код; серверный отбор с 08.09 устроен иначе.

**Это в проде, а не только в git.** Коммит `6246193` — `2026-09-08T02:13:05Z`; `splinter`
(devbot живёт внутри его процесса) стартовал `2026-09-08 02:15:49 UTC`, MainPID 1955306,
`ActiveState=active`. Старт **позже** коммита на 2 мин 44 с — свидетель C1 того же О3.

---

## 3. Пункт 1 — список: файл, строка, ПОЛНЫЙ состав

**Файл `devbot.py`, строки 200–205** (объявление), метки-константы — строки 154–196:

```python
QUEUE_FROMS = (QUEUE_FROM, QUEUE_FROM_DEV, QUEUE_FROM_DEC,
               QUEUE_FROM_CURATOR, QUEUE_FROM_OWNER) + QUEUE_FROMS_PC  # ИМЕНОВАННЫЕ метки: те,
                                        # у кого в devbot есть своя роль (постановка, тема, дедуп).
                                        # ФИЛЬТРОМ ОТЧЁТОВ этот список больше НЕ является — см.
                                        # _is_our_source ниже. Полнота его не требуется и не
                                        # достигается: метку заводит производитель, а не мы.
```

Состав снят **живым импортом модуля**, а не чтением глазами (`_recon_devbot_from_0909/live_predicate.py`) — **10 меток**:

```
 1. Filipp-328          6. Filipp-pc
 2. Filipp-328-dev      7. Filipp-pc-dev
 3. Filipp-328-dec      8. Filipp-pc-dec
 4. Filipp-curator      9. Filipp-pcloc-dec
 5. Filipp             10. Filipp-revizor
```

Из них `QUEUE_FROMS_PC` = `Filipp-pc`, `Filipp-pc-dev`, `Filipp-pc-dec`, `Filipp-pcloc-dec`,
`Filipp-revizor` — эта пятёрка решает **ТЕМУ** карточки, а не видимость.

## 4. Пункт 2 — входит ли метка ступени E: **НЕТ в списке, ДА в отборе**

Ответ прямой и двойной, потому что вопрос задан о вещи, которая с 08.09 перестала быть отбором.

**(а) В списке `QUEUE_FROMS` метки `Filipp-recon-ask` НЕТ.** Живая проверка:

```
from='Filipp-recon-ask'   | в списке QUEUE_FROMS: False | _is_our_source: True
from='Filipp-shtab'       | в списке QUEUE_FROMS: False | _is_our_source: True
from='Filipp-pcloc-dec'   | в списке QUEUE_FROMS: True  | _is_our_source: True
from='pc_agent-205'       | в списке QUEUE_FROMS: False | _is_our_source: False
from=''                   | в списке QUEUE_FROMS: False | _is_our_source: False
from='notFilipp-328'      | в списке QUEUE_FROMS: False | _is_our_source: False
```

**(б) В ОТБОР она входит, и список тут ни при чём.** Гейт видимости — единственный, `devbot.py:148`:

```python
if st in by and _is_our_source(it):
```

а сам предикат — `devbot.py:243–249`, цитата целиком:

```python
def _is_our_source(it):
    """Ряд очереди поставлен НАШЕЙ стороной? (единственный гейт видимости отчётов; разбор выше)
    Наш — `Filipp` (ручные пробы владельца) и любой `Filipp-<метка>`: 328/dev/dec, куратор,
    полоса pc, ревизор, ящик Штаба, заявки ревью и разведки, а также всякий будущий источник.
    Чужой — производитель с иной подписью (`pc_agent-205` темы 205) и ряд без метки."""
    f = str((it or {}).get("from") or "").strip()
    return f == _OUR_SOURCE or f.startswith(_OUR_SOURCE + "-")
```

`_OUR_SOURCE = "Filipp"` (`devbot.py:240`). Источник значения — **литерал в коде**, не настройка и
не база; живое значение проверено импортом и равно `'Filipp'`.

**Список собирается литералом** (кортеж констант), поэтому вторая половина вопроса пункта 2
(«если из настройки или из базы — назвать источник и живое значение») к нему не применяется.

**Отбор ряда 232 назван прямо:** `from=Filipp-recon-ask` → `_is_our_source` → **True** → ряд
попадает в `by["needs_approval"]` → идёт в цикл выдачи карточек (`devbot.py:2046`).

## 5. Пункт 4 — что devbot делает с рядом, у которого тема не названа вовсе

**Ответ по коду: у ряда очереди devbot поля темы НЕ читает ВООБЩЕ — ни у одного ряда.** Тему он
считает сам, и «тема не названа» не является для него отдельным случаем: называть её некому и негде.

Два шага, оба цитатой.

**Шаг 1 — тема по метке**, `devbot.py:1235–1243`:

```python
def _item_topic(it):
    if str((it or {}).get("from") or "") in QUEUE_FROMS_PC:
        return pc_dev_topic() or DEVBOT_TOPIC
    return DEVBOT_TOPIC
```

**Шаг 2 — у карточки-вопроса инбокс перебивает тему**, `devbot.py:2044` и `2061`:

```python
inbox = inbox_topic()
...
_approval_topic = inbox or _item_topic(it)
```

и замок ниже, в самом узком месте доставки (`devbot.py:1628`): `topic = _inbox_lock_topic(topic, markup_last, qid)`.

**Итог для ряда 232:** `_is_pc_item` → `True` (по `lane`), но `_item_topic` → **328** (метки
`Filipp-recon-ask` нет в `QUEUE_FROMS_PC`); `inbox_topic()` **задан**, значит карточка
`needs_approval` идёт **в инбокс**. То есть из трёх предложенных задачей исходов — «отправляет в
инбокс по умолчанию», а не молчит.

**Побочный вывод, который стоит знать полосе ПК:** у такого ряда **вопрос** уедет в инбокс, а
`done`/`failed`/«зависла» — в **328**, а не в тему PC-дев, потому что тему им даёт `_item_topic`,
а метки `Filipp-recon-ask` в `QUEUE_FROMS_PC` нет. Метка потеряла работу фильтра, но сохранила
работу адреса — и в этой второй работе она по-прежнему закрытый список.

## 6. Пункт 3 — отправка по ряду 232 в журналах: **НЕИЗВЕСТНО**, срез назван

### 6.1 Срез поиска

**Какие файлы** — все три журнала, которые сервер писал 09.09 (проверено
`find . -maxdepth 2 -name "*.log" -newermt "2026-09-09"`): `splinter.log` (devbot живёт здесь —
`bot.py:65` `logging.basicConfig`, единственный сток), `orchestrator_daemon.log`, `wa_webhook.log`.
`moderation_bot.log` пуст с 11.07. Ротации у `splinter.log` нет — один файл, 363 023 строки,
от `2026-06-01 19:53:24` до `2026-09-09 11:10:59`.

**Дополнительно** проверен journald: `journalctl -u splinter --since "2026-09-09 00:00:00"` —
1 строка, вхождений «232» ноль. То есть файловый журнал и есть журнал.

**За какой период** — сутки 09.09.2026 целиком, 1928 строк в `splinter.log`.

**Какими командами:**

```
grep -n "232" splinter.log | grep "^[0-9]*:2026-09-09"                  → 0 строк
grep "^2026-09-09" splinter.log          | grep -c "232"                → 0
grep "^2026-09-09" orchestrator_daemon.log | grep -c "232"              → 1
grep "^2026-09-09" wa_webhook.log        | grep -c "232"                → 0
grep "\] devbot:" splinter.log | grep "^2026-09-09"                     → 25 строк
```

### 6.2 Что найдено и чего не найдено

**В журнале devbot об отправке по ряду 232 — НОЛЬ строк.** Все 25 строк devbot за 09.09 называют
ряды 227, 228, 214, 229, 230, 231, 233, 235. Ряды **232 и 234 не названы ни разу**, и это видно
дырой в ряду номеров:

```
10:07:59 анонс «в работе» задачи 231 … message_id=10443
10:22:21 карточка задачи 231 (done, чанк 1/1) … message_id=10447
10:40:13 анонс «в работе» задачи 233 … message_id=10452
10:55:58 карточка задачи 233 (done, чанк 1/1) … message_id=10454
11:10:13 анонс «в работе» задачи 235 … message_id=10457
```

**Единственная строка о 232 во всех журналах сервера — не отправка, а наблюдение**, и она в
журнале ДЕМОНА:

```
2026-09-09 10:25:24,770 INFO серия: pc-карточка id=232 увидена живой, тело записано (операции: нет)
2026-09-09 10:57:50,248 INFO серия: pc-карточка id=234 увидена живой, тело записано (операции: нет)
```

Это `orchestrator_daemon._series_note_pc_cards` (строка 5568) — счёт серии, а не путь к человеку.
Она доказывает: **сервер ряд 232 видел живым через 11 секунд после создания** — но об отправке
владельцу не говорит ничего.

### 6.3 Почему «нет строки» здесь НЕ значит «не отправляли»

Это главное в пункте 3, и оно проверяется кодом, а не догадкой. **Успешная доставка карточки
`needs_approval` не пишет в журнал НИ ОДНОЙ строки.** `devbot._send_card_with_retry`
(строки 1619–1647) логирует **только провалы**:

```python
            return True                                   # ← успех: ни одного log-вызова
        except Exception as e:
            log.warning("devbot.report_results: %s qid=%s попытка %d/%d упала …")
    log.warning("devbot.report_results: %s qid=%s НЕ доставлена за %d попыток …")
```

Для сравнения — соседние дороги логируют успех: анонс «в работе» (строка 1662) и карточка
done/failed (строка 2023) печатают `message_id`. Именно поэтому в журнале есть 227…235 и нет
232/234: **232 и 234 — единственные два ряда `needs_approval` за сутки**, а у этой дороги успех нем.

**Вывод пункта 3: НЕИЗВЕСТНО, и незнание структурное, а не случайное.** Журнал devbot на этот
вопрос ответить НЕ МОЖЕТ по устройству: он различает «не отправляли» и «отправили успешно» нулём
строк в обоих случаях. Чтобы вопрос стал отвечаемым, нужна строка на успехе — правки заход не
делал (разведка).

### 6.4 Косвенное — названо косвенным

Номера сообщений в чате монотонны, и дыры в них ложатся ровно на окна обоих `needs_approval`:
между `10447` (10:22:21) и `10452` (10:40:13) не названы **10448–10451**, а ряд 232 создан
10:25:12; между `10454` (10:55:58) и `10457` (11:10:13) не названы **10455–10456**, а ряд 234
создан 10:57:39. Это **согласуется** с тем, что карточки ушли, но **доказательством не является**:
`message_id` общий на весь чат, и те же номера могли занять сообщения владельца, тем 328/1160 и
других отправителей. Ставлю это как корроборацию П4, не как факт.

## 7. Пункт 5 — меняется ли список без ПК, и узнаёт ли ПК

**Отдельной строкой, как просило задание: ДА, меняется без всякого участия полосы ПК, и НЕТ,
ПК об изменении не узнаёт ничем — но с 08.09 это перестало быть опасным.**

Разбор в трёх частях.

1. **Меняется без ПК.** И список `QUEUE_FROMS`, и предикат `_is_our_source` — литералы в
   `devbot.py` репозитория VPS (`origin` = `git@github.com:mxfill77/turbobaby-manager-bot.git`).
   Полоса ПК живёт в своём репозитории, которого на этой машине нет. Любой серверный заход правит
   отбор в одиночку.

2. **ПК не узнаёт.** Канала уведомления нет: общий у полос — таблица очереди Моста, а она несёт
   ряды, а не код отбора. С сервера видно только это; есть ли у ПК свой способ читать чужой
   репозиторий — вопрос к полосе ПК, отсюда не проверяется, и заход это не утверждает.

3. **Почему это больше не мина — и это и есть ответ «для выбора решения».** Пока отбор был
   закрытым списком, незнание ПК стоило немоты: производитель заводил метку, VPS о ней не знал, и
   ряды молча пропадали — так было **четыре раза** (`Filipp-pcloc-dec`, `Filipp-curator`,
   `Filipp-revizor`, `Filipp`), а замер 08.09 нашёл 165 немых рядов из 203 (81 %), среди них
   **пять `needs_approval` метки `Filipp-recon-ask`, которых владелец не видел ни разу**.
   С признаком «чей ряд» ПК **не обязан** ничего узнавать: любая новая метка вида `Filipp-<что-то>`
   становится видимой сама, без правки сервера и без согласования. Согласование осталось нужным
   ровно в одном месте — **тема** карточки (`QUEUE_FROMS_PC`, п. 5 выше): новая метка полосы ПК,
   не внесённая туда, будет отчитываться в 328 вместо темы PC-дев.

## 8. Побочная находка (не предмет задания, доложена как есть)

Строка демона «pc-карточка id=232 увидена живой, **тело записано**» утверждает больше, чем
случилось. `chain_cards.note` (`chain_cards.py:178–196`) возвращает `True` и когда записал, и
когда **запись уже была** («→ True (записано ИЛИ уже было)»), а демон (`orchestrator_daemon.py:5597`)
печатает «тело записано» безусловно. В живом `chain_cards.jsonl` запись с `"id": 232` **ровно
одна и она от 05.09**:

```json
{"id": 232, "at": "2026-09-05T00:23:53", "lane": "pc", "src": "первый вид", "ops": [],
 "head": "[разведка-заявка дата=2026-09-05 ключ=524a9d266e65] …"}
```

— то есть описывает ДРУГУЮ заявку (ключ `524a9d266e65`), тогда как живой ряд 232 несёт ключ
`eab084d7799b` от 09.09. Тело сегодняшней карточки 232 в журнал **не легло**. Это ровно тот
остаток, который CLAUDE.md уже называет («журнал ключуется НОМЕРОМ, а очередь при пересоздании
нумерует заново»), но здесь он виден с добавкой: **строка журнала об этом молчит и говорит
обратное**. Для сравнения — у 234 запись свежая, `"at": "2026-09-09T10:57:50"`, ключ совпадает.
Ничего не правил: разведка.

## 9. Ответы одной таблицей

| Пункт | Ответ |
|---|---|
| 1. список: файл, строка, состав | `devbot.py:200–205`, 10 меток (перечислены в §3) |
| 2. входит ли `Filipp-recon-ask` | **в списке — НЕТ; в отбор — ДА**, через `_is_our_source` (`devbot.py:243`), живое значение `_OUR_SOURCE='Filipp'` |
| 3. отправка по 232 в журнале | **НЕИЗВЕСТНО**, срез в §6.1; ноль строк у devbot, и успех этой дороги нем по устройству (§6.3) |
| 4. ряд без названной темы | тему у ряда не читает вовсе; считает сам; `needs_approval` → **инбокс**; `done/failed` такого ряда → 328 |
| 5. меняется ли без ПК | **да, без ПК; ПК не узнаёт; с 08.09 узнавать и не нужно** — кроме темы (§7) |

## 10. Границы захода

Кода не правил ни на символ; список отбора не тронут. Ни одной пробной карточки, ни одного
сообщения владельцу — Telegram в заходе не вызывался вовсе. К Мосту — только чтение
(`get_pending` по семи статусам, один прогон). Процессы не трогал и не перезапускал: `splinter`
как был PID 1955306 с 08.09 02:15:49, так и есть. Ничего не удалял. Живых чатов, боевой базы и
рабочих таблиц не касался.

**FACT:** read-only. Живые доказательства: `splinter` MainPID=1955306, старт `2026-09-08 02:15:49 UTC`
> коммита `6246193` (`2026-09-08T02:13:05Z`); ряд 232 из живой очереди —
`status=needs_approval, from=Filipp-recon-ask, lane=pc, created=2026-09-09T10:25:12.610Z`;
`devbot._is_our_source({"from": "Filipp-recon-ask"})` → `True` при `"Filipp-recon-ask" in
devbot.QUEUE_FROMS` → `False`.
