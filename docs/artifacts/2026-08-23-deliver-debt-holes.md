# Доставка шести дыр долгового следа в прод (23.08.2026)

Заход ТОЛЬКО доставляющий: кода не тронуто ни строки, в рабочие таблицы и в зеркало не писали,
байков не касались, мост не выкладывали, демон-оркестратор не трогали, ничего не удаляли.
Доставка = перезапуск `splinter`; коммит работы — `d61d862`.

## 1. Что доедет вместе с нашим — и нет ли среди этого денег и клиента

Процесс до захода: PID **1544189**, старт **2026-08-23 09:59:55 UTC**. Всё, что легло в
репозиторий после этой отметки, процесс не читал.

Спрошен ЖИВОЙ прибор О3 (`expectations.delivery_state` поверх `expectations_run.delivery_facts`),
окно фактов 96 ч, 24 коммита. Раскладка: **не доставлен — 1** (наш), доставлен — 8,
вне доставки — 14, неизвестно — 1 (`73ce4dd`, код моста в `bridge_build/` — вид «выкладка
наружу», перезапуском `splinter` не доставляется вовсе).

| коммит | легло UTC | что трогает | исход прибора |
|---|---|---|---|
| `d61d862` | 11:09:40 | `service_debt.py` (новый, 209 строк) · `splinter.py` (119 строк) · 4 сьюта + node-харнесс · артефакт | **не доставлен** (missing: `service_debt.py`, `splinter.py`) |
| `1a1344f` | 10:14:20 | только `docs/artifacts/2026-08-23-pending-debt-revision.md` | вне доставки (кода нет) |

Больше в окне ничего недоставленного нет: `930da84` (09:44:05) и `c592119` (08:40:57) —
документы, `b733506` (09:18:47) правил `splinter.py`/`service_debt.py`/`invariants_check.py`, но
он СТАРШЕ подъёма 09:59:55 и прибор числит его доставленным.

**Денег и клиентского контура среди доезжающего нет, и это разбор строк диффа, а не совпадение
имени файла.** По добавленным и снятым строкам `splinter.py` в `d61d862` словарь
(`wallet` · `balance` · `add_transaction` · `void_last` · касса · деньги · сумма · транзакция ·
депозит · CRM · Зарплаты · `create/activate/close_booking` · клиент · `faq` · `suggest` ·
`moderation` · `userbot`) даёт **0 попаданий**; по `service_debt.py` — **0 попаданий**, и
импортов у модуля **ноль**. Останавливаться было не на чем.

## 2. Сверка до

* `git ls-remote origin main` → `d61d8625375fbfda3f3d4cbe194163e060ce7eaa` — то есть работа
  лежит на сервере, а не только в локальной ветке;
* `HEAD` = `origin/main` = `d61d862`; `git diff --name-only origin/main` → **0 файлов**;
  tracked-изменений в дереве **0** (untracked каталоги разведки не в счёт);
* процесс: PID **1544189**, старт **2026-08-23 09:59:55 UTC**, `ActiveState=active`.

## 3. Гейт

Первый прогон без ярлыка операции ушёл в селективный режим (125 тестов, 87.8 с) — права на
доставку он не даёт по правилу 17.08. Прогон с ярлыком `--for restart_splinter` сам поднял набор
до полного и сказал это вслух:

```
🚚 ДОСТАВКА В ПРОД: СЕЛЕКТИВНЫЙ НАБОР ПРАВА НА ДОСТАВКУ В ПРОД НЕ ДАЁТ …
✅ храповик: слепых читателей 72 — базовая линия держится.
✅ ГЕЙТ (полный): 232 тестов зелёные (131.6с) — прод-операция «restart_splinter» разрешена.
   🚚 ПРАВО НА ДОСТАВКУ В ПРОД дано ПОЛНЫМ набором (232 тестов)
```

**232 зелёных** — ровно ожидаемое число, останавливаться было не на чем. Дерево между гейтом и
перезапуском не менялось (проверено `git status --untracked-files=no` — пусто).

## 4. Перезапуск и чтение назад

`systemctl restart splinter`, затем спрошено состояние, а не расписка команды:

* PID **1544189 → 1559451**, старт **2026-08-23 11:25:09 UTC**, `ActiveState=active`,
  `SubState=running`, `NRestarts=0`;
* старт чистый — `Bridge: ✅ alive (v1.0.0)`, `Auditor: ✅ подключён к splinter._send`,
  `Auditor LLM: ✅ надзор логики`, привязок тема→байк **41**, закрепов **41**, интервалы ТО
  прогреты, `Bot polling started`, все 7 job'ов планировщика подняты;
* **ошибок после подъёма ноль**: `ERROR|CRITICAL|Traceback` в журнале после 11:25:09 — 0 строк;
  последняя ERROR журнала (`Bridge timeout for action=get_pending`) датирована **11:19:45**, то
  есть ДО перезапуска.

## 5. Отпечаток модуля долга в памяти против файла на диске

Процесс запущен из этого дерева: `cwd=/root/turbobaby-manager-bot`,
`argv=[venv/bin/python, /root/turbobaby-manager-bot/bot.py]`.

| модуль | sha256 исходника | размер | записан UTC | шапка .pyc = диску | тело байткода = свежей компиляции |
|---|---|---|---|---|---|
| `service_debt.py` | `98a9961e35d7…7866` | 31 581 | 10:45:11 | да (timestamp-режим, flags=0) | да, 33 935 байт побайтно |
| `splinter.py` | `512f1cd0887d…3b5` | 587 747 | 10:46:54 | да | да, 614 425 байт побайтно |
| `works_ledger.py` | `b99d0bce5aec…ddf9` | 12 893 | 22.08 14:18:45 | да | да, 15 528 байт побайтно |

Цепочка замкнута: байткод на диске валиден ровно для этих исходников (Python сверяет
`mtime`+`size` источника на КАЖДОМ импорте, иначе перекомпилирует), тело байткода побайтно равно
свежей компиляции текущего файла, а исходники записаны ДО старта процесса — значит в память легли
именно эти строки. Независимо то же говорит прибор О3 после перезапуска: `d61d862` —
**доставлен**, `missing` пуст, недоставленных в окне ноль.

## 6. Соседи по тому же файлу

**Замок повторных подсказок — ЖИВ, и это рантаймовый след нового процесса, а не рассуждение.**
Первый часовой тик заявок в поднятом процессе (11:30:10) напечатал:

```
11:30:11,863 splinter:   🔁 замок повторных подсказок: память в /root/turbobaby-manager-bot/hint_dedup_state.json
11:30:11,864 splinter:   🔁 подсказка F ПОДАВЛЕНА (замок повторов): повтор того же состояния, прошло 8.5ч < 24ч · байк=ADV 350 372 тема=2583
```

Файл памяти на месте (130 байт), запись переживает перезапуск — в ней тот же ADV 350 372.

**Сторож потери работ (`works_ledger`) — рантаймового следа нет и не будет, пока не приедет
партия работ; отсутствие следа поломкой не является.** Его строка «сторож партии: принято N ·
записано M» рождается только на записи партии, а таких за всё наблюдение не было: `grep -c
'сторож партии' splinter.log` → **0** (и до перезапуска, и после). Живость доказана признаком:
модуль импортирован (`splinter.py:33`), отпечаток в памяти равен файлу на диске (таблица выше),
инвариант чистоты `WORKS_LEDGER_PURE` зелёный внутри полного набора 232.

**Про новую ветку долга сказано честно:** `d61d862` правит и `scheduled_service_pending_reminder`,
и этот тик исполнился в 11:30:11 без единой ошибки, решив по строке ADV 350 372 «не долг» — но на
сегодняшних данных тот же исход дал бы и прежний код, поэтому отличительным следом ЭТО не
является. Отличительный след (`→ долг ТО …`) появится на первой строке долга.

## 7. Лист заявок, read-only

`service_pending_list(open=True)` через мост:

* **открытых строк — 1**: `ADV 350 372`, статус `ждёт_факт`, тема 2583, `declared=gear`,
  `done=gear`, нота пуста;
* **строк долга — 0** (долгом является статус `ждёт_подтверждения`, слово взято у самого
  `service_debt.STATUS`, своего словаря состояний не заводили).

Ожидание задания («одна открытая ADV 350 372 и ноль долга») сошлось числом.

## Улики

Разведочные скрипты и сырые ответы — `_recon_deliver6_0823/` (`before.json`,
`fingerprint_after.json`, `pending_list.json`). Каталог лежит в корне репозитория, а не в
`/tmp/tb_scratch`, как велит R17: песочница движка запрещает создание каталогов вне рабочих
(`mkdir /tmp/tb_scratch` отклонён), а уборка из репозитория — красная зона владельца, поэтому
ничего не удалялось.
