# TurboBaby — состояние проекта (для продолжения в новом чате)

**Дата среза:** 7 июня 2026 (последняя сессия — см. «🗓️ СЕССИЯ 7 июня 2026» ниже). Этот файл — точка входа для нового чата: что готово, что дальше.

---
## ⭐ TL;DR — ЧИТАТЬ ПЕРВЫМ (сводка на 1 июня)

**Кто:** Филипп (Фил), владелец TurboBaby — премиум аренда мотобайков, Пхукет, 38 байков. НЕ программист, работает разговором.

**Что построено за сессию 1 июня:**
1. ✅ Splinter (manager-bot) работает 24/7 на VPS Hetzner (IP 5.223.94.179, Сингапур, Ubuntu 26.04/Python 3.14), systemd-сервис `splinter`, active. Комп выключен — крутится.
2. ✅ Код в приватном GitHub: github.com/mxfill77/turbobaby-manager-bot. Секреты (.env, memory.db) НЕ в репо.
3. ✅ Claude Code установлен на сервере (Opus 4.8, Max), работает в VS Code (Remote-SSH). Агент-разработчик с подтверждениями. Видит CLAUDE.md.
4. ✅ Этап 4 (бронь) готов и протестирован: Booking.gs (createBooking/activateBooking, резолв байка по номеру), статус "Бронь" не начисляет долг. Bridge.gs +2 case. bridge_client.py +методы.
5. ✅ Правки splinter.py (грязь≠повреждение, депозит, _send через аудитор) + bot.py (set_auditor) — СДЕЛАНЫ, но возможно ещё не на сервере (см. хвост 1).

**Рабочий формат:** планирование/стратегия → чат на сайте (тут) + project_state.md. Код/сервер → Claude Code в VS Code. Telegram Dev Bot — отложено.

**🔴 ХВОСТЫ (сделать):**
1. Подтянуть на сервер свежий код (splinter/bot/bridge_client + Booking.gs/Bridge.gs): с ПК `git add/commit/push` → на сервере `git pull` + `systemctl restart splinter`. На сервере сейчас может крутиться код из первого коммита!
2. ⚠️ БЕЗОПАСНОСТЬ: отозвать засветившиеся в чате GitHub-токен (ghp_...) и EdenAI API-ключ. Выпустить новые. (снято 5.06 — см. секцию 5 июня)
3. Booking.gs/Bridge.gs — выкатить в Apps Script проект Bridge (Deploy NEW VERSION).

**▶ ТЕКУЩАЯ ЗАДАЧА (с чего начать новый чат):** общий "мозг" базы знаний на Google Drive для обоих Claude. Коннектор у Филиппа ЗАРАБОТАЛ. План: (а) проверить чтение Drive, (б) создать на Drive папку с базой знаний как Google Docs, (в) дать Claude Code задачу — endpoint read_doc в Bridge, чтобы он тоже читал Drive. Детали — в разделе "общий мозг" ниже.

**⏭ ПОТОМ:** этап 5 (задача тайцу из брони в Delivery -1002445921469, RU/TH) → этап 6 (фото выдачи → activate_booking). Отдельный трек: клиентский WhatsApp (Cloud API, виртуальный номер, готовая платформа, задаче-ориентированный бот + handoff — по политике Meta с 15.01.2026).

**Ключевые ID/доступы:** VPS 5.223.94.179 (root, пароль у Фила) · GitHub mxfill77/turbobaby-manager-bot (приватный) · Bridge Web App (URL в KB) · таблица "менеджеру Байки" 1sL-rw0klRcJKtWKpgIzLVge6U1GswtBUacJ_jo0JgL0 · форма-проект 17OTrEgB... · код на сервере /root/turbobaby-manager-bot.
---

## ✅ СЕССИЯ 5 июня 2026 — UX кассы + каналы связи Claude↔Claude

### UX-блок Money Cashflow (DONE, в проде)
- Двуязычные сообщения: монолитные блоки (один флаг 🇹🇭/🇷🇺 на блок, НЕ чередовать
  построчно), баланс столбиком (метка на строке, валюты под ней с отступом).
- passport = латинский код единицы (как ฿/EUR) в обоих блоках; паспорт в балансе
  показывается ТОЛЬКО при значении !=0 (нулевой = шум, убран). THB всегда.
- Коммиты e3b9478 + 4a5988c, оба в main и в проде, рантайм-тест подтверждён.
- Открыто отдельно: msg_undo/EUR печатают ฿ для любой валюты; balance_match/
  mismatch, expense_high, photo_reminder, service_due ещё чередуют языки.

### Каналы связи между Claude Code и Claude-на-сайте (DONE)
- cc_log (KB_claude_code_log, id 1o0be0v9xGI7L0tmpzpefgXBgq4jgmg2eM9lxL_Rlj2E):
  Claude Code пишет PLAN (перед действием) / DONE (после) / NOTE (диагностика).
  Филипп говорит «проверь» → Claude-на-сайте читает через коннектор и комментирует.
- KB_review (KB_claude_review, id 1NYh2XSeojiaAOWhJxeG69d7Sg2sykcP83Xyiy58G7y8):
  прямой буфер ревью. Claude Code пишет «PLAN <UTC> ждёт ревью: <суть>»;
  Claude-на-сайте читает через коннектор, отвечает Филиппу одной строкой
  «передай в KB_review: ОК/правки»; Филипп переносит в терминал. Новые записи СВЕРХУ.
- Правило логирования cc_log (PLAN/DONE/NOTE) + правило KB_review с пунктом
  «ОЖИДАНИЕ ПОСЛЕ PLAN» (не действовать пока Филипп явно не сказал «да») —
  в memory.db (id 17) + память-файлы review-policy.md / cc-log-logging-policy.md.
- ТРЕБУЮТ явного «да» Филиппа даже после ОК Claude-на-сайте: git push,
  systemctl restart, deploy Apps Script, проводки Bot Data, SQL по memory.db,
  запись в рабочие таблицы (CRM/Зарплаты/Байки).

### ⚠️ ПРИЗНАК ПРАВИЛЬНОГО Bridge-проекта (важно, потеряли время 5.06)
- В Apps Script НЕСКОЛЬКО проектов. Рабочий Bridge = проект «TurboBaby Bridge».
- Опознать: в его ReadDocs.gs ЕСТЬ функция setupCcLog (и setupReview).
  Файлы: Config/Bridge/BotData/ReadFleet/ReadClients/ReadFinance/ReadDocs/Booking.gs.
  Надёжнее всего — сверить deployment ID из BRIDGE_URL с Deploy→Manage deployments.
- setup-функции (setupBrain/setupCcLog/setupReview) запускать ТОЛЬКО в этом проекте,
  иначе ключ уйдёт в манифест чужого проекта (Script Properties у проектов раздельные).

### 🔴 НЕЗАКРЫТЫЕ ХВОСТЫ (с 1 июня)
- ✅ Безопасность: проверено с Филиппом 5.06 — GitHub-токен фактически НЕ светился;
  EdenAI-ключ некритичен (ничего ценного). Отзыв НЕ требуется. Пункт снят.
- ✅ requirements.txt: ptb 21.7 → 22.7 (совместимо с Python 3.14) — СДЕЛАНО 5.06, коммит 60ac562.
- ✅ clasp: заведён для Bridge Apps Script — деплой/синк .gs без копипасты. СДЕЛАНО 6.06.
  Все 8 шагов пройдены, прод-цикл доказан (push→redeploy @18→@19, URL не сменился).
  Штатный цикл — в CLAUDE.md (раздел «Деплой Bridge через clasp»). Детали ниже.

---

## 🗓️ СЕССИЯ 7 июня 2026

### 1) 🔧 ЗАДАЧА 4255 (замена масла NMAX BLACK GOLD) — IN PROGRESS, НЕ ЗАКРЫТА
Splinter отрапортовал «зафиксировал замену 24094 в таблице», НО в Лист1 «Байки»
колонка I осталась **17000** — запись фактически НЕ прошла. Подтверждено прямым
чтением таблицы (не доверяем рапорту бота).

**Числа (расшифровка путаницы):**
- `24094` — реальный пробег с фото (vision дал 24082/24091/24094, confidence high).
- `17000` — прошлая замена, то что сейчас лежит в колонке I.
- `21000` — СТАРЫЙ порог `17000 + 4000` (расчёт от старой замены).
- `28094` — `24094 + 4000` — следующая замена, посчитано ВЕРНО.
- Интервал 4000 км для NMAX (скутер) — верный.

**ОТКРЫТО (доделать):**
- Разобраться, почему `service_upsert` не записал в Лист1 «Байки» колонку I:
  баг или by-design (пишет только в Bot Data «обслуживание», а в Байки col I —
  обратной записи нет?). Предварительный разбор кода 7.06 указывает на второе:
  `service_upsert` → BotData «обслуживание», Лист1 col I он только ЧИТАЕТ
  (`find_bike`/`getFleetStatus`). Требует подтверждения и решения, как синкать.
- Подтвердить пробег глазами (фото) и записать `24094` в колонку I —
  ТОЛЬКО с approve Филиппа (правка рабочей таблицы Байки = guarded-действие).

### 2) ⚙️ WORKFLOW-ФИКС — NOTE при СТАРТЕ диагностики (важно)
Усилено правило логирования Claude Code: при НАЧАЛЕ диагностики писать короткий
NOTE «начал копать X» СРАЗУ, не только итог в конце. Жёсткое правило:
**не останавливаться/не завершать копание без NOTE.**
- **Причина:** между «делаю» и «записал DONE» Claude-на-сайте слеп — он видит
  только cc_log, real-time за терминалом не наблюдает. Без стартового маячка
  Филипп вынужден гадать, движется ли работа.
- Закреплено в памяти Claude Code (`cc-log-logging-policy`): стартовая NOTE
  обязательна, «тишина без NOTE» считается нарушением.

---

## 🚀 ЗАДАЧА clasp — ✅ ВЫПОЛНЕНО 6 июня 2026 (автоправка+деплой Bridge .gs без копипасты)

**РЕЗУЛЬТАТ:** все 8 шагов пройдены. clasp 3.3.0 на VPS, залогинен info@turbophuket.com
(/root/.clasprc.json). Прод-Bridge склонирован в `/root/turbobaby-bridge-gs` (отдельно от
Python-репо), Script ID 12iXPDU…HhOJ. Цикл доказан end-to-end: правка → `clasp push` (HEAD) →
`clasp redeploy AKfycbxNC9…NOqw` (прод-деплой @18→@19, URL `BRIDGE_URL` НЕ сменился, ping ok
v1.0.0, list_brain цел). Штатный цикл записан в CLAUDE.md (раздел «Деплой Bridge через clasp»).
**Важные поправки по ходу (clasp v3 ≠ план):** обновление прода = `clasp redeploy <deploymentId>`
(НЕ `deploy --deploymentId` — это создало бы новый деплой и сменило URL); login --no-localhost
просит ПОЛНЫЙ redirect-URL (localhost:8888/?code=…), а не короткий код; для push нужен
включённый Apps Script API (https://script.google.com/home/usersettings, одноразово).

Ниже — исходный план (как ставилась задача), оставлен для истории.

**ЦЕЛЬ:** Claude Code сам правит `.gs` локально и деплоит Bridge через clasp;
Филипп не копипастит код в браузер Apps Script и не жмёт Deploy руками.

**ЕДИНСТВЕННЫЙ ручной шаг Филиппа** (нельзя автоматизировать): один раз
`clasp login --no-localhost` → перейти по ссылке, войти под **info@turbophuket.com**,
скопировать код в терминал (~2 мин). Дальше — только «да» на развилках, можно с телефона.

**ПОШАГОВО** (Claude Code делает, Филипп подтверждает):
1. `npm i -g @google/clasp` на VPS; `clasp --version`.
2. `clasp login --no-localhost` → Филипп проходит OAuth (info@turbophuket.com).
3. Узнать Script ID проекта «TurboBaby Bridge»: `clasp list` ИЛИ Филипп копирует
   из URL проекта (`/projects/<SCRIPT_ID>/edit`). Проверка верности после clone:
   файлы `Config/Bridge/BotData/ReadFleet/ReadClients/ReadFinance/ReadDocs/Booking.gs`
   + `ReadDocs.gs` содержит `setupCcLog` И `setupReview`.
4. `clasp clone <SCRIPT_ID>` в ОТДЕЛЬНУЮ папку (НЕ `/root/turbobaby-manager-bot`,
   напр. `/root/turbobaby-bridge-gs`). Это первый pull — тянет актуальный прод-код.
5. Сверить с репо-зеркалом `ReadDocs.gs`; при расхождении прод важнее.
   Зафиксировать `.clasp.json`.
6. ТЕСТ push на безопасной правке (комментарий в `ReadDocs.gs`) → `clasp push` →
   проверить в браузере. БЕЗ deploy пока.
7. Deploy: deploymentId действующего Bridge (из BRIDGE_URL между `/s/` и `/exec`
   ИЛИ `clasp deployments`) → `clasp deploy --deploymentId <ID>` (обновляет
   СУЩЕСТВУЮЩИЙ, URL не меняется, Splinter не падает).
   Проверить ping v1.0.0 + `list_brain`.
8. Записать в `CLAUDE.md` штатный clasp-цикл (pull→правка→push→deploy --deploymentId)
   как замену копипасте.

**КРИТИЧНО** (каждое ломает прод):
- `clasp push` ПЕРЕЗАПИСЫВАЕТ весь проект → первый раз ТОЛЬКО `clone`.
- deploy строго `--deploymentId` существующего (не плодить новые, иначе URL сменится).
- clasp-папку держать отдельно от Python-репо.
- чужие `.gs` не трогать.

---

## Что это
AI-команда ботов для TurboBaby (премиум аренда мотобайков, Пхукет, владелец Филипп, 38 байков). Владелец — не программист, работает на Windows (VS Code + Python 3.12 + Node.js). Рабочие папки на ПК: `D:\turbobaby-bot\` (userbot-скрипты) и `D:\turbobaby-bot\manager-bot\` (бот Splinter).

## Архитектура (готово и работает)
- **Apps Script Bridge** (Web App) — мост к Google Sheets. Деплоится в Apps Script. GET: ping/fleet/clients/anomalies/daily_pulse. POST: транзакции, баланс, обслуживание (ТО), важное, аудит. URL и токен — в .env бота.
- **Splinter** (manager-bot, Python) — внутренний бот-супервайзер в Telegram. Работает в HQ + 5 операционных группах (касса, обслуживание-форум-с-темами, доставка, отметки). Двуязычный RU+TH (в группах есть тайцы). Модель claude-sonnet-4-5.
- **memory.db** (SQLite) — память: правила, заметки, история диалогов (изолирована по chat_id+topic_id).
- **Auditor** (auditor.py) — надзорный модуль: сверяет действия бота с фактами (БД/KB), ловит галлюцинации, нарушения правил, ошибки логики, смешение языков. Журнал — лист `аудит`. Отчёт в HQ 09:30 + команда /audit. Сам не правит — только флаги + предложения, применяются после апрува Филиппа.

## Ключевые правила (бизнес-логика)
- Депозит = деньги ИЛИ паспорт (не оба).
- HONDA CLICK 125 НЕ сдаём (предлагать PCX 150 / ADV 150 / NMAX 155).
- Доставка только по Пхукету.
- Интервал ТО масла: скутеры (NMAX/XMAX/ADV/FORZA/PCX) — 4000 км; XADV + мотоциклы (XSR/CB/CBR/MT/NINJA/VULCAN/R7/REBEL) — 5000 км.
- ТО Oil берётся из колонки I «ТО Oil» Листа1 Байки (НЕ J «ТО Gear»).
- Важные цифры с ФОТО (пробег, суммы) — ВСЕГДА подтверждать у человека перед записью (vision ошибается на LCD-одометрах: 19705→19105→29105).
- Splinter сам деньги не двигает, изменения в Sheets — через approve-флоу.
- Никаких рассылок клиентам.
- Тайская команда НЕ общается с клиентами вообще.

## Два контура — НЕ путать
- **Splinter (внутренний)** → RU+TH (в рабочих группах есть тайцы).
- **Клиент-бот (будущий, наружу)** → RU+EN, БЕЗ тайского (с клиентами работают только Даня/Даша, русскоязычные).

## Последние сделанные правки (эта серия сессий)
1. Корректный интервал ТО по типу байка.
2. Полный надзор за важным: заметил→спросил→ОДНО «да»→закрепил→напоминание раз в 3 дня→закрытие.
3. Изоляция истории по темам (chat_id+topic_id).
4. Подтверждение важных цифр с фото перед записью (промпт + код-страховка confirmed).
5. Ответ без тега: только когда бот сам задал вопрос в теме (10 мин) ИЛИ фото без подписи в теме обслуживания. В HQ awaiting выключен.
6. Индикатор «Splinter печатает…» (фоновый, гаснет сам, не зависает; ask вынесен в executor).
7. Читаемые логи: «тема 73 (NINJA 400 6334)» вместо голого номера.
8. get_service берёт пробег из листа обслуживания если фото нет в буфере (после рестарта).
9. Auditor: факт-проверки + проверка стиля/языка; убрана ложная тревога на отложенный закреп.
10. fetch_client_chats.py — выгрузка клиентских переписок TG за год (read-only).
11. turbobaby_faq_v1.md — FAQ из 710 диалогов (топ-темы: цена, выбор, оплата, доставка, сроки, депозит).

## Что дальше (выбрать в новом чате)
**Клиентское направление (выбран этот путь):**
- Проверить формулировки FAQ под тон владельца.
- WhatsApp: ручной экспорт чатов (.txt) → разбор (авто нельзя — бан номера).
- EN-версии ответов FAQ.
- Клиент-бот: лестница — черновики менеджеру → полуавтономный → узкие авто-сценарии (бронь/цена/часы). Языки RU+EN.

**Внутренний контур (отложено):**
- Кнопки/команды по группам (тайцам: Пришёл/Выехал/Доставил; владельцу меню в HQ).
- LLM-проверка перевода в аудиторе (естественность, совпадение смысла блоков).
- Этап 4: запись броней в CRM с approve-флоу.
- Перенос на Render (24/7) после обкатки. Userbot остаётся на ПК.

## Файлы в этом снапшоте
- `turbobaby-manager-bot/` — bot.py, splinter.py, claude_client.py, bridge_client.py, memory.py, prompts.py, auditor.py, requirements.txt, README.md
- `turbobaby-bridge/` — Config.gs, Bridge.gs, BotData.gs, ReadFleet.gs, ReadClients.gs, ReadFinance.gs, README.md
- `fetch_topics.py`, `fetch_client_chats.py` — userbot-скрипты (запуск с ПК)
- `knowledge_base.md` — полная база знаний (v1.6+)
- `turbobaby_faq_v1.md` — клиентский FAQ

## Как продолжить
В новом чате: загрузить эти файлы в проект (knowledge_base.md и этот файл — обязательно в Project Knowledge, как ОТДЕЛЬНЫЕ .md файлы, НЕ внутри .zip). Сказать новому Claude: «продолжаем TurboBaby, см. project_state.md» — он сориентируется.

## ✅ ЭТАП 4 (постановка брони) — СДЕЛАНО (1 июня)
Механика предв.брони разобрана по реальной таблице менеджеру_Байки.xlsx:
- Статус живёт в колонке A листа "клиенты" (текст: "В аренде"/"Завершена"; теперь +"Бронь").
- Колонка W — вспом.формула IF(NOW()>F...) "для скрипта и проверки", НЕ главный статус.
- Формулы долга I/J: =IF(OR(A="в аренде","в аренде 1","в аренде 2"), ...) → при статусе
  "Бронь" долг НЕ начисляется. Это и используем: бронь не тикает до выдачи.
- Форма Rental Agreement пишет в "клиенты" со статусом "В аренде", карта formToSheetColumns:
  C байк, D имя, E старт, F конец+время, K оплачено, L цена/мес, M цена/день, N пробег,
  S залог(число ИЛИ "passport"), T шлемы, U контакты, V note.

Созданы файлы (в outputs, надо выкатить):
- Booking.gs (НОВЫЙ в Bridge-проекте): createBooking() ставит A="Бронь"; activateBooking()
  меняет Бронь→В аренде после фото выдачи. Защита от дублей. Карта колонок сверена 14/14.
- Bridge.gs: +case create_booking, +case activate_booking (не забыть Deploy NEW VERSION!).
- bridge_client.py: +create_booking(**fields), +activate_booking(bike,name).

⚠️ БЕЗОПАСНОСТЬ: API-ключ EdenAI засветился в чате (был в config формы) — Филиппу ОТОЗВАТЬ
   и выпустить новый, положить в Script Properties EDENAI_API_KEY (Config.gs формы уже правлен).
   (снято 5.06 — см. секцию 5 июня: EdenAI некритичен, отзыв не требуется)

ДАЛЬШЕ: этап 5 — из брони сформировать задачу тайцу в группу Delivery (-1002445921469),
двуязычно RU/TH через Splinter. Затем этап 6 — фото выдачи → activate_booking.

## ▶️ ТЕКУЩАЯ ТОЧКА (конец сессии 1 июня) — продолжить в новом чате
СДЕЛАНО в этой сессии:
- Этап 4 (бронь) готов: Booking.gs (+testBooking/testActivate), Bridge.gs (+2 case),
  bridge_client.py (+create_booking/activate_booking). Карта колонок сверена 14/14 по
  реальному meneджеру_Байки.xlsx. Бронь пишется в первую пустую строку A (сейчас 1253),
  дырок в таблице нет, формулы не затрагиваются. Статус "Бронь" не начисляет долг.
- Также ранее в сессии: splinter.py (грязь≠повреждение, депозит, обёртка _send через
  аудитор), bot.py (set_auditor). Всё откомпилировано/протестировано.

НАДО СДЕЛАТЬ ФИЛИППУ (руками):
1. Выкатить Booking.gs + Bridge.gs в Apps Script проект Bridge, Deploy → NEW VERSION.
2. Запустить testBooking() кнопкой Run, проверить строку 1253 = "Бронь", долг пустой, удалить тест.
3. Выкатить splinter.py + bot.py + bridge_client.py в manager-bot, перезапустить.
4. ⚠️ Отозвать засветившийся EdenAI API-ключ, новый → Script Properties EDENAI_API_KEY. (снято 5.06 — см. секцию 5 июня)

## 🚀 СЛЕДУЮЩАЯ БОЛЬШАЯ ЗАДАЧА: перенос Splinter в онлайн (24/7, комп выключен)
Начать в НОВОМ чате (этот уперся в длину). Стартовые вводные:
- Переносим ТОЛЬКО manager-bot (Splinter) — это токен-бот, безопасно на сервере.
  Userbot (Telethon, личный аккаунт) НЕ переносим — остаётся на ПК (риск бана по IP).
- Хостинг: у Филиппа есть Render. Вариант — Render worker, ИЛИ VPS (терминал с телефона).
- ⚠️ ГЛАВНАЯ ЗАСАДА — memory.db (SQLite на диске). На Render файловая система эфемерная →
  при передеплое стирается, бот забудет правила. Решить ДО переноса: либо Render persistent
  disk, либо вынести память в Google Sheets через Bridge (Bot Data уже есть).
- Нужно для переноса: .env (токены — НЕ в git!), requirements.txt (есть), точка входа bot.py.
- Сделать .gitignore: .env, venv, *.session, memory.db, __pycache__.
- Bridge доступен с любого IP (Web App "Anyone") — с сервера достучится, проблем нет.

## ⏭️ ПОТОМ: этап 5 (задача тайцу из брони в Delivery -1002445921469, RU/TH) + этап 6
   (фото выдачи → activate_booking). И отдельный трек — клиентский WhatsApp (Cloud API,
   виртуальный номер, готовая платформа, задаче-ориентированный бот + handoff).

## 🚀 ПЕРЕНОС В ОНЛАЙН — ПРОГРЕСС (1 июня)
РЕШЕНО: хостинг = VPS (Hetzner, обычный, Европа — для Splinter IP не важен, это токен-бот).
  Userbot НЕ переносим (остаётся на ПК / в будущем Raspberry Pi дома, пхукетский IP).
  Память memory.db: на VPS диск постоянный → проблема Render отпадает, ничего переписывать не надо.
ЦЕЛЬ: на VPS крутится Splinter 24/7 + стоит Claude Code (агент-разработчик, правит код
  по задачам Филиппа через подтверждения) + git для истории/бэкапов.

СДЕЛАНО:
- ✅ Git установлен на ПК (Windows), настроен user.name/email.
- ✅ .gitignore создан (исключает .env, memory.db, venv, *.session, __pycache__) — проверен,
  секреты НЕ попадают в репо.
- ✅ git init + commit (9 файлов: код + requirements + .gitignore).
- ✅ Приватный GitHub репозиторий: https://github.com/mxfill77/turbobaby-manager-bot
  Код залит (git push), секретов в репо НЕТ. Проверено.

СЛЕДУЮЩИЕ ШАГИ (продолжить отсюда):
1. Создать VPS Hetzner (CX22, Ubuntu 24.04). Зайти по SSH.
2. На VPS: установить Python 3.12, git, склонировать репозиторий.
3. Перенести .env на сервер ВРУЧНУЮ (его нет в git!) — токены TG/Bridge/Anthropic.
   Также перенести memory.db с ПК (история/правила) — тоже не в git, копировать отдельно (scp).
4. Создать venv, pip install -r requirements.txt.
5. Запустить bot.py как systemd-сервис (24/7, автоперезапуск). Проверить логи.
6. Установить Claude Code на VPS, настроить режим подтверждений.
⚠️ .env содержит: TG токен бота, BRIDGE_URL/BRIDGE_TOKEN, ANTHROPIC_API_KEY. На сервер — только
   вручную/scp, НЕ через git.

## ✅✅ ПЕРЕНОС SPLINTER В ОНЛАЙН — ЗАВЕРШЁН (1 июня)
Splinter работает 24/7 на VPS Hetzner, независимо от ПК.
- VPS: Hetzner CPX12, Сингапур, IP 5.223.94.179, Ubuntu 26.04 (Python 3.14).
- Код: GitHub приватный https://github.com/mxfill77/turbobaby-manager-bot
- На сервере: /root/turbobaby-manager-bot, venv, .env + memory.db перенесены через scp (НЕ в git).
- ⚠️ python-telegram-bot пришлось обновить (21.7 несовместим с Python 3.14) — обновлён до свежей.
  NB: requirements.txt в репо обновлён до ==22.7 (сделано 5.06, коммит 60ac562) — совпадает с сервером.
- systemd-сервис splinter.service: active(running), Restart=always, enable (автозапуск).
- Логи: /root/turbobaby-manager-bot/splinter.log
- ⚠️ Бот на ПК ДОЛЖЕН быть выключен (один токен = один бот). Рабочий теперь серверный.

Команды управления: systemctl {status|restart|stop} splinter ; tail -f splinter.log

ОБНОВЛЕНИЕ КОДА на сервере (когда правим): на ПК commit+push → на сервере
  cd /root/turbobaby-manager-bot && git pull && systemctl restart splinter
  (но .env/memory.db не трогаются — они вне git).

СЛЕДУЮЩЕЕ: установить Claude Code на VPS (агент-разработчик с подтверждениями).
Затем вернуться к этапу 5 (задача тайцу из брони) и этапу 6.
Не забыть: выкатить в репо актуальные splinter.py/bot.py/bridge_client.py (правки этой сессии:
грязь≠повреждение, _send через аудитор, create_booking) — закоммитить и git pull на сервере.
И отозвать засветившийся EdenAI ключ + GitHub токен (светился в чате). (снято 5.06 — см. секцию 5 июня)

## 🧭 РАБОЧИЙ ФОРМАТ (выбрано с Филиппом — он не программист, работает разговором)
Связка двух сред, по типу задачи:
1. ПЛАНИРОВАНИЕ / разбор / решения → чат Claude на сайте (этот интерфейс) + project_state.md
   + knowledge_base.md как контекст. "Штаб". Здесь НЕ пишется код на сервере, проектируется.
2. ВОПЛОЩЕНИЕ в коде на сервере → Claude Code, но через VS Code (Remote-SSH к 5.223.94.179),
   НЕ голый терминал. Разговорный формат, наглядные диффы, кнопки принять/отклонить.
3. Telegram Dev Bot (управление ботом из TG-группы) — ОТЛОЖЕНО на потом (отдельный проект,
   строить через Claude Code когда основное готово; сейчас отвлекает от цикла аренды).

СЛЕДУЮЩИЙ ШАГ КОМФОРТА: настроить VS Code + расширение Remote-SSH + Claude Code расширение,
подключиться к серверу. Превращает работу с VPS в визуальную среду (окна, диффы, чат сбоку).
Расписать установку по шагам в новом чате.

## 🚚 ПЕРЕХОД РАЗРАБОТКИ К CLAUDE CODE (решено 1 июня)
Основная разработка переезжает на сервер в Claude Code (VS Code, Remote-SSH к 5.223.94.179).
Туда переносится база знаний как контекст:
- В репо создать папку docs/ и положить: knowledge_base.md, project_state.md,
  turbobaby_faq_v1.md, park_list.md. Claude Code читает их + CLAUDE.md как контекст.
- Перенос: перетащить файлы в VS Code проводник (папка docs) ИЛИ через git (commit на ПК → git pull на сервере).

РАЗДЕЛЕНИЕ РОЛЕЙ:
- Claude Code (сервер) = ВСЁ про код/сервер: писать, править, тестировать, деплоить, этапы 5-6,
  новые модули, рефакторинг. Деятель — делает быстро, но решения/контроль за Филиппом.
- Чат на сайте (этот) = думать ДО кода: архитектура, стратегия, риски, новые направления,
  второй взгляд на необратимое. "Что строить и зачем" — здесь; "как в коде" — Claude Code.

⚠️ Claude Code по натуре исполнитель: даёшь задачу — делает. Большие развилки сначала
проектировать (обсудить), потом давать ему оформленную задачу.

## 🧠 ИДЕЯ: общий "мозг" базы знаний на Google Drive (для Claude-сайт + Claude Code)
Цель Филиппа: единое место для базы знаний, доступное обоим Claude.
РЕАЛЬНОСТЬ (граница):
- Код → остаётся в Git/на сервере (Drive для кода не годится). Я (сайт) в приватный репо не вижу.
- База знаний/документы → МОГУТ жить на Google Drive как общая папка:
  - Claude-сайт читает/пишет через Drive-коннектор (нестабилен, по запросу, не real-time).
  - Claude Code (сервер) читает через Bridge — НО нужен НОВЫЙ endpoint "прочитать Google Doc по ID"
    (текущий Bridge умеет только Sheets-таблицы и запрограммированные действия, не произвольные доки).
- Формат: база знаний как Google Docs (их читают оба). project_state.md на Drive = живой мост.

ЧТОБЫ ЗАРАБОТАЛО — 2 доработки:
1. Проверить/переподключить Drive-коннектор к info@turbophuket.com (у нас падал).
2. Задача для Claude Code: добавить в Bridge.gs endpoint read_doc (читать Google Doc/файл по ID
   через DocumentApp/DriveApp), вернуть текст. Тогда Claude Code видит базу знаний с Drive.

ВЫВОД: "всё в одном месте для двоих" достижимо для ЗНАНИЙ (Drive), НЕ для кода. Полный live-доступ
Claude-сайт к серверу невозможен (изоляция + приватность) — и это правильно для безопасности.

## ✅ ОБЩИЙ «МОЗГ» (Brain) — СДЕЛАНО (1 июня)
База знаний живёт на Google Drive как Google Docs, читают оба Claude:
- Claude-сайт — через Drive-коннектор (папка «TurboBaby Brain»).
- Claude Code (сервер) — через Bridge: `read_doc` (по `id` или `name`) и `list_brain`.

Реализация:
- Новый файл Bridge **ReadDocs.gs** (зона: read_doc/list_brain + одноразовая setupBrain).
  В Bridge.gs добавлено ТОЛЬКО два case в роутер doGet. Деплой — NEW VERSION.
- Манифест {логическое_имя → doc_id} хранится в Script Properties (ключ `BRAIN_MANIFEST`),
  без хардкода ID в коде. Дубликат-оглавление — Google Doc «KB_index» в папке Brain.
- Проверено через Bridge: list_brain отдаёт манифест; read_doc читает все 4 дока по name и по id;
  ошибки мягкие (unknown_name / missing_param).

ID базы знаний (Brain):
- Папка Brain : `1uWqHsxk7aEWoSNqaUBMmqkYOh2UKYLkY`
- knowledge_base (KB_knowledge_base): `1TwNzB_bGw1aHD1ThEuT9V6EgG7p70Tud6WeB0sKDt1Y`
- project_state (KB_project_state) : `1cRedmoltyntz5oNqjB3S3MloLftefEN9HlY7QPOHMQ8`
- faq           (KB_faq)           : `1tv8Y-K3gLyT9Y0mXyYXZLf2c2rEzPMLHPzvg4lGqs98`
- park_list     (KB_park_list)     : `1jD3VJGeoET8Yf5ma6iZPmwwmuw_yIYyP2A4N5RAEZho`
- index         (KB_index)         : `1klCUC5domgR6t-3hu-Yc7UC75hgf-y6-rCILFnN7JJs`

### 🔁 ПРАВИЛО СИНКА (источник правды = git, Drive = генерируемое зеркало)
- Истина живёт в репозитории: `docs/knowledge_base.md`, `docs/project_state.md`,
  `docs/turbobaby_faq_v1.md`, `docs/park_list.md`. Правим — здесь, через Claude Code (дифф+коммит+push).
- Google Docs в папке Brain (KB_*) — это ЗЕРКАЛО. Руками их не редактируем (перезатрутся при синке).
- ОБЫЧНЫЙ СИНК после правки .md = одна команда Claude Code «синкни мозг» (или конкретный док):
  Claude Code читает docs/*.md → для каждого вызывает write_doc(name=<ключ>, text=<содержимое>) через
  Bridge POST. Тело KB_-дока перезаписывается (clear+setText), doc_id и манифест НЕ меняются.
  Ре-деплой Bridge НЕ нужен (код не трогается). Никаких ручных действий в Drive.
  Ключи: knowledge_base, project_state, faq, park_list.
- write_doc (POST в Bridge, файл ReadDocs.gs): защита от затирки — пустой text запрещён
  (empty_text); неизвестное имя → unknown_name; перезапись только существующего дока по id из манифеста.
- setupBrain() — ТОЛЬКО аварийное пересоздание с нуля (если папку Brain/доки удалили или манифест
  потерян). В обычном цикле НЕ используется. Процедура аварийного: залить .md в папку Brain
  (имена точь-в-точь, без авто-конвертации) → Run setupBrain → пересоздаст доки + манифест.
- Манифест (BRAIN_MANIFEST в Script Properties) не редактируем вручную. Свериться: ?action=list_brain.
- Проверка после синка: read_doc name=<ключ> → длина по code points = длине git-исходника.

### ID базы знаний (Brain) — дубль для быстрого доступа
- Папка Brain : `1uWqHsxk7aEWoSNqaUBMmqkYOh2UKYLkY`
- knowledge_base : `1TwNzB_bGw1aHD1ThEuT9V6EgG7p70Tud6WeB0sKDt1Y`
- project_state  : `1cRedmoltyntz5oNqjB3S3MloLftefEN9HlY7QPOHMQ8`
- faq            : `1tv8Y-K3gLyT9Y0mXyYXZLf2c2rEzPMLHPzvg4lGqs98`
- park_list      : `1jD3VJGeoET8Yf5ma6iZPmwwmuw_yIYyP2A4N5RAEZho`
- index          : `1klCUC5domgR6t-3hu-Yc7UC75hgf-y6-rCILFnN7JJs`
