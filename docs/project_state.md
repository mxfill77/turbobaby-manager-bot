# TurboBaby — состояние проекта (для продолжения в новом чате)

**Дата среза:** 11 июня 2026 (внутренний контур Splinter готов и в проде; система ТО/обслуживания построена ПОЛНОСТЬЮ — закрытие масла кол.I + регламенты J/K/L + расчёт сроков/напоминания + карточка байка + единая сводка ТО — и функционально подтверждена на живых заменах; следующее — КЛИЕНТСКАЯ интеграция). Этот файл — точка входа для нового чата: что готово, что дальше. **Новый чат — читай блок «🚀 СТАРТ НОВОГО ЧАТА» сразу ниже.**

---
## 🚀 СТАРТ НОВОГО ЧАТА (читать первым, 11 июня)

Продолжаем TurboBaby. Внутренний контур (Splinter: учёт / ТО / обслуживание / аудитор / автономия) готов, в проде и функционально подтверждён на живых байках.

**ЧТО РАБОТАЕТ (система ТО/обслуживания — построена и обкатана):**
- **Закрытие масла:** «заменили масло» + фото пробега → сторож B (пробег не убывает) → вопрос кнопкой → «да» доверенного (Пым/владелец) → `set_fleet_oil` в Лист1 кол.I, снят закреп.
- **Регламенты J/K/L** (gear/ABS/возд.фильтр): запись по типу работы (`_classify_work`) + расчёт срока (`next_km`, зеркало масла) + напоминания. Интервалы бот читает из `knowledge_base` (блок `SERVICE_INTERVALS`) через `read_doc`, фоллбэк — хардкод.
- **Карточка байка по запросу** («инфа/статус/что по байку» → `_is_status_request` → `_build_bike_card`): собирается из ЧИТАЕМЫХ источников (find_bike + service_list + история «события» через `read_events`), ничего не пишет.
- **Единая сводка ТО-цикла:** одно итоговое сообщение (работы буллетами, тайский список `_WORK_TH`, закреп итога) вместо разнесённых реплик.
- **Двуязычие:** 🇹🇭 чистый тайский / 🇷🇺 чистый русский, единый разделитель блоков централизованно в `_send`; служебные тоже двуязычные.
- **«События»** (инфо-работы без интервала: масл.фильтр/колодки/цепь/вилка) — отдельный регистр-сноска; `delete_event` чистит по точному ключу.

СЛЕДУЮЩЕЕ = клиентская интеграция: сначала Telegram userbot (от @turbophuket, старт на ПК Пхукет, полуавтомат-черновики, потом Raspberry Pi 24/7), затем WhatsApp Cloud API. Язык RU+EN без тайского, база = turbobaby_faq_v1.md. Первый шаг — движок ответов по FAQ.
Открытый хвост (единственный): revoke EdenAI + GitHub PAT (см. RISK).

---

## 🏗️ ФУНДАМЕНТ ПЕРЕД АВТОНОМИЕЙ (решено 17.06)

Лестница к оркестратору/дев-боту — строим фундамент ДО автономии, не параллельно:

1. **УВЕДОМЛЕНИЯ** — ✅ СДЕЛАНО (17.06). Пуш Филиппу в личку Telegram (id `504608015`) по завершении
   блока Claude Code. Скрипт `notify.py` (рут репо): токен из `.env` `BOT_TOKEN` (в логи не светится),
   формы `--done "<задача>"` → «✅ … готова» / `--need "<задача>"` → «🔧 … — нужно решение» /
   произвольный текст. Зовётся в КОНЦЕ блока вместе с DONE в cc_log. Предусловие выполнено: Филипп
   написал боту `@turbobaby_manager_bot` Start, личка достижима (тестовый sendMessage прошёл).
   Ограничение (честно): момент «застрял на встроенном guard-вопросе» программно НЕ перехватывается —
   пуш по ЗАВЕРШЕНИЮ блока (покрывает «отошёл, не знаю — готово ли»).
2. **ЗДОРОВЬЕ СИСТЕМЫ** — ✅ СДЕЛАНО (17.06). Скрипт `health.py` (рут репо) — read-only сводка одной
   командой `venv/bin/python3 health.py`: Splinter (`systemctl` active + uptime + рестарты) / Bridge
   (`ping` alive+версия, живой запрос) / Аудитор (маркеры старт-лога `Auditor ✅` + `Auditor LLM ✅`) /
   Polling (`Bot polling started`) / лог (свежесть посл. строки + ошибки ТЕКУЩЕГО запуска) / прод-коммит.
   ✅/❌ по компонентам + итог «ВСЁ ОК» / «ПРОБЛЕМА: X». Ничего не пишет, не рестартит, не деплоит.
   `❌` только на реально лежащем компоненте; ошибки в логе → `⚠️` (бот жив); лог event-driven, тишина =
   `ℹ️`, не провал. Exit 0=ок, 1=проблема. Нюанс: лог не ротируется → ошибки считаются только текущего
   запуска (строки-продолжения трейсбэка датируются по родительской строке).
   Добивка (17.06): `health.py --push` — пуш Филиппу в личку ТОЛЬКО при `❌` (лежит компонент);
   `--push-always` — пуш всегда (тест/расписание); `⚠️` (ошибки в логе при живом боте) пушем НЕ
   дёргает. Ретрай `ping` ×3 — чтобы транзиентный таймаут Bridge не будил ложной тревогой.
   Авто-проверка: systemd timer `splinter-health.timer` (+`.service`) — зовёт `health.py --push`
   каждые 4ч (`OnUnitActiveSec=4h`, `Persistent`), юниты версионируются в `deploy/`
   (+`deploy/README-health-timer.md`: установка/откат/смена интервала). Установлен в
   `/etc/systemd/system/`, `enabled --now`.
3. **ОБКАТКА на живых тайцах** (неделя-две) — фаза 2 / карточка / allowlist проверены на моках,
   не на неделях.
4. **ТЕХ.ГЕЙТ красной зоны** (боевая запись через Bridge сейчас держится дисциплиной — усилить
   технически) + **ТЕСТЫ-ГЕЙТ** (ни одна правка в прод без авто-прогона тестов). Делать ВПЛОТНУЮ
   перед автономией.
5. **ДЕВ-БОТ** (полу-оркестратор) → и только потом **ПОЛНЫЙ ОРКЕСТРАТОР**, СТРОГО с правилом:
   красную зону сам не проходит никогда.

**ПРИНЦИП:** автономию строим на ОБКАТАННОМ фундаменте, не параллельно. Приборы + тормоза +
чёрный ящик до автопилота.

---

## 🗓️ СЕССИЯ 10-11 июня 2026 — система ТО/обслуживания достроена + UX сводки + двуязычие

Все правки splinter-only (исключение — `read_events` на стороне Bridge), по зелёному/оранжевому протоколу, с моками и отчётами в cc_log. Функционально подтверждено на живых байках.
Коммиты сессии (хронологически): `a9c8ecf` → `e1dc31b` → `f91a390` → `2b47634` → `8ebb051` → `2146b45` → `b236097` → `346470b`.

### ⚙️ Фаза 2 учёта J/K/L (расчёт + напоминания) — в проде
- **ЧАСТЬ A — интервалы из книги знаний** (`a9c8ecf`): бот читает блок ```json SERVICE_INTERVALS``` из `knowledge_base` через `read_doc` (`_load_service_intervals`, кэш ~10 мин). ЛЮБАЯ ошибка чтения/парса → фоллбэк на хардкод `_SVC_INTERVALS_FALLBACK` (расчёт ТО не падает при флапе Docs). Тип байка: scooter по ключевым словам, иначе moto; XADV → moto. Дубли `_oil_interval`/`oil_interval_for` НЕ удалены — это и есть фоллбэк.
- **ЧАСТЬ B — расчёт срока + напоминания** (`e1dc31b`): для gear/ABS/возд.фильтра — зеркало масляного флоу. `service_upsert(interval=…)` считает `next_km = km + интервал`, статус ok/due/overdue; просрочка → закреп-напоминание (`_pin_overdue_reminder`). gear трекается ТОЛЬКО у скутеров (`gear.moto=null`), ABS/возд.фильтр — у всех.

### 🧾 UX сервис-цикла — одно итоговое сообщение вместо разнесённых реплик
- **Единое итоговое сообщение ТО-цикла** (`2b47634`): одна сводка вместо разрозненных квитанций по ходу.
- **Квитанции работ по факту записи + чистка промежуточных** (`f91a390`): квитанция формируется по факту записи; промежуточные сообщения цикла подчищаются.
- **Сводка ТО — работы по пунктам + тайский список + закреп итога** (`8ebb051`, заход 1): работы выводятся буллетами « — » в ОБОИХ блоках (🇹🇭 названия из `_WORK_TH`, 🇷🇺 русские), итог закрепляется (`msg_service_summary`).

### 🪪 Карточка байка по запросу + сервис-на-пробеге
- **Карточка по запросу** (`2146b45`, заход 2): короткий свободный запрос «инфа/статус/что по байку/сводка по/карточка/status/info» → `_is_status_request` → `_build_bike_card` собирает карточку из ЧИТАЕМЫХ источников (find_bike + service_list), НИЧЕГО не пишет.
- **Сервис-на-пробеге в карточке** (`b236097`, заход 3): в карточку добавлена секция последних инфо-работ из истории «события» — `bridge.read_events(bike, limit=6)` (Bridge-экшен `read_events`/`readEvents`, деплой @31), фильтр по паттерну «<работа> — <км> км», newest-first.

### 🌐 Двуязычные служебные + единый разделитель блоков
- **`346470b`:** служебные сообщения (toast `q.answer`, «кнопка устарела») переведены на двуязычный формат; единый разделитель между 🇹🇭/🇷🇺 блоками вынесен ЦЕНТРАЛЬНО в `_send` (идемпотентно) → разделитель появляется ВЕЗДЕ (сводка, карточка, квитанции, вопросы, служебные). Кнопочные потоки переведены на `_send` → тоже получают разделитель + аудит. 🇹🇭-блок без кириллицы.

### 🔗 Связанное (сделано 9.06, относится к контуру обслуживания)
- `delete_event` — удаление строк «события» по точному ключу (Bridge `@29→@30`, `a0dfaf6`) — детали в секции 9 июня.
- Автопрореживание `KB_review` по размеру — `prune_review` / `prune_review_size` / `setup_review_prune_trigger` (`2f9fba3`) — зеркало автопрореживания cc_log.

---

## ⭐ TL;DR — ЧИТАТЬ ПЕРВЫМ (сводка на 1 июня)

**Кто:** Филипп (Фил), владелец TurboBaby — премиум аренда мотобайков, Пхукет, 38 байков. НЕ программист, работает разговором.

**Что построено за сессию 1 июня:**
1. ✅ Splinter (manager-bot) работает 24/7 на VPS Hetzner (IP 5.223.94.179, Сингапур, Ubuntu 26.04/Python 3.14), systemd-сервис `splinter`, active. Комп выключен — крутится.
2. ✅ Код в приватном GitHub: github.com/mxfill77/turbobaby-manager-bot. Секреты (.env, memory.db) НЕ в репо.
3. ✅ Claude Code установлен на сервере (Opus 4.8, Max), работает в VS Code (Remote-SSH). Агент-разработчик с подтверждениями. Видит CLAUDE.md.
4. ✅ Этап 4 (бронь) готов и протестирован: Booking.gs (createBooking/activateBooking, резолв байка по номеру), статус "Бронь" не начисляет долг. Bridge.gs +2 case. bridge_client.py +методы.
5. ✅ Правки splinter.py (грязь≠повреждение, депозит, _send через аудитор) + bot.py (set_auditor) — СДЕЛАНЫ, но возможно ещё не на сервере (см. хвост 1).

**Рабочий формат:** планирование/стратегия → чат на сайте (тут) + project_state.md. Код/сервер → Claude Code в VS Code. Telegram Dev Bot — отложено.

**🔴 ХВОСТЫ (сделать):**
1. Подтянуть на сервер свежий код (splinter/bot/bridge_client + Booking.gs/Bridge.gs): с ПК `git add/commit/push` → на сервере `git pull` + `systemctl restart splinter`. На сервере сейчас может крутиться код из первого коммита!
2. ⚠️ БЕЗОПАСНОСТЬ [РИСК ОТКРЫТ]: отозвать засветившиеся в чате GitHub-токен (ghp_...) и EdenAI API-ключ (OCR паспортов клиентов). Выпустить новые. Отложено осознанно 7.06 — см. секцию 7 июня → RISK.
3. Booking.gs/Bridge.gs — выкатить в Apps Script проект Bridge (Deploy NEW VERSION).

**▶ ТЕКУЩАЯ ЗАДАЧА (с чего начать новый чат):** общий "мозг" базы знаний на Google Drive для обоих Claude. Коннектор у Филиппа ЗАРАБОТАЛ. План: (а) проверить чтение Drive, (б) создать на Drive папку с базой знаний как Google Docs, (в) дать Claude Code задачу — endpoint read_doc в Bridge, чтобы он тоже читал Drive. Детали — в разделе "общий мозг" ниже.

**⏭ ПОТОМ:** этап 5 (задача тайцу из брони в Delivery -1002445921469, RU/TH) → этап 6 (фото выдачи → activate_booking). Отдельный трек: клиентский WhatsApp (Cloud API, виртуальный номер, готовая платформа, задаче-ориентированный бот + handoff — по политике Meta с 15.01.2026).

**Ключевые ID/доступы:** VPS 5.223.94.179 (root, пароль у Фила) · GitHub mxfill77/turbobaby-manager-bot (приватный) · Bridge Web App (URL в KB) · таблица "менеджеру Байки" 1sL-rw0klRcJKtWKpgIzLVge6U1GswtBUacJ_jo0JgL0 · форма-проект 17OTrEgB... · код на сервере /root/turbobaby-manager-bot.
---

## ✅ СЕССИЯ 5 июня 2026 — UX кассы + каналы связи Claude↔Claude

### UX-блок Money Cashflow (DONE, в проде)
- Двуязычные сообщения: монолитные блоки (один флаг 🇹🇭/🇷🇺 на блок, НЕ чередовать
  построчно), баланс столбиком (метка на строке, валюты под ней с отступом).
- passport = латинский код единицы (как ฿/EUR) в обоих блоках; паспорт в балансе
  показывается ТОЛЬКО при значении !=0 (нулевой = шум, убран). THB всегда.
- Коммиты e3b9478 + 4a5988c, оба в main и в проде, рантайм-тест подтверждён.
- Открыто отдельно: msg_undo/EUR печатают ฿ для любой валюты; balance_match/
  mismatch, expense_high, photo_reminder, service_due ещё чередуют языки.

### Каналы связи между Claude Code и Claude-на-сайте (DONE)
- cc_log (KB_claude_code_log, id 1o0be0v9xGI7L0tmpzpefgXBgq4jgmg2eM9lxL_Rlj2E):
  Claude Code пишет PLAN (перед действием) / DONE (после) / NOTE (диагностика).
  Филипп говорит «проверь» → Claude-на-сайте читает через коннектор и комментирует.
- KB_review (KB_claude_review, id 1NYh2XSeojiaAOWhJxeG69d7Sg2sykcP83Xyiy58G7y8):
  прямой буфер ревью. Claude Code пишет «PLAN <UTC> ждёт ревью: <суть>»;
  Claude-на-сайте читает через коннектор, отвечает Филиппу одной строкой
  «передай в KB_review: ОК/правки»; Филипп переносит в терминал. Новые записи СВЕРХУ.
- Правило логирования cc_log (PLAN/DONE/NOTE) + правило KB_review с пунктом
  «ОЖИДАНИЕ ПОСЛЕ PLAN» (не действовать пока Филипп явно не сказал «да») —
  в memory.db (id 17) + память-файлы review-policy.md / cc-log-logging-policy.md.
- ТРЕБУЮТ явного «да» Филиппа даже после ОК Claude-на-сайте: git push,
  systemctl restart, deploy Apps Script, проводки Bot Data, SQL по memory.db,
  запись в рабочие таблицы (CRM/Зарплаты/Байки).

### ⚠️ ПРИЗНАК ПРАВИЛЬНОГО Bridge-проекта (важно, потеряли время 5.06)
- В Apps Script НЕСКОЛЬКО проектов. Рабочий Bridge = проект «TurboBaby Bridge».
- Опознать: в его ReadDocs.gs ЕСТЬ функция setupCcLog (и setupReview).
  Файлы: Config/Bridge/BotData/ReadFleet/ReadClients/ReadFinance/ReadDocs/Booking.gs.
  Надёжнее всего — сверить deployment ID из BRIDGE_URL с Deploy→Manage deployments.
- setup-функции (setupBrain/setupCcLog/setupReview) запускать ТОЛЬКО в этом проекте,
  иначе ключ уйдёт в манифест чужого проекта (Script Properties у проектов раздельные).

### 🔴 НЕЗАКРЫТЫЕ ХВОСТЫ (с 1 июня)
- ⚠️ Безопасность: статус ПЕРЕСМОТРЕН 7.06 (см. секцию 7 июня → RISK). 5.06 пункт сняли
  как некритичный, но 7.06 переоценили: EdenAI-ключ = OCR ПАСПОРТОВ клиентов (перс.данные),
  GitHub PAT (ghp_) тоже светился. РИСК ОТКРЫТ, отзыв отложен ОСОЗНАННО (решение Филиппа).
  Действие: revoke EdenAI → новый в Script Properties EDENAI_API_KEY формы; revoke GitHub PAT.
- ✅ requirements.txt: ptb 21.7 → 22.7 (совместимо с Python 3.14) — СДЕЛАНО 5.06, коммит 60ac562.
- ✅ clasp: заведён для Bridge Apps Script — деплой/синк .gs без копипасты. СДЕЛАНО 6.06.
  Все 8 шагов пройдены, прод-цикл доказан (push→redeploy @18→@19, URL не сменился).
  Штатный цикл — в CLAUDE.md (раздел «Деплой Bridge через clasp»). Детали ниже.

---

## 🗓️ СЕССИЯ 7 июня 2026

### 1) 🔧 ЗАДАЧА 4255 (замена масла NMAX BLACK GOLD) — IN PROGRESS, НЕ ЗАКРЫТА
Splinter отрапортовал «зафиксировал замену 24094 в таблице», НО в Лист1 «Байки»
колонка I осталась **17000** — запись фактически НЕ прошла. Подтверждено прямым
чтением таблицы (не доверяем рапорту бота).

**Числа (расшифровка путаницы):**
- `24094` — реальный пробег с фото (vision дал 24082/24091/24094, confidence high).
- `17000` — прошлая замена, то что сейчас лежит в колонке I.
- `21000` — СТАРЫЙ порог `17000 + 4000` (расчёт от старой замены).
- `28094` — `24094 + 4000` — следующая замена, посчитано ВЕРНО.
- Интервал 4000 км для NMAX (скутер) — верный.

**ОТКРЫТО (доделать):**
- Разобраться, почему `service_upsert` не записал в Лист1 «Байки» колонку I:
  баг или by-design (пишет только в Bot Data «обслуживание», а в Байки col I —
  обратной записи нет?). Предварительный разбор кода 7.06 указывает на второе:
  `service_upsert` → BotData «обслуживание», Лист1 col I он только ЧИТАЕТ
  (`find_bike`/`getFleetStatus`). Требует подтверждения и решения, как синкать.
- Подтвердить пробег глазами (фото) и записать `24094` в колонку I —
  ТОЛЬКО с approve Филиппа (правка рабочей таблицы Байки = guarded-действие).

### 2) ⚙️ WORKFLOW-ФИКС — NOTE при СТАРТЕ диагностики (важно)
Усилено правило логирования Claude Code: при НАЧАЛЕ диагностики писать короткий
NOTE «начал копать X» СРАЗУ, не только итог в конце. Жёсткое правило:
**не останавливаться/не завершать копание без NOTE.**
- **Причина:** между «делаю» и «записал DONE» Claude-на-сайте слеп — он видит
  только cc_log, real-time за терминалом не наблюдает. Без стартового маячка
  Филипп вынужден гадать, движется ли работа.
- Закреплено в памяти Claude Code (`cc-log-logging-policy`): стартовая NOTE
  обязательна, «тишина без NOTE» считается нарушением.

### 3) 🔐 RISK безопасности — ОТКРЫТ, отложен осознанно (держать в хвостах до отзыва)
Пересмотр статуса с 5.06 (тогда сняли как некритичный). 7.06 переоценили вверх:
- **EdenAI API-ключ** (OCR ПАСПОРТОВ клиентов) и **GitHub PAT (ghp_)** светились в чате — НЕ отозваны.
- Это доступ к **перс.данным клиентов** (паспорта), не про деньги — поэтому риск значимый.
- Решение Филиппа: отзыв **отложен ОСОЗНАННО**, не блокер прямо сейчас.
- **Действие когда дойдут руки:** revoke EdenAI key (кабинет EdenAI) → новый в Script Properties
  `EDENAI_API_KEY` формы-проекта; revoke GitHub PAT `ghp_`.
- Зеркало в `cc_log` строкой `RISK <UTC>`; Claude Code тащит этот RISK в хвостах каждого DONE до отзыва.

---

## 🗓️ СЕССИЯ 7 июня — ВЕЧЕР: итоги внутреннего контура + ПЛАН клиентской интеграции

### ✅ СДЕЛАНО В ЭТОЙ СЕССИИ (всё в проде, с бэкапами и отчётами)
- **`set_fleet_oil`** — запись «ТО Oil» в Лист1 Байки col I по НОМЕРУ байка (guarded, `confirmed=true`,
  защиты not_found/ambiguous/oil_decreasing). Задача 4255 закрыта (col I 17000→24094).
- **`serviceUpsert` анти-дубль** — матч строки ТО по НОМЕРУ (+`canonicalBikeName_`), а не по имени;
  oil/gear не схлопываются. Новые дубли по дрейфу имени не плодятся.
- **LLM-аудитор Splinter в проде** — `judge` на **Haiku 4.5**, контекст группы из memory.db,
  детерминир. префильтр, канал правок в тему **«Аудит» = thread 161** форума HQ (`-1003853365891`),
  кнопки 👍 принять(→`add_rule`)/✏️ дописать/👎 отклонить. Доставка карточки починена
  (GC-баг `asyncio.create_task` без ссылки → `_bg_tasks` + `add_done_callback`).
  `AUDIT_GROUPS` = Самоорганизация/Cashflow/Обслуживание. verdict=ok карточку не шлёт.
- **Персист тема→байк** — memory.db таблица `topic_bike` (источник правды), seed `_TOPIC_NAMES`
  при старте + write-through. Переживает рестарт (раньше привязка стиралась → бот слеп по темам).
- **Автономия Claude Code** — 3 зоны (🟢 зелёная / 🟠 оранжевая / 🔴 тёмно-красная),
  `settings.local.json` read-only allowlist (рутину не переспрашивает), WAITING-лог (дословно),
  честность статуса («технически» ≠ «функционально подтверждено»).
- **Триггер «масло без чёткого пробега → бот сам просит фото одометра»** — RU/TH (TH первым),
  анти-спам (буфер high-пробега + троттлинг 10 мин), без тега Пыма. gear oil = тот же масло-контекст.
- **Интервалы ТО в KB** — масло скутеры 4000 / мото+XADV 5000; gear oil = тот же (только скутеры);
  ABS oil 10000 (все); масляный фильтр 20000 (все). ⚠️ Расчёт ABS/фильтра в боте НЕ реализован — будущий шаг.
- Деплой: коммиты …→`01ac3e6`(масло-триггер)→`b0f06b9`(интервалы KB);
  clasp redeploy `@20`(set_fleet_oil) `@21`(serviceUpsert).

### ▶️ ПЛАН — КЛИЕНТСКАЯ ИНТЕГРАЦИЯ (главное направление, решение Филиппа 7 июня)
**Главное:** бот общается с КЛИЕНТАМИ. Порядок:
1. **СНАЧАЛА Telegram userbot** (Telethon от личного **@turbophuket**, 1798 диалогов). Старт на ПК
   (Пхукет, домашний IP — безопасно, привычный IP). Целевое устройство = **Raspberry Pi дома**
   (ещё НЕ куплен; апгрейд на 24/7, IP тот же → перенос безопасен). **Полуавтомат-черновики** на старте
   (бот готовит ответ → человек отправляет): обкатка FAQ + TG не видит автомат 24/7.
2. **ПОТОМ WhatsApp Cloud API** (виртуальный номер, НЕ личный — легально по политике Meta;
   задаче-ориентированный бот + handoff). После обкатки FAQ на TG.
3. **Язык клиентского бота:** RU (основной) + EN, БЕЗ тайского (с клиентами только Даня/Даша;
   тайцы с клиентами не общаются — два контура НЕ путать).
4. **База ответов:** `turbobaby_faq_v1.md` (710 диалогов; топ-темы цена/выбор/оплата/доставка/сроки/депозит,
   приветственный скрипт).
5. **ПЕРВЫЙ конкретный шаг = движок ответов на базе FAQ** (понять тему входящего → ответить по FAQ
   или позвать человека), общий для TG и WA.

### 🔗 ЦИКЛ АРЕНДЫ (этапы 5-6, внутренний контур) — встроить ПОЗЖЕ, когда клиентский бот доводит до брони
- Этап 4 (бронь, `Booking.gs` createBooking/activateBooking) СДЕЛАН, но не выкачен
  (хвост Филиппа: Deploy NEW VERSION в Apps Script Bridge).
- Этап 5: из брони → задача тайцу в Delivery (`-1002445921469`), RU/TH через Splinter.
- Этап 6: фото выдачи → `activate_booking` (Бронь→В аренде).
- ЛОГИКА: этапы 5-6 склеиваются с клиентским ботом в точке «клиент согласился на бронь →
  createBooking → задача тайцу → фото → активация». Делать когда есть вход (бот доводит до брони),
  иначе повиснут.

### 🔧 ПАРАЛЛЕЛЬНО (по ходу, не блокирует клиентскую ветку)
Мелкие правки аудитора и поведения в группах — последовательно.

### 🔴 ОТКРЫТЫЙ RISK (держать в хвостах)
EdenAI API-ключ (OCR паспортов клиентов) + GitHub PAT не отозваны — revoke когда дойдут руки.

---

## 🗓️ СЕССИЯ 9 июня 2026 — ТО-блок «закрытие ТО» автоматизирован (B/A/C) + касса + allowlist

### 🔧 ТО-БЛОК «ЗАКРЫТИЕ ТО» — тройка фиксов A/B/C, ВСЕ В ПРОДЕ
**Проблема (диагностика):** при «заменили масло + фото приборки» бот вместо закрытия ТО писал
«просрочка X» (сверял с СТАРЫМ ТО Oil), не распознавал «замена выполнена»; vision-ошибки пробега
(31844 < прошлого 37000) проходили молча; `set_fleet_oil` существовала, но в АВТОпоток бота
**не была подключена** (ручное закрытие 4255 7.06 — было, автоматизации — не было).
- **Фикс B — сторож пробега** (коммит `254a152`): пробег с фото сравнивается с последним известным
  по теме (`last_mileage_in_topic`, `exclude_km` — текущий замер уже в буфере). new < prev → флаг
  «пробег не может уменьшиться», в ТО/Лист1 НЕ пишем до корректной цифры; `floor` в pending не даёт
  «да» подтвердить заведомо неверное число.
- **Фикс A — закрытие ТО + запись ТО Oil** (коммит `f2703fb`): контекст «масло заменили»
  (`_is_oil_context` + распознанный пробег = `oil_done`) → после сторожа B бот предлагает
  «🔧 Записать новое ТО Oil = X? да/нет» (вместо «просрочка»). По «да» → `set_fleet_oil(plate, X,
  confirmed=True)` в Лист1 кол.I → просрочка закрыта, закреп снят (unpin + important_close).
  Номер байка — голый `plate` (`_plate_from_name`, зеркало `plateFromName_`).
  **Авторизация = «доверенные»:** боевую запись запускает «да» ТОЛЬКО от Пыма/владельца
  (`TRUSTED_AUTHORS`); «да» техника (Earth) НЕ пишет — бот просит подтверждение у Пыма, pending
  держится с TTL 30 мин. «нет» → обычный контрольный замер.
- **Фикс C — dirt в сервис-контексте** (коммит `c3fab9b`): совет «помыть/воск/чехол» подавляется,
  когда байк в сервисе (`oil_done`/`_is_oil_context`/`event_type==repair`/`vis.kind==receipt`) —
  байк чинят, а не моют. Грязь vision распознаёт как прежде, подавляется только совет.
- Все три по оранжевому протоколу (бэкап→push→restart→проверка active+polling+Auditor LLM✅+0 ошибок).
  Реальный `confirmed=True` в проде запускает ТОЛЬКО доверенный. **Ждут функционального теста** на
  живых фото/сообщениях (сквозной сценарий: фото+«заменили» → сторож B → вопрос → «да» Пыма → запись).

### 💰 КАССА САМООРГАНИЗАЦИИ — разовая путаница Пыма, сторно НЕ потребовалось
Пым дважды учёл перенос из Money Cashflow (+2015 авто-перенос, затем ручное +2000 = дубль).
Запрос был сторнировать −2000. По сырому листу Bot Data дубль **уже был снят** записью −2000
«фиксация баланса до 1529» (04:25). Реальный баланс = **1429 THB**. Повторное сторно увело бы в минус —
НЕ делали, в Bot Data ничего не писали. Урок: сверять с фактическим листом, а не с описанием задачи.

### ⚙️ ALLOWLIST Claude Code дотянут (`.claude/settings.local.json`)
- `additionalDirectories`: `/root/turbobaby-bridge-gs` + memory-папка → `cd`-чтение в наши папки без Yes
  (главный фикс: `cd` спрашивал, т.к. цель была ВНЕ рабочей папки, а не из-за паттерна).
- `ask`-гейт (перебивает allow): `sqlite3 *`, `clasp push/redeploy/deploy` — боевая запись/деплой под гейтом.
- `confirmed=true` к Bridge технически не отделим в settings → гейт ДИСЦИПЛИНАРНЫЙ (Claude Code сам
  стопает перед боевой записью). `cd`+`git` в одной цепочке всегда спрашивает (хардкод) → юзаем `git -C`.

### 🔧 Функциональный тест ТО-блока — ОЖИДАЕТ первой реальной замены
Фиксы A/B/C в проде, протестированы на моках. Боевое крещение — на ПЕРВОЙ живой замене масла после деплоя (05:57 UTC 9.06). Что проверить когда Earth/Пым пришлёт «заменили масло» + фото приборки:
1) бот спрашивает «🔧 записать ТО Oil = X? да/нет» (НЕ «просрочка»);
2) «да» от Пыма/владельца → запись в Лист1 кол.I + снят закреп;
3) «да» от Earth (недоверенный) → бот просит подтверждения Пыма, pending 30 мин;
4) пробег ниже прошлого → сторож B блокирует до записи.
NINJA 6334: разбор этой замены (37844) ВСКРЫЛ три бага → стал поводом для фиксов A/B/C. Сама замена 37844 была ДО деплоя A (ручного закрытия не делали — ждём, либо закрыть вручную через set_fleet_oil('6334',37844,confirmed=True) с «да» Филиппа).
**ОБНОВЛЕНО вечером 9.06:** первый реальный прогон состоялся (FORZA 8950) и вскрыл ещё баги доставки/закрепа/квитанции → фиксы A2 + service_set_pin + квитанция (см. подраздел «🐞 ТО-блок — баги первого боевого прогона» ниже). FORZA 8950 закрыта вручную. Чистый end-to-end прогон на живой замене всё ещё ОЖИДАЕТСЯ.

### ⚙️ Клики Termux — диагностированы и убраны формой команд
Причина «да-да-да» = ВСТРОЕННЫЙ guard Claude Code (инъекц-детектор: «cd with output redirection», «brace with quote»), НЕ наш конфиг. Конфигом не отключается (--dangerously-skip-permissions = всё-или-ничего, снял бы и гейт боевой записи — НЕ применяем). Решение: правило «чистой формы команд» в CLAUDE.md (коммит a7b6dc5) — чтение через абс.путь без cd/редиректа; python-разведка через файл (Write-инструмент), не heredoc; git -C вместо cd. Гейт боевой записи (sqlite3/clasp/confirmed=true в Лист1/CRM) СОХРАНЁН. Claude Code уже пишет команды по-новому (проверено на NOTE). Клики стали редкими, не нулём (guard намеренно неотключаем).

### 🐞 ТО-блок — баги первого боевого прогона и фиксы (вечер 9.06, все в проде)
Первый реальный прогон closing-ТО был на **FORZA 300 WHITE 8950** и ВСКРЫЛ цепочку проблем. По каждой: разведка → NOTE → PLAN → ревью/«да» → 🟠-деплой → отчёт.
- **Фикс A2 — маркер замены + фото-пробега из РАЗНЫХ сообщений** (коммит `98804f2`). Провал: Earth прислал текст «заменены масло и фильтр» и фото приборки 30225 ОТДЕЛЬНЫМИ сообщениями → `oil_done` (требовал масло-слова И пробег в ОДНОМ сообщении) не собрался → 30225 ушёл в обычную сверку → «просрочка», вопрос о записи НЕ появился. Фикс: маркер «замена ВЫПОЛНЕНА» (прошедшее время: заменили/заменено/поменяли/готово/แล้ว/เสร็จ; будущее «надо заменить» НЕ ловится) взводит окно по теме (TTL 15 мин); пока активно — следующее фото с пробегом = `oil_done`. Симметрия (фото раньше маркера → по `last_mileage_in_topic`). dirt-подавление распространено на активное окно. Сторож B и авторизация доверенных НЕ ослаблены.
- **FORZA 8950 закрыта ВРУЧНУЮ** (🔴, по «да»): `set_fleet_oil('8950',30225,confirmed=True)` → Лист1 строка 8, кол.I **25400→30225**, следующее 34225. Закреп просрочки снят. Бэкап 25400 в cc_log до записи. Кол.H/J не трогали.
- **Фикс service_set_pin → not_found** (коммит `5f95b53`, splinter-only). Корень: `serviceUpsert` пишет КАНОНИЧНОЕ имя из Лист1 и матчит по номеру, а `serviceSetPin` + две splinter-проверки сравнивали ТОЧНО с именем темы → не находили строку → `pinned_msg_id` не сохранялся, автоснятие закрепа не работало, повторный закреп каждый раз. Фикс: новый `_same_bike` (матч по номеру, fallback на точное имя) в `_check_service`+`_close_service_reminder`; в `service_set_pin` шлём каноничное имя из `service_upsert`. Bridge .gs НЕ трогали.
- **Квитанция на подтверждённый пробег** (коммит `eabac77`, splinter-only). `_check_service` при «ТО в норме» молча выходил → на «да» по фото пробега человек не получал ответа. Теперь короткая двуязычная `msg_mileage_ok` («✅ Пробег X км принят. ТО в норме, следующее на Y (осталось ~Z км)»). Ветка просрочки (закреп) не тронута.
- **Открытый хвост (НЕ делали):** `serviceSetPin` в `BotData.js` тоже матчить по номеру — внутренняя консистентность Bridge; это .gs → `clasp redeploy` 🔴, отдельным шагом с «да». Сейчас splinter-side фикс закрывает баг. NINJA 6334 (37844) ручного закрытия так и не делали — закрыть при случае или дождаться повторной отправки через починенный флоу.
- Каждый фикс протестирован на моках и задеплоен по оранжевому протоколу (бэкап-коммит → push → restart → active/polling/Auditor LLM✅/0 ошибок). Все ЖДУТ функционального теста на следующей живой замене.

### 🔧 ТО-блок — доработки по итогам закрытия NINJA 6334 (9.06, в проде)
Закрытие 6334 отработало (кол.I 30300→37823). По итогам — заход 1 + фикс раскладки работ.
- **Заход 1** (коммит `36c3119`, splinter-only):
  - **п.1** инлайн-кнопка `[✅ ใช่/Да]` на подтверждение распознанного пробега (текст «да»/число тоже работает); токен `mileconf` хранит floor → сторож B сохранён; trust-гейт записи кол.I не ослаблен.
  - **п.2** открепление: `_close_service_reminder` снимает ВСЕ накопленные ТО-пины темы (`unpin_all_forum_topic_messages`) + чистит `pinned_msg_id`; `_pin_overdue_reminder` откручивает прошлый пин перед новым (не копить). Чинит «открепа не видно» (пины копились, снимался лишь последний).
  - **п.4** прочие работы техника → Bot Data «события» (`event_type='repair'`, что+пробег). Масло — на кнопочном флоу (кол.I).
- **Фикс раскладки `_is_oil_work`** (коммит `1b6a096`, splinter-only). По ФАКТ-карте Лист1 (прочитана живьём): записываемый ботом столбец один — **I «ТО Oil» (моторное масло)**. Прежняя версия считала маслом любое "oil"/"масл" → gear oil / ABS oil / редуктор ТЕРЯЛИСЬ. Теперь `True` только для моторного масла; немоторные (gear/редуктор/ремень/шестерни/трансмисс/ABS/абс/вилка + фильтры) → «события» как справка, не теряются. Колодки/цепь → события. Мок 18 видов работ ✅.
- **п.3 (чистка промежуточных сообщений цикла)** — ХВОСТ, заход 2, ещё не начат.

### 🔧 УЧЁТ J/K/L — ФАЗА 1 (ЗАПИСЬ) СДЕЛАНА 9.06, в проде
ФАКТ-карта столбцов Лист1 (прочитана живьём): `I=ТО Oil` (моторное масло), `J=ТО Gear` (редуктор/
ремень/шестерни), `K=АБС` (ABS oil), `L=аир фильтр` (воздушный). Модель Филиппа = 2 регистра: регистр-1 =
столбцы I/J/K/L (рабочие данные, расчёт ТО); регистр-2 = «события» (сноска-инфо без интервала: масляный
фильтр, колодки, цепь, грипсы, тормоза, прочее).
- **Фаза 1 — ЗАПИСЬ J/K/L** (Bridge redeploy @22 + Python `6c24f9b`, в проде): ReadFleet читает
  `gear_last_km/abs_last_km/airfilter_last_km` (row[9..11]); guarded `setFleetService_(number,kind,km,
  confirmed)` kind gear→J/abs→K/airfilter→L (зеркало setFleetOil_; кол.I/H не трогает). splinter:
  `_classify_work`→oil|gear|abs|airfilter|info; группа B + известный пробег → кнопка [✅ Зафиксировать
  <тип>] (двуязычно), боевая запись ТОЛЬКО доверенным; info → «события». set_fleet_oil/кол.I не тронут.
  ЖДЁТ функционального теста (техник пишет «заменили редуктор/ABS/фильтр» + пробег → кнопка → «да» Пыма
  → значение в кол.J/K/L). Бэкап .gs: ReadFleet.js.bak-fase1 / Bridge.js.bak-fase1; откат Bridge=redeploy -V 21.
- **Фаза 2 — РАСЧЁТ + НАПОМИНАНИЯ** (НЕ начата, отдельный «да»): машиночитаемый блок интервалов в
  knowledge_base на Drive (read_doc + парс, с фоллбэком на код; убрать дубль `_oil_interval`/`oil_interval_for`);
  расчёт next_km по типу + напоминания/закрепы (gear только скутеры; ABS все; возд.фильтр все). Интервалы
  (KB+решение Филиппа): масло скутеры 4000/мото 5000; gear=как масло (скутеры); ABS 10000; возд.фильтр 20000.
  В KB дописать строку «воздушный фильтр (кол.L) = 20000, все» (сейчас в KB только масляный фильтр 20000).

### 🧹 Автопрореживание cc_log (Apps Script) — в проде 9.06
cc_log распух до 162KB → Google Docs флапал на чтении/записи (тормоз дня). Решение в 2 части:
- **Часть A** (разово, Python+Drive MCP): создан архив `KB_claude_code_log_archive` (id 19hO_c8FRD…894OBfY)
  в папке Brain; cc_log ужат до ~50KB (27 новейших записей), 98 старых → в архив, без потерь (125=27+98).
- **Часть B** (Apps Script, Bridge redeploy @25): новый `Archive.gs` — `pruneCcLog_()` читает cc_log
  ЛОКАЛЬНО (без HTTP-флапа), держит его <= `CCLOG_MAX_BYTES` (Script Property, дефолт 50000), старое →
  архив (newest-first). Экшены `prune_cc_log` / `setup_prune_trigger`; `bridge_client` — одноимённые методы.
  Архив зарегистрирован в BRAIN_MANIFEST (ключ `cc_log_archive`). Порог меняется без редеплоя (Script Property).
  **ОСТАЁТСЯ 1 ручной шаг Филиппа** (как setupBrain): запустить `setupPruneTrigger()` из редактора Apps Script
  (нужен scope script.scriptapp) → ставит ежедневный триггер ~13:00 UTC. До этого авто-расписания нет, разовый
  `prune_cc_log` работает. Откат Bridge = `clasp redeploy AKfycbxNC9…NOqw -V 22`.

### 🛠️ РАЗБОР ПЕРЕЧНЯ РАБОТ ПО АДРЕСАМ + delete_event (вечер 9.06, всё в проде, функц. подтверждено)
Цепочка фиксов сервис-контура (Splinter), все с моками и отчётами в cc_log:
- **Фикс молчания на текст-работ** (`36479ef`): перечень работ ТЕКСТОМ без фото пробега больше не
  игнорируется — бот отвечает квитанцией и переспрашивает пробег. Корень был: `parse` без mileage →
  все реплики-ветки пропускались, ask-odometer глушился доработочным замером из буфера фото (3ч TTL).
- **Квитанция/ответы ТО — 🇹🇭 без кириллицы** (`ee4232c`, `aeee257`): аудитор метил `style_issue`
  «кириллица в тайском блоке» — «ТО»/«ТО Oil»/русский список работ протекали в 🇹🇭. Убрано (тайский
  эквивалент: `การเปลี่ยนน้ำมันเครื่อง` / `รอบเปลี่ยนน้ำมันปกติ`), 🇷🇺 не тронут. Шаблоны 1319/1437/1929.
- **Диаг-лог `return add_event`** (`2ae4c85`): бот раньше ВЫБРАСЫВАЛ результат `add_event` — не было
  видно saved/duplicate/error. Диагноз показал: запись событий РАБОЧАЯ (`saved=True`); «пустота
  «события» с 01.06» оказалась АРТЕФАКТОМ КЭША Drive-экспорта `read_file_content` (он отдаёт устаревший
  снимок — сверять записи по modifiedTime / return Bridge / лог, НЕ по экспорту).
- **★ РАЗБОР ПЕРЕЧНЯ РАБОТ ПО АДРЕСАМ** (`baa0863`, вариант A — строка-на-работу): перечень больше НЕ
  валится одной repair-строкой. Каждая работа → свой адрес: масло→кол.I, gear/abs/возд.фильтр→группа B
  (J/K/L), ИНФО-работы (колодки/цепь/масл.фильтр/вилка) → история «события» ОТДЕЛЬНОЙ строкой с
  привязкой пробега. Пробег привязывается КО ВСЕМ инфо-работам: если км в этом сообщении — пишем сразу;
  если нет — буфер `_PENDING_WORKS` по теме + переспрос, **flush при приходе подтверждённого км**
  (фото/число) дописывает строки «<работа> — <км> км». `msg_id` с суффиксом `:wN` (дедуп Bridge не
  схлопывает), `pop` → без задвоения, TTL 3ч. Сторож B / trust-гейт / кол.H/I / set_fleet — НЕ тронуты.
  **Функц. подтверждено в бою 22:23–22:24** (NINJA 6334): текст работ без км → буфер → фото 37823 →
  3 строки «фильтр/колодки/цепь — 37823 км» + масло в кол.I. Тесты `_test_works.py` (18 PASS) +
  `_test_servicing.py`.
- **★ `delete_event` — удаление строк «события» по точному ключу** (Bridge `@29→@30`, `bridge_client`
  `a0dfaf6`): новый Bridge-экшен `deleteEvent` (BotData.js) с защитой — без ключа → `no_key`; пустой/
  `*`/`%` group → `bad_group`; совпало > лимита (50, потолок 200) → `too_many` (НИЧЕГО не удаляет);
  EXACT-равенство; ТОЛЬКО лист EVENTS (рабочих таблиц не касается); возвращает `deleted`+`rows` для
  сверки по return. `bridge_client.delete_event(msg_id, group, max)`. Откат Bridge = `clasp redeploy
  AKfycbxNC9…NOqw` версия @29. Разово применён: убраны 2 диаг-строки `group='DIAG-TEST'`.

---

## 🚀 ЗАДАЧА clasp — ✅ ВЫПОЛНЕНО 6 июня 2026 (автоправка+деплой Bridge .gs без копипасты)

**РЕЗУЛЬТАТ:** все 8 шагов пройдены. clasp 3.3.0 на VPS, залогинен info@turbophuket.com
(/root/.clasprc.json). Прод-Bridge склонирован в `/root/turbobaby-bridge-gs` (отдельно от
Python-репо), Script ID 12iXPDU…HhOJ. Цикл доказан end-to-end: правка → `clasp push` (HEAD) →
`clasp redeploy AKfycbxNC9…NOqw` (прод-деплой @18→@19, URL `BRIDGE_URL` НЕ сменился, ping ok
v1.0.0, list_brain цел). Штатный цикл записан в CLAUDE.md (раздел «Деплой Bridge через clasp»).
**Важные поправки по ходу (clasp v3 ≠ план):** обновление прода = `clasp redeploy <deploymentId>`
(НЕ `deploy --deploymentId` — это создало бы новый деплой и сменило URL); login --no-localhost
просит ПОЛНЫЙ redirect-URL (localhost:8888/?code=…), а не короткий код; для push нужен
включённый Apps Script API (https://script.google.com/home/usersettings, одноразово).

Ниже — исходный план (как ставилась задача), оставлен для истории.

**ЦЕЛЬ:** Claude Code сам правит `.gs` локально и деплоит Bridge через clasp;
Филипп не копипастит код в браузер Apps Script и не жмёт Deploy руками.

**ЕДИНСТВЕННЫЙ ручной шаг Филиппа** (нельзя автоматизировать): один раз
`clasp login --no-localhost` → перейти по ссылке, войти под **info@turbophuket.com**,
скопировать код в терминал (~2 мин). Дальше — только «да» на развилках, можно с телефона.

**ПОШАГОВО** (Claude Code делает, Филипп подтверждает):
1. `npm i -g @google/clasp` на VPS; `clasp --version`.
2. `clasp login --no-localhost` → Филипп проходит OAuth (info@turbophuket.com).
3. Узнать Script ID проекта «TurboBaby Bridge»: `clasp list` ИЛИ Филипп копирует
   из URL проекта (`/projects/<SCRIPT_ID>/edit`). Проверка верности после clone:
   файлы `Config/Bridge/BotData/ReadFleet/ReadClients/ReadFinance/ReadDocs/Booking.gs`
   + `ReadDocs.gs` содержит `setupCcLog` И `setupReview`.
4. `clasp clone <SCRIPT_ID>` в ОТДЕЛЬНУЮ папку (НЕ `/root/turbobaby-manager-bot`,
   напр. `/root/turbobaby-bridge-gs`). Это первый pull — тянет актуальный прод-код.
5. Сверить с репо-зеркалом `ReadDocs.gs`; при расхождении прод важнее.
   Зафиксировать `.clasp.json`.
6. ТЕСТ push на безопасной правке (комментарий в `ReadDocs.gs`) → `clasp push` →
   проверить в браузере. БЕЗ deploy пока.
7. Deploy: deploymentId действующего Bridge (из BRIDGE_URL между `/s/` и `/exec`
   ИЛИ `clasp deployments`) → `clasp deploy --deploymentId <ID>` (обновляет
   СУЩЕСТВУЮЩИЙ, URL не меняется, Splinter не падает).
   Проверить ping v1.0.0 + `list_brain`.
8. Записать в `CLAUDE.md` штатный clasp-цикл (pull→правка→push→deploy --deploymentId)
   как замену копипасте.

**КРИТИЧНО** (каждое ломает прод):
- `clasp push` ПЕРЕЗАПИСЫВАЕТ весь проект → первый раз ТОЛЬКО `clone`.
- deploy строго `--deploymentId` существующего (не плодить новые, иначе URL сменится).
- clasp-папку держать отдельно от Python-репо.
- чужие `.gs` не трогать.

---

## Что это
AI-команда ботов для TurboBaby (премиум аренда мотобайков, Пхукет, владелец Филипп, 38 байков). Владелец — не программист, работает на Windows (VS Code + Python 3.12 + Node.js). Рабочие папки на ПК: `D:\turbobaby-bot\` (userbot-скрипты) и `D:\turbobaby-bot\manager-bot\` (бот Splinter).

## Архитектура (готово и работает)
- **Apps Script Bridge** (Web App) — мост к Google Sheets. Деплоится в Apps Script. GET: ping/fleet/clients/anomalies/daily_pulse. POST: транзакции, баланс, обслуживание (ТО), важное, аудит. URL и токен — в .env бота.
- **Splinter** (manager-bot, Python) — внутренний бот-супервайзер в Telegram. Работает в HQ + 5 операционных группах (касса, обслуживание-форум-с-темами, доставка, отметки). Двуязычный RU+TH (в группах есть тайцы). Модель claude-sonnet-4-5.
- **memory.db** (SQLite) — память: правила, заметки, история диалогов (изолирована по chat_id+topic_id).
- **Auditor** (auditor.py) — надзорный модуль: сверяет действия бота с фактами (БД/KB), ловит галлюцинации, нарушения правил, ошибки логики, смешение языков. Журнал — лист `аудит`. Отчёт в HQ 09:30 + команда /audit. Сам не правит — только флаги + предложения, применяются после апрува Филиппа.

## Ключевые правила (бизнес-логика)
- Депозит = деньги ИЛИ паспорт (не оба).
- HONDA CLICK 125 НЕ сдаём (предлагать PCX 150 / ADV 150 / NMAX 155).
- Доставка только по Пхукету.
- Интервал ТО масла: скутеры (NMAX/XMAX/ADV/FORZA/PCX) — 4000 км; XADV + мотоциклы (XSR/CB/CBR/MT/NINJA/VULCAN/R7/REBEL) — 5000 км.
- ТО Oil берётся из колонки I «ТО Oil» Листа1 Байки (НЕ J «ТО Gear»).
- Важные цифры с ФОТО (пробег, суммы) — ВСЕГДА подтверждать у человека перед записью (vision ошибается на LCD-одометрах: 19705→19105→29105).
- Splinter сам деньги не двигает, изменения в Sheets — через approve-флоу.
- Никаких рассылок клиентам.
- Тайская команда НЕ общается с клиентами вообще.

## Два контура — НЕ путать
- **Splinter (внутренний)** → RU+TH (в рабочих группах есть тайцы).
- **Клиент-бот (будущий, наружу)** → RU+EN, БЕЗ тайского (с клиентами работают только Даня/Даша, русскоязычные).

## Последние сделанные правки (эта серия сессий)
1. Корректный интервал ТО по типу байка.
2. Полный надзор за важным: заметил→спросил→ОДНО «да»→закрепил→напоминание раз в 3 дня→закрытие.
3. Изоляция истории по темам (chat_id+topic_id).
4. Подтверждение важных цифр с фото перед записью (промпт + код-страховка confirmed).
5. Ответ без тега: только когда бот сам задал вопрос в теме (10 мин) ИЛИ фото без подписи в теме обслуживания. В HQ awaiting выключен.
6. Индикатор «Splinter печатает…» (фоновый, гаснет сам, не зависает; ask вынесен в executor).
7. Читаемые логи: «тема 73 (NINJA 400 6334)» вместо голого номера.
8. get_service берёт пробег из листа обслуживания если фото нет в буфере (после рестарта).
9. Auditor: факт-проверки + проверка стиля/языка; убрана ложная тревога на отложенный закреп.
10. fetch_client_chats.py — выгрузка клиентских переписок TG за год (read-only).
11. turbobaby_faq_v1.md — FAQ из 710 диалогов (топ-темы: цена, выбор, оплата, доставка, сроки, депозит).

## Что дальше (выбрать в новом чате)
**Клиентское направление (выбран этот путь):**
- Проверить формулировки FAQ под тон владельца.
- WhatsApp: ручной экспорт чатов (.txt) → разбор (авто нельзя — бан номера).
- EN-версии ответов FAQ.
- Клиент-бот: лестница — черновики менеджеру → полуавтономный → узкие авто-сценарии (бронь/цена/часы). Языки RU+EN.

**Внутренний контур (отложено):**
- Кнопки/команды по группам (тайцам: Пришёл/Выехал/Доставил; владельцу меню в HQ).
- LLM-проверка перевода в аудиторе (естественность, совпадение смысла блоков).
- Этап 4: запись броней в CRM с approve-флоу.
- Перенос на Render (24/7) после обкатки. Userbot остаётся на ПК.

## Файлы в этом снапшоте
- `turbobaby-manager-bot/` — bot.py, splinter.py, claude_client.py, bridge_client.py, memory.py, prompts.py, auditor.py, requirements.txt, README.md
- `turbobaby-bridge/` — Config.gs, Bridge.gs, BotData.gs, ReadFleet.gs, ReadClients.gs, ReadFinance.gs, README.md
- `fetch_topics.py`, `fetch_client_chats.py` — userbot-скрипты (запуск с ПК)
- `knowledge_base.md` — полная база знаний (v1.6+)
- `turbobaby_faq_v1.md` — клиентский FAQ

## Как продолжить
В новом чате: загрузить эти файлы в проект (knowledge_base.md и этот файл — обязательно в Project Knowledge, как ОТДЕЛЬНЫЕ .md файлы, НЕ внутри .zip). Сказать новому Claude: «продолжаем TurboBaby, см. project_state.md» — он сориентируется.

## ✅ ЭТАП 4 (постановка брони) — СДЕЛАНО (1 июня)
Механика предв.брони разобрана по реальной таблице менеджеру_Байки.xlsx:
- Статус живёт в колонке A листа "клиенты" (текст: "В аренде"/"Завершена"; теперь +"Бронь").
- Колонка W — вспом.формула IF(NOW()>F...) "для скрипта и проверки", НЕ главный статус.
- Формулы долга I/J: =IF(OR(A="в аренде","в аренде 1","в аренде 2"), ...) → при статусе
  "Бронь" долг НЕ начисляется. Это и используем: бронь не тикает до выдачи.
- Форма Rental Agreement пишет в "клиенты" со статусом "В аренде", карта formToSheetColumns:
  C байк, D имя, E старт, F конец+время, K оплачено, L цена/мес, M цена/день, N пробег,
  S залог(число ИЛИ "passport"), T шлемы, U контакты, V note.

Созданы файлы (в outputs, надо выкатить):
- Booking.gs (НОВЫЙ в Bridge-проекте): createBooking() ставит A="Бронь"; activateBooking()
  меняет Бронь→В аренде после фото выдачи. Защита от дублей. Карта колонок сверена 14/14.
- Bridge.gs: +case create_booking, +case activate_booking (не забыть Deploy NEW VERSION!).
- bridge_client.py: +create_booking(**fields), +activate_booking(bike,name).

⚠️ БЕЗОПАСНОСТЬ: API-ключ EdenAI засветился в чате (был в config формы) — Филиппу ОТОЗВАТЬ
   и выпустить новый, положить в Script Properties EDENAI_API_KEY (Config.gs формы уже правлен).
   (РИСК ОТКРЫТ — пересмотрено 7.06: EdenAI = OCR паспортов клиентов, отзыв отложен осознанно; см. секцию 7 июня → RISK)

ДАЛЬШЕ: этап 5 — из брони сформировать задачу тайцу в группу Delivery (-1002445921469),
двуязычно RU/TH через Splinter. Затем этап 6 — фото выдачи → activate_booking.

## ▶️ ТЕКУЩАЯ ТОЧКА (конец сессии 1 июня) — продолжить в новом чате
СДЕЛАНО в этой сессии:
- Этап 4 (бронь) готов: Booking.gs (+testBooking/testActivate), Bridge.gs (+2 case),
  bridge_client.py (+create_booking/activate_booking). Карта колонок сверена 14/14 по
  реальному meneджеру_Байки.xlsx. Бронь пишется в первую пустую строку A (сейчас 1253),
  дырок в таблице нет, формулы не затрагиваются. Статус "Бронь" не начисляет долг.
- Также ранее в сессии: splinter.py (грязь≠повреждение, депозит, обёртка _send через
  аудитор), bot.py (set_auditor). Всё откомпилировано/протестировано.

НАДО СДЕЛАТЬ ФИЛИППУ (руками):
1. Выкатить Booking.gs + Bridge.gs в Apps Script проект Bridge, Deploy → NEW VERSION.
2. Запустить testBooking() кнопкой Run, проверить строку 1253 = "Бронь", долг пустой, удалить тест.
3. Выкатить splinter.py + bot.py + bridge_client.py в manager-bot, перезапустить.
4. ⚠️ Отозвать засветившийся EdenAI API-ключ, новый → Script Properties EDENAI_API_KEY. (РИСК ОТКРЫТ — пересмотрено 7.06, отзыв отложен осознанно; см. секцию 7 июня → RISK)

## 🚀 СЛЕДУЮЩАЯ БОЛЬШАЯ ЗАДАЧА: перенос Splinter в онлайн (24/7, комп выключен)
Начать в НОВОМ чате (этот уперся в длину). Стартовые вводные:
- Переносим ТОЛЬКО manager-bot (Splinter) — это токен-бот, безопасно на сервере.
  Userbot (Telethon, личный аккаунт) НЕ переносим — остаётся на ПК (риск бана по IP).
- Хостинг: у Филиппа есть Render. Вариант — Render worker, ИЛИ VPS (терминал с телефона).
- ⚠️ ГЛАВНАЯ ЗАСАДА — memory.db (SQLite на диске). На Render файловая система эфемерная →
  при передеплое стирается, бот забудет правила. Решить ДО переноса: либо Render persistent
  disk, либо вынести память в Google Sheets через Bridge (Bot Data уже есть).
- Нужно для переноса: .env (токены — НЕ в git!), requirements.txt (есть), точка входа bot.py.
- Сделать .gitignore: .env, venv, *.session, memory.db, __pycache__.
- Bridge доступен с любого IP (Web App "Anyone") — с сервера достучится, проблем нет.

## ⏭️ ПОТОМ: этап 5 (задача тайцу из брони в Delivery -1002445921469, RU/TH) + этап 6
   (фото выдачи → activate_booking). И отдельный трек — клиентский WhatsApp (Cloud API,
   виртуальный номер, готовая платформа, задаче-ориентированный бот + handoff).

## 🚀 ПЕРЕНОС В ОНЛАЙН — ПРОГРЕСС (1 июня)
РЕШЕНО: хостинг = VPS (Hetzner, обычный, Европа — для Splinter IP не важен, это токен-бот).
  Userbot НЕ переносим (остаётся на ПК / в будущем Raspberry Pi дома, пхукетский IP).
  Память memory.db: на VPS диск постоянный → проблема Render отпадает, ничего переписывать не надо.
ЦЕЛЬ: на VPS крутится Splinter 24/7 + стоит Claude Code (агент-разработчик, правит код
  по задачам Филиппа через подтверждения) + git для истории/бэкапов.

СДЕЛАНО:
- ✅ Git установлен на ПК (Windows), настроен user.name/email.
- ✅ .gitignore создан (исключает .env, memory.db, venv, *.session, __pycache__) — проверен,
  секреты НЕ попадают в репо.
- ✅ git init + commit (9 файлов: код + requirements + .gitignore).
- ✅ Приватный GitHub репозиторий: https://github.com/mxfill77/turbobaby-manager-bot
  Код залит (git push), секретов в репо НЕТ. Проверено.

СЛЕДУЮЩИЕ ШАГИ (продолжить отсюда):
1. Создать VPS Hetzner (CX22, Ubuntu 24.04). Зайти по SSH.
2. На VPS: установить Python 3.12, git, склонировать репозиторий.
3. Перенести .env на сервер ВРУЧНУЮ (его нет в git!) — токены TG/Bridge/Anthropic.
   Также перенести memory.db с ПК (история/правила) — тоже не в git, копировать отдельно (scp).
4. Создать venv, pip install -r requirements.txt.
5. Запустить bot.py как systemd-сервис (24/7, автоперезапуск). Проверить логи.
6. Установить Claude Code на VPS, настроить режим подтверждений.
⚠️ .env содержит: TG токен бота, BRIDGE_URL/BRIDGE_TOKEN, ANTHROPIC_API_KEY. На сервер — только
   вручную/scp, НЕ через git.

## ✅✅ ПЕРЕНОС SPLINTER В ОНЛАЙН — ЗАВЕРШЁН (1 июня)
Splinter работает 24/7 на VPS Hetzner, независимо от ПК.
- VPS: Hetzner CPX12, Сингапур, IP 5.223.94.179, Ubuntu 26.04 (Python 3.14).
- Код: GitHub приватный https://github.com/mxfill77/turbobaby-manager-bot
- На сервере: /root/turbobaby-manager-bot, venv, .env + memory.db перенесены через scp (НЕ в git).
- ⚠️ python-telegram-bot пришлось обновить (21.7 несовместим с Python 3.14) — обновлён до свежей.
  NB: requirements.txt в репо обновлён до ==22.7 (сделано 5.06, коммит 60ac562) — совпадает с сервером.
- systemd-сервис splinter.service: active(running), Restart=always, enable (автозапуск).
- Логи: /root/turbobaby-manager-bot/splinter.log
- ⚠️ Бот на ПК ДОЛЖЕН быть выключен (один токен = один бот). Рабочий теперь серверный.

Команды управления: systemctl {status|restart|stop} splinter ; tail -f splinter.log

ОБНОВЛЕНИЕ КОДА на сервере (когда правим): на ПК commit+push → на сервере
  cd /root/turbobaby-manager-bot && git pull && systemctl restart splinter
  (но .env/memory.db не трогаются — они вне git).

СЛЕДУЮЩЕЕ: установить Claude Code на VPS (агент-разработчик с подтверждениями).
Затем вернуться к этапу 5 (задача тайцу из брони) и этапу 6.
Не забыть: выкатить в репо актуальные splinter.py/bot.py/bridge_client.py (правки этой сессии:
грязь≠повреждение, _send через аудитор, create_booking) — закоммитить и git pull на сервере.
И отозвать засветившийся EdenAI ключ + GitHub токен (светился в чате). (РИСК ОТКРЫТ — пересмотрено 7.06, отзыв отложен осознанно; см. секцию 7 июня → RISK)

## 🧭 РАБОЧИЙ ФОРМАТ (выбрано с Филиппом — он не программист, работает разговором)
Связка двух сред, по типу задачи:
1. ПЛАНИРОВАНИЕ / разбор / решения → чат Claude на сайте (этот интерфейс) + project_state.md
   + knowledge_base.md как контекст. "Штаб". Здесь НЕ пишется код на сервере, проектируется.
2. ВОПЛОЩЕНИЕ в коде на сервере → Claude Code, но через VS Code (Remote-SSH к 5.223.94.179),
   НЕ голый терминал. Разговорный формат, наглядные диффы, кнопки принять/отклонить.
3. Telegram Dev Bot (управление ботом из TG-группы) — ОТЛОЖЕНО на потом (отдельный проект,
   строить через Claude Code когда основное готово; сейчас отвлекает от цикла аренды).

СЛЕДУЮЩИЙ ШАГ КОМФОРТА: настроить VS Code + расширение Remote-SSH + Claude Code расширение,
подключиться к серверу. Превращает работу с VPS в визуальную среду (окна, диффы, чат сбоку).
Расписать установку по шагам в новом чате.

### Правила для Claude-на-сайте (чтобы обе стороны синхронились честно) — заведено 7 июня 2026
- **ПРОВЕРКА = ВСЕ затронутые файлы, не только cc_log.** На «проверь» читать `cc_log` +
  `KB_review` + САМ правленый файл (project_state.md / CLAUDE.md / целевой док), а не один журнал.
- **НЕ ДОГАДЫВАТЬСЯ.** Не видно правки — сказать «проверяю» и открыть файл, не сочинять
  «наверное, ещё в процессе».
- **РАССИНХРОН DRIVE.** Терминал показал завершение, а лог пуст = DONE просто НЕ записан,
  а НЕ «шаг не сделан». Требовать запись в cc_log, не гадать.
(Зеркало со стороны Claude Code — раздел «РЕЖИМ РАБОТЫ» в CLAUDE.md: лог сразу, один шаг = один DONE.)

## 🚚 ПЕРЕХОД РАЗРАБОТКИ К CLAUDE CODE (решено 1 июня)
Основная разработка переезжает на сервер в Claude Code (VS Code, Remote-SSH к 5.223.94.179).
Туда переносится база знаний как контекст:
- В репо создать папку docs/ и положить: knowledge_base.md, project_state.md,
  turbobaby_faq_v1.md, park_list.md. Claude Code читает их + CLAUDE.md как контекст.
- Перенос: перетащить файлы в VS Code проводник (папка docs) ИЛИ через git (commit на ПК → git pull на сервере).

РАЗДЕЛЕНИЕ РОЛЕЙ:
- Claude Code (сервер) = ВСЁ про код/сервер: писать, править, тестировать, деплоить, этапы 5-6,
  новые модули, рефакторинг. Деятель — делает быстро, но решения/контроль за Филиппом.
- Чат на сайте (этот) = думать ДО кода: архитектура, стратегия, риски, новые направления,
  второй взгляд на необратимое. "Что строить и зачем" — здесь; "как в коде" — Claude Code.

⚠️ Claude Code по натуре исполнитель: даёшь задачу — делает. Большие развилки сначала
проектировать (обсудить), потом давать ему оформленную задачу.

## 🧠 ИДЕЯ: общий "мозг" базы знаний на Google Drive (для Claude-сайт + Claude Code)
Цель Филиппа: единое место для базы знаний, доступное обоим Claude.
РЕАЛЬНОСТЬ (граница):
- Код → остаётся в Git/на сервере (Drive для кода не годится). Я (сайт) в приватный репо не вижу.
- База знаний/документы → МОГУТ жить на Google Drive как общая папка:
  - Claude-сайт читает/пишет через Drive-коннектор (нестабилен, по запросу, не real-time).
  - Claude Code (сервер) читает через Bridge — НО нужен НОВЫЙ endpoint "прочитать Google Doc по ID"
    (текущий Bridge умеет только Sheets-таблицы и запрограммированные действия, не произвольные доки).
- Формат: база знаний как Google Docs (их читают оба). project_state.md на Drive = живой мост.

ЧТОБЫ ЗАРАБОТАЛО — 2 доработки:
1. Проверить/переподключить Drive-коннектор к info@turbophuket.com (у нас падал).
2. Задача для Claude Code: добавить в Bridge.gs endpoint read_doc (читать Google Doc/файл по ID
   через DocumentApp/DriveApp), вернуть текст. Тогда Claude Code видит базу знаний с Drive.

ВЫВОД: "всё в одном месте для двоих" достижимо для ЗНАНИЙ (Drive), НЕ для кода. Полный live-доступ
Claude-сайт к серверу невозможен (изоляция + приватность) — и это правильно для безопасности.

## ✅ ОБЩИЙ «МОЗГ» (Brain) — СДЕЛАНО (1 июня)
База знаний живёт на Google Drive как Google Docs, читают оба Claude:
- Claude-сайт — через Drive-коннектор (папка «TurboBaby Brain»).
- Claude Code (сервер) — через Bridge: `read_doc` (по `id` или `name`) и `list_brain`.

Реализация:
- Новый файл Bridge **ReadDocs.gs** (зона: read_doc/list_brain + одноразовая setupBrain).
  В Bridge.gs добавлено ТОЛЬКО два case в роутер doGet. Деплой — NEW VERSION.
- Манифест {логическое_имя → doc_id} хранится в Script Properties (ключ `BRAIN_MANIFEST`),
  без хардкода ID в коде. Дубликат-оглавление — Google Doc «KB_index» в папке Brain.
- Проверено через Bridge: list_brain отдаёт манифест; read_doc читает все 4 дока по name и по id;
  ошибки мягкие (unknown_name / missing_param).

ID базы знаний (Brain):
- Папка Brain : `1uWqHsxk7aEWoSNqaUBMmqkYOh2UKYLkY`
- knowledge_base (KB_knowledge_base): `1TwNzB_bGw1aHD1ThEuT9V6EgG7p70Tud6WeB0sKDt1Y`
- project_state (KB_project_state) : `1cRedmoltyntz5oNqjB3S3MloLftefEN9HlY7QPOHMQ8`
- faq           (KB_faq)           : `1tv8Y-K3gLyT9Y0mXyYXZLf2c2rEzPMLHPzvg4lGqs98`
- park_list     (KB_park_list)     : `1jD3VJGeoET8Yf5ma6iZPmwwmuw_yIYyP2A4N5RAEZho`
- index         (KB_index)         : `1klCUC5domgR6t-3hu-Yc7UC75hgf-y6-rCILFnN7JJs`

### 🔁 ПРАВИЛО СИНКА (источник правды = git, Drive = генерируемое зеркало)
- Истина живёт в репозитории: `docs/knowledge_base.md`, `docs/project_state.md`,
  `docs/turbobaby_faq_v1.md`, `docs/park_list.md`. Правим — здесь, через Claude Code (дифф+коммит+push).
- Google Docs в папке Brain (KB_*) — это ЗЕРКАЛО. Руками их не редактируем (перезатрутся при синке).
- ОБЫЧНЫЙ СИНК после правки .md = одна команда Claude Code «синкни мозг» (или конкретный док):
  Claude Code читает docs/*.md → для каждого вызывает write_doc(name=<ключ>, text=<содержимое>) через
  Bridge POST. Тело KB_-дока перезаписывается (clear+setText), doc_id и манифест НЕ меняются.
  Ре-деплой Bridge НЕ нужен (код не трогается). Никаких ручных действий в Drive.
  Ключи: knowledge_base, project_state, faq, park_list.
- write_doc (POST в Bridge, файл ReadDocs.gs): защита от затирки — пустой text запрещён
  (empty_text); неизвестное имя → unknown_name; перезапись только существующего дока по id из манифеста.
- setupBrain() — ТОЛЬКО аварийное пересоздание с нуля (если папку Brain/доки удалили или манифест
  потерян). В обычном цикле НЕ используется. Процедура аварийного: залить .md в папку Brain
  (имена точь-в-точь, без авто-конвертации) → Run setupBrain → пересоздаст доки + манифест.
- Манифест (BRAIN_MANIFEST в Script Properties) не редактируем вручную. Свериться: ?action=list_brain.
- Проверка после синка: read_doc name=<ключ> → длина по code points = длине git-исходника.

### ID базы знаний (Brain) — дубль для быстрого доступа
- Папка Brain : `1uWqHsxk7aEWoSNqaUBMmqkYOh2UKYLkY`
- knowledge_base : `1TwNzB_bGw1aHD1ThEuT9V6EgG7p70Tud6WeB0sKDt1Y`
- project_state  : `1cRedmoltyntz5oNqjB3S3MloLftefEN9HlY7QPOHMQ8`
- faq            : `1tv8Y-K3gLyT9Y0mXyYXZLf2c2rEzPMLHPzvg4lGqs98`
- park_list      : `1jD3VJGeoET8Yf5ma6iZPmwwmuw_yIYyP2A4N5RAEZho`
- index          : `1klCUC5domgR6t-3hu-Yc7UC75hgf-y6-rCILFnN7JJs`
